# Leadly — Plataforma multi-tenant de asesores de WhatsApp con IA

> Este archivo es la fuente de verdad técnica y de backlog del proyecto — explica el *por qué*, el *cómo* y el *qué falta*. Historial de decisiones previas en [plan.md](plan.md).
>
> **Aviso de depuración (2026-09-06)**: este archivo estuvo sin actualizar desde el 2026-08-29 hasta hoy pese a una semana entera de trabajo real (módulo POS completo, facturación electrónica DIAN, rediseño de Calendario/Dashboard, rebranding a Lexy — ver la entrada del 2026-09-06 más abajo). Además, las secciones de referencia **3 (Modelo de datos)**, **4 (Layouts y navegación)** y **6 (Edge Functions)** describen el esquema y la arquitectura del MVP original (tablas `crm_*`, 4 Edge Functions, nav de 5 módulos) — ese esquema fue reemplazado por uno sin prefijo (`clients`, `products`, `sales_orders`, etc.) desde el pivote a ERP del 2026-08-15, y hoy hay decenas de tablas y Edge Functions más. Se dejan esas secciones tal cual (reescribirlas a fondo es trabajo aparte, requiere catalogar el esquema actual completo) pero **no son confiables como estado vigente** — para eso, confiar en el código y en el historial cronológico de "Estado actual" de abajo, empezando por las entradas más recientes.

## Estado actual (2026-09-21, segunda ronda) — Causa real de la lentitud: las Edge Functions corrían en Virginia y la base está en Oregón

**Reporte del usuario:** abrir una orden tarda 2 a 3 s; al cargar un producto aparece "verificando stock" ~3 s y bloquea "Volver"; al salir y volver a entrar el resumen de precios tarda ~2 s en aparecer.

**Causa raíz, medida (no estimada):** el proyecto `leadly-portal` está en `us-west-2` (Oregón), pero Supabase ejecuta cada Edge Function cerca de quien la llama: las 207 llamadas a `calculate-order` de las últimas 24 h (desde Colombia) corrieron en `x-sb-edge-region: us-east-1` (Virginia). Cada consulta que la función le hace a la base cruzaba el país, y `calculate-order` hace ~12 en serie. Tiempos de ejecución del lado del servidor en esas 24 h (logs `function_edge_logs`, campo `execution_time_ms`) -- **línea base para comparar**: `calculate-order` p50 1.381 ms / p90 2.525 ms / máx 3.743 ms (207 llamadas); `create-order` p50 2.015 / p90 5.094 / máx 5.623 (10); `list-sales-orders` p50 559 / p90 1.894 / máx 2.138 (20). Esto explica los tres síntomas: "verificando stock" es el guardado de `calculate-order` más su preview, y el resumen de precios es ese mismo preview al montar.

**Cambio (solo frontend, un punto):** `fetchWithSessionGuard` en `lib/supabaseClient.ts` -- por donde ya pasa todo el fetch del cliente supabase-js -- agrega `?forceFunctionRegion=us-west-2` a las URLs `/functions/v1/…` (lógica pura en `lib/functionsRegion.ts`; REST/Auth/Storage/RPC no cambian; respeta un `forceFunctionRegion` ya presente; soporta string, `URL` y `Request`). **Query param y no header `x-region`, a propósito:** `supabase-js` 2.111 manda las dos cosas al usar `region`, pero un header custom dispara un preflight CORS y `_shared/cors.ts` (`Access-Control-Allow-Headers`: authorization, x-client-info, apikey, content-type) no lo permite; el query param no exige tocar CORS ni redesplegar funciones. Verificado con `curl` real contra `calculate-order`: sin forzar → `us-east-1`; con `x-region` o `forceFunctionRegion=us-west-2` → `us-west-2`. Reversión sin tocar código: variable `VITE_SUPABASE_FUNCTIONS_REGION=auto` (vacía = `us-west-2` por defecto). Solo `whatsapp-webhook` lee parámetros de su URL y lo llama Meta, no el navegador, así que no se ve afectado.

**Verificado:** `npm run lint` (0 errores) y `npm run build` limpios; script descartable que ejercita la reescritura (string, URL, Request con body y authorization, URL REST intacta, sin duplicar el parámetro, env `auto`). **No probado en el navegador con sesión real, y la mejora NO está medida:** falta desplegar/recargar el frontend y repetir la consulta de percentiles sobre `function_edge_logs` (`x_sb_edge_region` debe pasar a `us-west-2`) para comparar contra la línea base. El navegador de Colombia ahora llega a Oregón en un solo salto en vez de a Virginia, así que la latencia de la primera pieza sube un poco a cambio de bajar ~12 saltos internos.

**Pendiente, ya aprobado por el usuario pero NO hecho (se decidió medir la región primero):** (1) "Volver" sin bloqueo: navegar de inmediato y guardar en segundo plano con aviso global si falla (hoy espera el `flush()` por el cambio C4 de la Ola 1); (2) agregar productos sin esperar "verificando stock" (solo "Cobrar" debe esperar); (3) **tope de cantidad con el stock que ya trae el listado** -- el usuario relajó su regla "sin validaciones de stock en el frontend" solo para este límite visual de UX; `calculate-order` (409), `create-order` y `pos-checkout` siguen siendo la autoridad; (4) mostrar al instante el último total devuelto por el servidor mientras se refresca el preview; (5) abrir una orden más rápido: `OrderDetail.tsx` hace ~17 requests al montar, 7 de catálogo completo sin paginar, aunque el pedido esté confirmado o anulado; cargar los catálogos solo al editar y en paralelo. Si tras medir la región `calculate-order` sigue por encima de ~1 s, el siguiente paso es recortar sus ~12 viajes seriales (paralelizar ownership checks, `delete`+`insert` no atómico).

## Estado actual (2026-09-21, cuarta ronda) — Lo pendiente de rendimiento: preflight cacheado, guardado atómico del carrito, totales en el guardado, abrir una orden más rápido y tope de cantidad

**Pedido del usuario:** "me gusta, ahora es mucho más rápido, ahora agrega lo que queda pendiente" (la lista de la tercera ronda). Se hizo en tres frentes paralelos con archivos disjuntos y **varias rondas de review independiente**: cada review encontró bugs reales de datos/dinero, sobre todo en `OrderDetail.tsx`, por eso el trabajo se alargó.

**Medición previa (logs `function_logs`, línea `[timing]` de `calculate-order` v19, us-west-2):** preview 239-319 ms dentro del código (auth 69-141, profile 35-61, stock 115-147); save 372-895 (write 0-648). El tiempo que reporta la plataforma (~700 ms) es mucho mayor que el de nuestro código (~270-475): ~300 ms de piso fuera de la función. Cada consulta Edge→PostgREST cuesta ~60-70 ms aun en la misma región. **Con `calculate-order` v19 (recorte de viajes) solo hubo 6 llamadas medidas, p50 718 ms: sin mejora visible frente a v18 (738 ms) en esa muestra chica; la mejora que el usuario percibió viene sobre todo de la región, la cola de guardados y "Volver" sin bloqueo.** La medición de esta ronda (v20 + frontend nuevo) NO está hecha.

**En producción (desplegado y verificado el 2026-09-21):**
- **Migración `20260921130000_save_cart_lines_atomic.sql`:** función `public.save_cart_lines(p_cart_id, p_tenant_id, p_patch jsonb, p_items jsonb) returns jsonb`. En UNA transacción bloquea el carrito (`for update`), valida tenant y estado abierto (errores SQLSTATE `LX404` no existe/otro tenant, `LX409` no abierto), aplica solo las claves presentes del parche (null explícito pone null, clave ausente no toca) y reemplaza las líneas, devolviendo el carrito con `items` (forma de `select("*, items:cart_items(*)")`, orden de inserción). `security invoker`, `search_path=''`, `revoke` a `public/anon/authenticated` y `grant` solo a `service_role` -- **verificado en la base viva** (`anon`/`authenticated` sin `EXECUTE`, `service_role` con él; ningún aviso de `get_advisors` sobre la función). Corrige de raíz el solape de guardados: antes `calculate-order` hacía `update carts` + `delete cart_items` + `insert cart_items` sin transacción y dos guardados solapados podían duplicar líneas o dejar el carrito vacío. **Orden de despliegue obligatorio: migración primero, función después** (la v19 vieja sigue funcionando si la migración va primero).
- **`calculate-order` v20:** guarda un carrito existente con UNA llamada `rpc(save_cart_lines)` (precedencia de errores idéntica: 403 pertenencia → 400/404/409 carrito → 409 stock POS, cero escrituras antes de validar; un carrito nuevo sigue igual) y el guardado devuelve además `totals` con la misma forma y la misma función (`computeOrderTotals`) que el preview, calculado en paralelo con el chequeo de stock, usando el envío guardado en el carrito; no viaja en el 409 de stock. Review independiente APROBADO (probado con un Postgres 15 local descartable: sintaxis, parche null/ausente, claves no permitidas ignoradas, tenant ajeno).
- **`Access-Control-Max-Age: 7200`** en `_shared/cors.ts` (Chrome capa en 7200 s; antes el preflight se recordaba solo 5 s y cada llamada espaciada pagaba un `OPTIONS` de ~294 ms). Redesplegadas `calculate-order` (v20), `create-order` (v14), `pos-checkout` (v16) y `list-sales-orders` (v8), todas `verify_jwt: true`; verificado con `curl` que el `OPTIONS` devuelve el header. Las otras ~26 funciones que importan `corsHeaders` lo heredarán cuando se redespleguen.

**En el árbol de trabajo, revisado, SIN desplegar y SIN commit (frontend; el hosting sigue sin definirse):**
- **Frente C, POS:** `saveCartDraft` expone `totals`; `useOrderTotalsPreview` usa los totales del guardado solo si su firma (`orderTotalsSignature`: envío + product_id/variant_id/warehouse_id/nombre/sku/cantidad/precio/descuento, igualdad exacta) coincide con las líneas de pantalla, y `hold` evita duplicar el preview mientras se guarda exactamente lo que hay; **tope de cantidad** (límite de UX aprobado por el usuario, el backend sigue decidiendo) en cuentas abiertas (`searchAlwaysOpen`) y en venta rápida con el stock que ya trae el listado, sin tope para `track_inventory=false`, variantes sin elegir ni stock no cargado, y NO en Órdenes; `listStockByWarehouse` ahora pagina de a 1000 (el tope de PostgREST truncaba en silencio); refresco de stock tras un 409 y al recuperar foco (mín. 30 s), con reintento cada 15 s si la carga falla; prop nuevo `stockLoading` en `OrderItemsEditor` (oculta lo derivado de `stockRows` local pero SIEMPRE pinta en rojo los faltantes del servidor); detalles menores (un solo toast al dividir, "Cobrar" espera la cola de guardados y abre el pago con lo que devolvió el guardado, el autosave no corre mientras se cierra/cancela una cuenta).
- **Frente B, `OrderDetail.tsx` (abrir una orden más rápido):** los catálogos grandes (productos con embeds, categorías, marcas, stock de todo el tenant) solo se cargan si el pedido es editable; en confirmado/anulado/facturado solo los productos y el stock de sus líneas; ya no se lista todo el tenant de clientes ni de oportunidades (el selector busca en el servidor, `lib/api/orderContactSearch.ts`, sin match por `phone_prefix`, mínimo 2 caracteres, escapando comodines y comas del `.or()`); facturas en paralelo con el pedido; el desglose de impuestos solo se recalcula cuando cambia el pedido; el editor se pinta en cuanto llegan los ítems, con `stockLoading` mientras carga el catálogo. Requests al montar (por lectura del código, NO medidos): cotización existente ~17 → ~13, confirmado/facturado ~17 → ~10.

**Bugs reales que los reviews encontraron y quedaron cerrados en el frontend** (todos sin commit ni deploy, así que no llegaron a producción):
- **`useOrderTotalsPreview`:** `loading` podía quedar en `true` para siempre tras A(en vuelo)→B→A en <400 ms y dejar "Cobrar" deshabilitado en venta rápida (se reemplazó `requestedSigRef` por `inflightSigRef`, demostrado con un script de 8 escenarios/27 comprobaciones).
- **"Cobrar" con totales desfasados:** si un autosave daba 409 mientras se pulsaba Cobrar, el pago podía abrirse con el total de las líneas CON la línea rechazada (proponía cobrar de más); ahora se usan los totales solo si la firma coincide, si no se pide un preview fresco.
- **"Confirmar venta" con los ítems sin cargar** (incluye un riesgo que introdujo la carga diferida sobre mi cambio "Confirmar guarda antes" de la Ola 1): guardaba una lista vacía, borraba las líneas reales y confirmaba una venta vacía (misma clase del incidente #28245). Ahora se deshabilitan Confirmar/Crear pedido mientras no carguen los ítems (o el carrito retomado) y el catálogo, con una guarda interna en `saveDraft`/`handleStatusSelect`/`handleCreate`, reinicio completo del estado al cambiar de id y descarte de respuestas tardías del pedido anterior (`idRef`).
- **Regla general nueva, "desconocido = bloqueado"** (`lib/loadStatus.ts`: `LoadStatus = loading|ready|error`, un reintento nunca limpia un error, solo el éxito pone `ready`): todo estado derivado de una carga (facturas, despacho, pagos, saldo de crédito, ítems, catálogo) bloquea lo destructivo o monetario mientras carga, falla o se reintenta. Casos reales que cerró: "Anular" reaparecía durante el reintento de facturas y podía anular un pedido con factura DIAN aceptada revirtiendo stock (ningún trigger bloquea `confirmada → cancelada`, solo la UI); un fallo al cargar pagos dejaba `payments=[]` y `PaymentDrawer` ofrecía cobrar el total completo otra vez (pago duplicado); un despacho no consultado se trataba como "sin despachar".
- **Cola de escrituras del pedido** (`enqueueSave` en `OrderDetail`, mismo patrón que `enqueueCartSave` del POS): "Confirmar" corría en paralelo con un autosave en vuelo y, como `persistOrderItems` borra e inserta sin transacción, podía duplicar líneas y descontar stock doble. Ahora autosave, confirmar, crear y cancelar borrador van en una sola cola, y "Confirmar venta"/"Crear pedido" se deshabilitan mientras hay un guardado en vuelo.
- Menores: búsqueda de clientes que borraba resultados vigentes con una respuesta vieja tardía (cambio mínimo en `ClientPickerCard`: bandera `cancelled` + `setSearching(false)`, que **también usa el POS**), `getCart` sin `.catch` con `?cart=` en la URL, resumen de totales en "Calculando…" eterno si fallaba, `listCommentsForOrder` sin `.catch`, banner con Reintentar para el despacho, autosave que sobrevivía a "Confirmar", y `loadCatalog('lines')` sin guarda por `idRef`.

**Verificado:** `npm run lint` (0 errores; 64 warnings frente a 63 en HEAD, la diferencia son 2 `exhaustive-deps` de `PosTabAccount`; 0 warnings en `OrderDetail`) y `npm run build` limpios; `deno check` limpio en las funciones tocadas; migración probada en un Postgres 15 local descartable y comprobada en la base viva. **No probado en el navegador con sesión real.** Los conteos de requests son por lectura del código, no medidos.

**Pendiente / deuda conocida, no corregida:**
- **Backend:** `persistOrderItems` (pedidos con `order_id`) sigue borrando e insertando SIN transacción -- la corrección de raíz sería una función atómica análoga a `save_cart_lines`; hoy solo la mitiga la cola del frontend. Si el `update` de la cabecera falla, las líneas de un carrito existente ya no se tocan por separado (lo cubre la transacción), pero antes de la RPC no era así. Ningún trigger bloquea `confirmada → cancelada` con factura transmitida (solo la UI). `getUser()` cuesta 70-140 ms por llamada: se podría leer el usuario del JWT que el gateway ya verificó, pero dejaría de detectar sesiones revocadas hasta que expire el token -- decisión del usuario, no tomada.
- **Frontend, informativos anotados por los reviews:** paginación con `offset` de `listStockByWarehouse` puede duplicar o saltar una fila con inserciones concurrentes; una cotización se bloquea por facturas desconocidas aunque no puede tener factura; con pagos no cargados el mensaje de "saldo pendiente" muestra el total; si `getClient` falla el selector dice "Selecciona un cliente" sin reintento; `handleCancelDraft` marca `cartConsumedRef` antes de `deleteCart` (un borrado fallido deja el autosave apagado); autosave espurio a los 2 s si los ítems llegan antes que el pedido (preexistente); el catálogo todo-o-nada incluye marcas y categorías que no hacen falta para confirmar; para quien tiene `sales.edit_invoiced_order` puede programarse un guardado solo-cliente tras confirmar (inofensivo, preexistente); escanear un código antes de que llegue el catálogo da "no encontrado".
- **Sigue pendiente de rondas anteriores:** unir guardado y preview en el resto de pantallas, tablas `_migration_client_map`/`_migration_product_map` sin RLS y expuestas en `public` (aviso `rls_disabled_in_public`, nivel ERROR de `get_advisors`, sin relación con este trabajo), 51 claves foráneas sin índice y `pg_trgm` sin instalar.

## Estado actual (2026-09-21, tercera ronda) — Medición tras la región, toast en vez de cuadro rojo, "Volver" sin bloqueo y `calculate-order` con menos viajes

**Medición tras forzar la región a us-west-2** (logs `function_edge_logs`, 25 llamadas; frente a la línea base de 210 llamadas en `us-east-1`): `calculate-order` p50 1.376 → 716 ms, p90 2.521 → 1.054 ms, máx 3.743 → 1.130 ms (~-48 % en la mediana). Lo que quedaba: (1) **piso de plataforma de ~300 ms por invocación** -- hasta el `OPTIONS` de preflight, que no toca la base, tarda 282-381 ms, y las ráfagas calientes no bajan de ~650 ms, así que no es solo arranque en frío; (2) **el navegador repite el preflight en cada llamada**: ninguna función manda `Access-Control-Max-Age` (verificado en `_shared/cors.ts`), y sin él Chrome recuerda el preflight solo 5 s -- **no corregido todavía**, es una línea en `cors.ts` más redesplegar las funciones que la importan (`calculate-order`, `create-order`, `pos-checkout`, `list-sales-orders`), ganancia esperada ~300 ms por llamada espaciada; (3) ~400 ms de trabajo propio de la función = ~11 viajes seriales a PostgREST; (4) cada edición dispara dos llamadas seguidas a la misma función (guardado y preview del resumen), por eso "verificando stock" tardaba ~3 s.

**Cambios de esta ronda:**
- **Toast en vez de cuadro rojo (pedido del usuario, con captura):** los errores transitorios de acciones del POS (guardado rechazado por stock, guardado, cobro, cerrar, cancelar, dividir, escaneo, DIAN) salen por `useToast().error` y se quitaron los bloques rojos inline y el estado que los alimentaba. Se mantienen las pantallas de error de CARGA con "Reintentar" (no son mensajes transitorios). Se eliminó el mensaje "Verificando stock…" y el `disabled` de "Cobrar"/"Dividir" por ese estado: sin él no se cobra con datos viejos porque `handleChargeAll`/`handleOpenSplit` esperan el guardado pendiente, no abren el pago si devuelve faltantes, y `handleCharge` pide totales frescos si el preview está en vuelo o falló; la puerta dura sigue siendo el 409 de `create-order`/`pos-checkout`.
- **"Volver" sin bloqueo:** navega de inmediato y guarda en segundo plano; si falla, toast.
- **Cola de guardados por carrito** (`enqueueCartSave`/`waitForCartSaves` en `lib/api/carts.ts`, un `Map` a nivel de módulo, sobrevive al desmontaje): **bug real encontrado por el review** -- sin el bloqueo "verificando", dos `calculate-order` podían solaparse, y como esa función guarda con "borra todas las líneas + inserta todas" SIN transacción, podían intercalarse y duplicar líneas o dejar el carrito vacío (y `create-order` cobraría los duplicados); además "Volver" en segundo plano dejaba reabrir o cobrar desde el listado un carrito viejo o a medio escribir. Ahora autosave, Cobrar, Dividir y Volver van por una sola cola (nunca dos en vuelo, cada tarea guarda el snapshot más reciente, un fallo no rompe la cadena) y reabrir la cuenta, cobrar/cancelar/cerrar desde el listado y dividir esperan esa cola. **La causa de fondo sigue viva en el backend** (borrar+insertar sin transacción): una función de base de datos atómica sería la corrección real, no hecha.
- **`calculate-order` con menos viajes** (desplegada, versión 19, `verify_jwt: true`, sin migraciones; review independiente APROBADO, `deno check` limpio en `calculate-order`, `stockAvailability`, `create-order` y `pos-checkout`): tras `getUser()` (se mantiene: el 401 "Invalid session" es deliberado) van en paralelo `profiles` y el carrito existente; luego las comprobaciones de pertenencia, el chequeo de stock y, en preview, `isTenantTaxEnabled` + la consulta de impuestos de productos; `findStockShortfalls` pide `products` y `product_stock` a la vez (resultado idéntico en 15 de 15 casos comparados contra HEAD con un cliente falso); en un carrito existente el `update carts` (ahora también con `.eq("tenant_id")`) corre en paralelo con el reemplazo de líneas, y se eliminó la relectura final (la respuesta `{cart:{...fila, items}}` conserva la forma de `select("*, items:cart_items(*)")`); un carrito nuevo omite el `delete`. Orden de precedencia de errores idéntico (403 pertenencia → 400/404/409 carrito → 409 stock POS), sin escrituras antes de las validaciones. Cada llamada deja una línea `[timing] {"mode":"preview|save","ms":{auth,profile,checks,stock,write,total}}` en los logs de la función (sin ids ni nombres). **Diferencia aceptada:** si el `update` de la cabecera falla, las líneas ya pueden haberse reemplazado (antes el 500 evitaba tocarlas); y el orden de `items` es el de inserción.
- `create-order` y `pos-checkout` NO se redesplegaron: siguen con su copia anterior de `findStockShortfalls` (mismo resultado).

**Verificado:** `npm run lint` (0 errores; 64 warnings frente a 63 en HEAD, el +1 es un `exhaustive-deps` de `PosTabAccount`) y `npm run build` limpios; dos reviews independientes del frontend (el primero CHANGES_REQUESTED por los dos bloqueantes de concurrencia de arriba, el segundo APROBADO) y uno del backend (APROBADO). **No probado en el navegador con sesión real. La mejora de `calculate-order` de esta ronda NO está medida:** falta que el usuario use el POS y comparar `[timing]` y los percentiles de `function_edge_logs` contra 716 / 1.054 ms.

**Detalles menores conocidos, no corregidos** (del segundo review del frontend): `handleOpenSplit` puede mostrar un toast duplicado cuando falla el guardado (`flush` ya avisó y el `catch` avisa otra vez); si se agrega un producto sin stock y se pulsa Cobrar antes de que vuelva el 409 del autosave, el pago puede abrirse con los ítems/totales del clic, todavía con la línea rechazada (`create-order`/`pos-checkout` la frenan y recalculan en el servidor); un instante con la cuenta en 0 líneas en el listado si el realtime dispara un reload durante el guardado de fondo de Volver (se corrige solo con el `finally(reload)`); un autosave podría dispararse durante `deleteCart`/`closeCart` y dar un toast espurio (ventana muy chica); y un guardado en vuelo que falla tras salir puede mostrar el aviso ya fuera de la cuenta.

**Pendiente, en orden de ganancia esperada:** (1) `Access-Control-Max-Age` en `_shared/cors.ts` + redespliegue de las funciones de ventas; (2) unir guardado y preview en una sola llamada (el guardado ya calcula los totales); (3) función de base de datos atómica para reemplazar `cart_items` (corrige además la causa de fondo del solape); (4) abrir una orden más rápido (`OrderDetail.tsx`: ~17 requests al montar, 7 de catálogo completo); (5) tope de cantidad con el stock que ya trae el listado (aprobado por el usuario como límite de UX, backend sigue decidiendo; no implementado).

## Estado actual (2026-09-21) — Diagnóstico completo de POS y Órdenes, y Ola 1 de arreglos seguros

**Pedido del usuario:** "analiza bien mi proyecto y dame el diagnóstico en este flujo de POS y de órdenes". Se investigó con tres exploradores de solo lectura (Órdenes del portal, POS fuera de lo ya visto, capa de base de datos) más verificaciones propias contra la base viva (`EXPLAIN`, avisos de rendimiento, logs). **Nada está medido en tiempo de punta a punta:** el rendimiento se razona por conteo de round-trips; los hallazgos de concurrencia son razonamiento sobre el código, ninguno tiene un caso reportado.

**Tesis del diagnóstico:** lo urgente es corrección (dinero, DIAN), no velocidad. Lo lento del POS viene sobre todo de la red -- 10 a 20 viajes seriales en `create-order`/`pos-checkout` y la DIAN síncrona dentro del drawer --, no de la base (confirmar una venta POS de N ítems son ~70 sentencias en una transacción, pero eso no domina).

**Ola 1 (hecha, bajo riesgo):**
- **Índice `sales_orders_tenant_created_at_idx`** (`(tenant_id, created_at desc) where deleted_at is null`, migración `20260921120000`, aplicada con `db push`). Verificado con `EXPLAIN` en el tenant con 28.478 pedidos: el listado de Órdenes pasó de Seq Scan + top-N heapsort (467 ms en frío, 1300 buffers) a Index Scan (0,2 ms, 26 buffers). `stock_movements(reference_id)` NO se creó: la tabla tiene 1.491 filas y no lo justifica todavía.
- **`sales-order-items` dada de baja** (borrada de Supabase el 2026-09-21; el código queda en el repo marcado como DEPRECADA, no redesplegar): reescribía ítems y totales de cualquier pedido del tenant sin ningún candado (confirmado, facturado, despachado), ningún código de `leadly-app` la llamaba y no tuvo invocaciones en 24 h (el log solo cubre un día, no es concluyente sobre más tiempo).
- **Frontend, pérdida de datos y fallos silenciosos:** "Volver" en cuentas abiertas guarda antes de salir (si el guardado falla, muestra el error; reintenta si el cajero editó de nuevo; un 409 de stock no cuenta como fallo); "Confirmar" en `OrderDetail` guarda antes y no confirma si el guardado falla; `PosOpenTabs`/`PosTabAccount`/`Pos.tsx` dejan de mostrar listas vacías o spinners eternos y ofrecen "Reintentar"; si falla `getTenant` ya no se asume el modo de venta; una mesa recién abierta y vacía ya no aparece como "Ya cobrada"; el escáner muestra los errores de red. Dos bugs encontrados en el review y corregidos: flags de error compartidos que podían dejar un spinner sin salida, y textos i18n con `{{x}}` cuando la app interpola con `{x}` (salían como "{las bodegas}"; el mismo defecto estaba en `stockRejected` de la ronda del 2026-09-20 y se corrigió también).
- **Verificado:** `npm run lint` (0 errores; 64 warnings frente a 63 en HEAD, el +1 es un `exhaustive-deps` de `t`/`toast` en `PosTabAccount`) y `npm run build` limpios; review independiente con segunda pasada. **No probado en el navegador.**

**Pendiente, para decisión del usuario / Ola 3 (riesgo alto, NO tocado):**
- **Dinero y fiscal:** "Anular" desde el listado (`Orders.tsx`, `handleVoid` → `updateOrderStatus(id, 'cancelada')`) no respeta los candados de factura DIAN ni de despacho que sí aplica el detalle, y ningún trigger bloquea `confirmada → cancelada`; un pedido anulado conserva su factura `pending` y se puede enviar a la DIAN; borrar un pago a crédito/saldo a favor (o anular la venta) no revierte `credit_charges` ni `store_credit_redemptions`; el servidor no valida descuento ni envío (total negativo posible) ni tope de sobrepago; `pos-checkout` confirma la venta antes de insertar los pagos y sin transacción; la idempotencia por `checkout_token` se rompe si el fallo ocurre después de insertar el pedido; en venta rápida el ticket se imprime antes del envío a la DIAN y sale siempre como remisión; los permisos `sales.*` no se usan en Órdenes y `list-sales-orders` lee con service role. **Requiere primero una decisión de producto:** qué significa anular una venta con factura aceptada (¿nota crédito obligatoria?).
- **Concurrencia:** dos cajas pueden vender la última unidad (sin lock, y tres chequeos de stock con criterios distintos); el guardado del carrito es "borra todo e inserta todo" sin control de concurrencia; `set_order_number` con `max(number)+1` devuelve un 500 al perdedor (el índice único evita la corrupción); cancelar no es idempotente.
- **Rendimiento (Etapa 2, sin medir):** `OrderDetail.tsx` (3.115 líneas, ~85 hooks) hace ~17 requests al montar, 7 de catálogo completo sin paginar; cada autosave dispara ~12 viajes seriales en `calculate-order`; escáner con 3 viajes seriales sin caché; DIAN síncrona dentro del drawer (solo el correo/PDF es diferible); tope de 1000 filas de PostgREST en silencio (hoy 214 productos, pero Barriles ya tiene más de 1000 clientes); no hay `pg_trgm`, así que el `ilike '%x%'` de búsqueda no tiene índice; 51 claves foráneas sin índice (las relevantes: `cart_items`, `carts`, `credit_charges.sales_order_id`, `sales_invoice_items.order_item_id`).
- **Deuda técnica:** el autosave con snapshot está copiado tres veces (el POS ya corrigió bugs que Órdenes aún tiene); la fórmula de totales del resumen SQL es copia aparte de `computeOrderTotals`; `PosTabAccount` (~1.000 líneas, 33 `useState`) y `PosFastCheckout`; no existe cierre de caja, turnos ni arqueo; voseo en mensajes del backend.
- **Sin medir todavía:** tiempos reales de `create-order`, `pos-checkout` y la DIAN (Ola 2: `Server-Timing` por paso), y las marcas `[pos-open]` de la consola.

## Estado actual (2026-09-20, ronda 2) — POS: sin stock no se agrega, y el backend rechaza en vez de guardar en rojo

**Reversión explícita de la decisión del 2026-09-06** (`calculate-order` guardaba el borrador con la línea problemática y devolvía `stock_shortfalls` sin rechazar), pedida por el usuario solo para el POS:
- `calculate-order`, carrito con `carts.origin = 'pos'` (en un carrito existente se lee de la fila guardada, no del body): si `findStockShortfalls` devuelve faltantes, responde 409 `Sin stock suficiente para: "X" (disponible N, pedido M)` con `stock_shortfalls` y el carrito tal como quedó en el servidor, **antes de escribir nada** (el guardado reemplaza todas las líneas, así que rechazarlo entero = esa línea no se guarda). Excepción: línea de un producto con variantes sin variante elegida (nace así desde el buscador) no rechaza, porque `findStockShortfalls` la compara contra la clave sin variante y daría siempre 0; sigue saliendo en rojo y `create-order`/`pos-checkout` la frenan al cobrar. Órdenes del portal (`origin 'portal'`), pedidos reales (`order_id`) y `preview` no cambian (siguen devolviendo `stock_shortfalls` sin rechazar; el preview de venta rápida no guarda nada).
- Frontend: `saveCartDraft` lanza `CartStockRejectedError` con el carrito del servidor y `PosTabAccount` vuelve a esa lista de líneas (sin línea fantasma) y muestra el error. Buscador del POS (venta rápida y cuentas abiertas): productos/variantes agotados aparecen inhabilitados ("Agotado"); `track_inventory=false` nunca se inhabilita; el estado sale del stock ya cargado y el 409 es la red de seguridad. `ProductSearchBox` recibe `isDisabled` y `handleSelect` no selecciona un resultado inhabilitado por ningún camino (clic, Enter, flechas+Enter): el review encontró que antes solo se frenaba el clic. Si el 409 llega por una línea agotada ya existente, el mensaje dice cuál línea hay que quitar y que el último cambio no se aplicó.
- **Desplegado** el 2026-09-20: `calculate-order` versión 18, `verify_jwt: true`, sin migraciones. Verificado por `lint`, `build` y `deno check`; **no probado en el navegador**.
- **Límites conocidos, no ocultos:** (1) un carrito nuevo toma `origin` del body, así que quien llame la API directo puede crearlo como `portal` y evitar el 409; `create-order` y `pos-checkout` siguen siendo la puerta dura al cobrar. (2) Escanear el código de barras de un producto agotado no lo inhabilita en el buscador: en venta rápida entra en rojo y `pos-checkout` lo rechaza; en cuentas abiertas el 409 lo revierte. (3) Una cuenta abierta que ya tenía una línea agotada rechaza cualquier edición hasta quitarla. (4) Órdenes del portal (`origin 'portal'`), edición con `order_id` y `preview` conservan el comportamiento del 2026-09-06 (guardan/devuelven `stock_shortfalls` sin rechazar).

## Estado actual (2026-09-20) — POS, Etapa 1: el faltante de stock se ve al cargar y abrir una cuenta deja de esperar cargas redundantes

**Pedido del usuario:** el POS se sentía lento al abrir una cuenta, al cobrar y al cerrar una mesa, y el stock "se validaba al cobrar" en vez de al cargar el producto al carrito. Se investigó con tres exploradores en paralelo (solo lectura) y se acordó con el usuario hacerlo por etapas: esta primera es solo frontend y de bajo riesgo; el cobro/cierre queda para la Etapa 2, decidida con datos medidos.

**Causa real del stock:** el backend estaba bien (`calculate-order` ya devuelve `stock_shortfalls` sin rechazar el guardado, y `create-order`/`pos-checkout` usan la misma `findStockShortfalls`). Fallaba el frontend de **cuentas abiertas** (`PosTabAccount.tsx`): cada edición limpiaba los faltantes y dejaba "Cobrar" habilitado hasta que volvía el guardado (2 s de debounce si se editaba cantidad/bodega/variante), y `handleChargeAll` hacía `flush()` y seguía a cobrar sin mirar los faltantes que ese guardado devolvió, por eso el 409 recién aparecía en el drawer de pago. Al abrir una cuenta existente tampoco se calculaba ningún faltante hasta la primera edición, y "Dividir cuenta" no se bloqueaba.

**Cambios** (solo `leadly-app`; sin tocar Edge Functions, migraciones, triggers, RLS ni DIAN):
- "Cobrar" y "Dividir cuenta" quedan deshabilitados en estado "Verificando stock…" (`pos.tabs.verifyingStock`) mientras haya cambios sin sincronizar o un guardado/preview en vuelo. `flush()` devuelve los faltantes y `handleChargeAll`/`handleOpenSplit` no abren el pago ni el selector si hay alguno. Al abrir una cuenta con productos, los faltantes salen del preview que ya corría al montar. Sigue vigente la regla del proyecto: el frontend solo usa lo que devuelve el backend, nunca compara contra stock cacheado. Si un guardado falla, "Cobrar" se rehabilita y el clic reintenta.
- Venta rápida (`PosFastCheckout.tsx`): `canCharge` exige `!totalsLoading`.
- Abrir cuenta (`PosOpenTabs.tsx`): el carrito que devuelve `calculate-order` se pasa como `initialCart` y la cuenta se pinta sin `getCart` ni `listChargesFromCart` redundantes; el cliente Consumidor Final se precarga y se reutiliza; el realtime de `carts` ya no recarga el listado mientras hay una cuenta abierta.
- `lib/posPerf.ts` (nuevo): `performance.mark` + `console.debug` con prefijo `[pos-open]` (clic → carrito-creado → cuenta-pintada → catalogo-listo), para medir en el navegador.

**Bug de concurrencia encontrado en el review, ya corregido:** con el bloqueo por "cambios sin sincronizar", si el cajero editaba, arrancaba un guardado y revertía el cambio antes de que respondiera, el guardado marcaba como sincronizado un estado que ya no era el de la pantalla y ningún efecto reintentaba, dejando "Cobrar" deshabilitado para siempre. Se arregló agregando `syncedSnap` a las dependencias del efecto de autoguardado (sin bucle: cuando coincide con el snapshot actual el efecto retorna) y descartando los faltantes de una versión vieja (`currentSnapRef`).

**Verificado:** `npm run lint` (0 errores; los warnings ya existían, el de `PosTabAccount.tsx` ahora suma `initialCart` a sus dependencias faltantes) y `npm run build` limpios; review independiente con una segunda pasada sobre la corrección. **No probado en el navegador**: pendiente que el usuario lo pruebe en vivo y lea los `[pos-open]` de la consola.

**Pendiente / Etapa 2, sin decidir hasta tener mediciones** (evidencia en la sesión; ninguna cifra medida, todo por conteo de round-trips):
- Cobro y cierre de mesa: el envío a la DIAN corre síncrono dentro del drawer (~15 esperas en serie, y al aceptar espera además el PDF y el correo, que sí son diferibles); `create-order` hace ~20 esperas secuenciales; los pagos se registran uno por línea desde el navegador (`pos-checkout` ya los inserta en bloque); el post-cobro relee carrito, cargos y lista; confirmar la venta dispara un movimiento de stock por ítem y reserva la factura aunque no se pida facturar (decisión de producto). Cualquier cambio ahí es riesgo alto (ya hubo bugs de doble cobro y stock).
- Abrir cuenta en backend: `calculate-order` hace 6 llamadas seriales para un carrito vacío y un `delete` inútil; conservar el `.eq("tenant_id")` al tocarlo. Y la ráfaga de 1000 productos con 6 embeds al montar `PosTabAccount`, no diferida a propósito.
- Huecos menores de stock: `handleChargeFromList` no mira faltantes (el servidor igual los rechaza), el buscador de `OrderItemsEditor` no muestra stock, un producto con variantes agregado desde el buscador nace sin `variant_id` y da un falso faltante hasta elegir variante, y el preview de la venta rápida deja un frame sin `loading`.

## Estado actual (2026-09-14) — Asistente de ventas por WhatsApp: el stock se dice en la consulta, sin datos pedidos dos veces, y formato de WhatsApp en código

**Origen: prueba real del usuario por WhatsApp sobre TecnoNova (2026-09-13).** El cliente eligió un control "azul", dio nombre, documento y dirección, recibió "pedido confirmado" y recién después le dijeron que no había stock; al cambiar a "negro" le volvieron a pedir la dirección; y los mensajes llegaban con `**doble asterisco**`. Usuario: "yo siendo un cliente ya abandono el proceso de compra con tanta cosa".

**Causa real, sacada de `ai_tool_executions` (no era falta de stock):** el azul tenía 5 unidades. El pedido #48 era una cotización abandonada del día anterior con 50 unidades; `add_item_to_quote` le sumó 1 (51). Se creó y confirmó bien el #49. Con `payment_options` en la mano, el modelo volvió a llamar `confirm_quote` en vez de la herramienta de pago, y `resolveLatestOrder` tomó "la última cotización pendiente": la #48 con 51 unidades, de ahí el `insufficient_stock`. La dirección se había guardado solo como facturación, y el contexto del modelo mostraba las direcciones por etiqueta ("sin nombre (default)"), sin su texto, con una instrucción que lo empujaba a aplicarlas a mano.

**Cambios en código** (`whatsapp-ai-tools`, `whatsapp-ai-respond`, `_shared/aiTools.ts`, `_shared/whatsapp.ts`):
- `list_catalog_products` y `list_product_variants` devuelven `in_stock` (sí/no, nunca la cantidad). **Revierte a propósito** el criterio de agosto de no exponer stock al mirar el catálogo.
- El pie de foto (`buildProductImageCaption`) lista solo las referencias con stock, y dice "Agotado por ahora" si no queda nada.
- `create_quote`/`add_item_to_quote` rechazan antes de escribir nada si una línea pide más de lo disponible (`assertStockForQuote`, sobre `findStockShortfalls`), con la cantidad disponible en el error. Revierte la decisión del 2026-08-10: la oportunidad ya la registra `create_opportunity` desde el primer interés.
- **La IA no toca pedidos anteriores.** Corrección explícita del usuario en la misma ronda, que revirtió una primera versión que anulaba sola las cotizaciones viejas: "la ia no debe tocar cotizaciones viejas ni nada de eso a menos que el cliente lo pida". `resolveTargetOrder` define sobre qué pedido actúan `add_item_to_quote`/`confirm_quote`/`cancel_quote`/`save_contact_address`:
  - **Sin `order_number`:** solo el pedido MÁS RECIENTE del contacto, y solo si sigue en cotización y se tocó en las últimas 12 h (`OPEN_QUOTE_MAX_AGE_HOURS`, sobre `updated_at`). Nunca cae a una cotización más vieja cuando el más reciente ya se confirmó, que es exactamente lo que falló en vivo.
  - **Con `order_number`** (el cliente nombró un pedido y pidió modificarlo): actúa sobre ese, sin límite de antigüedad, y solo si sigue en cotización.
  - `get_quote_status` acepta `order_number` y devuelve `can_modify`.
- Si el pedido más reciente ya está confirmado, `confirm_quote` le dice al modelo que use la herramienta de pago en vez de reintentar.
- **Direcciones confirmadas con el cliente, nunca asignadas solas.** Corrección del usuario en la misma ronda: la primera versión usaba la de facturación como envío sin preguntar. El flujo quedó así:
  - `create_quote` devuelve `saved_addresses` (en texto, sin repetidas) y el modelo pregunta en un solo mensaje si envía a esa dirección y factura con los mismos datos.
  - Si no hay ninguna, o el cliente dice que no, pide la nueva y en ese mismo mensaje pregunta si se usan también para facturación.
  - `confirm_quote` bloquea con `address_confirmation_required` (más `missing` y `saved_addresses`) mientras el pedido no tenga aplicadas las dos direcciones.
  - Al confirmar devuelve `shipping_address` y `billing_address` en texto.
  - Aplica solo al flujo de la IA: el trigger de confirmación sigue resolviendo direcciones por defecto en los demás caminos.
- `buildCustomerContext` muestra el texto real de cada dirección y para qué sirve.
- **Enlace de la tienda entregado por la herramienta, no escrito en el prompt.** Encontrado en vivo el mismo día, en dos conversaciones de TecnoNova: ante "me podrías dar el catálogo" y "qué tienen", la IA respondió solo con las categorías y nunca compartió la tienda, aunque el enlace estaba escrito en el `system_prompt` con la instrucción de compartirlo.
  - `list_catalog_categories` devuelve `storefront_url` siempre que el tenant tenga la tienda activa (`storefront_enabled` + `storefront_slug`). `list_catalog_products` también, con `category`/`brand` o con una búsqueda que no deja un único producto.
  - Si una herramienta devolvió el enlace en ese turno y el modelo no lo incluyó, `ensureStorefrontLink` (en `whatsapp-ai-respond`) lo agrega al final.
  - La URL base no existía en ningún lado del backend (la tienda la arma el frontend): quedó como `PUBLIC_APP_URL` en `whatsapp-ai-tools`, sobrescribible con un secreto del mismo nombre y con valor por defecto `https://leadly.lexycolombia.com`.
  - El prompt de TecnoNova ya no tiene el enlace escrito a mano.
- **Enlace de cada producto (`product_url`)**, pedido del usuario el mismo día. `list_catalog_products` lo devuelve por producto, `list_product_variants` a nivel del producto, y el pie de la foto agrega "Ver en la tienda: …" antes de la descripción, para que un recorte por largo nunca se lleve el enlace. `buildProductUrl` arma la misma ruta que el frontend (`ProductCard.tsx`: `/tienda/:slug/producto/:slug-o-id`) y solo lo devuelve si la tienda está activa y el producto tiene `is_visible_in_catalog`; si no, el enlace terminaría en "no encontrado". Los textos anteriores prohibían compartir enlaces a productos sueltos: la migración `20260914180000` cambia esa regla en `catalogo` y en TecnoNova. En una lista de varias opciones sigue alcanzando con el enlace general de la tienda.
- **Dirección elegida por número de opción.** `saved_addresses` trae `option` (1, 2, 3) y `save_contact_address` acepta en `address_id` el id real o ese número (`resolveAddressReference`, contra la misma lista y el mismo orden que recibió el modelo). Pasó en vivo dos veces: el cliente respondió "1" y el modelo mandó `"1"` como `address_id`. La primera corrección fue solo un error que pedía el id real, y el modelo, en vez de reintentar, le volvió a preguntar la dirección al cliente. Si el número no existe, el error lista las opciones válidas con su id y pide reintentar en el mismo turno.
- `toWhatsappMarkup` convierte `**negrita**`, títulos `#`, `__cursiva__`, `~~tachado~~` y enlaces `[texto](url)` a formato WhatsApp sobre todo lo que responde la IA. Pedirlo en el prompt no había alcanzado.

**Prompts** (migraciones `20260914150000`, `20260914160000` , `20260914170000` , `20260914180000` y `20260914190000`; la segunda reescribe de nuevo `ventas` y el prompt de TecnoNova con la corrección del usuario, y la tercera agrega `storefront_url` con reemplazos que abortan la migración si el texto de origen no existe): fragmentos globales `catalogo` y `ventas` **reescritos completos**, no con `replace()`, porque un `replace()` cuyo texto de origen no coincide no falla ni avisa. Prompt de TecnoNova reescrito en tuteo con las mismas reglas. El acoplamiento skills ↔ código de la entrada de abajo aplica igual: estos fragmentos describen `in_stock`, `saved_addresses`, `order_number`/`can_modify`, `address_confirmation_required`, `storefront_url`, `product_url` y el límite de 12 h, así que si cambia alguno, se actualizan en la misma ronda.

**Datos de prueba sin tocar:** del contacto de la prueba quedan el #48 (cotización, 51 unidades), el #49 (confirmado, azul) y el #50 (cotización, negro). La IA ya no los toma: no son el pedido en armado de una conversación nueva, y solo los tocaría si el cliente nombra su número.

**Sin verificar de punta a punta:** tipos limpios en `whatsapp-ai-tools`; en `whatsapp-ai-respond` quedan los 8 errores de siempre en `enabledSkills`. `toWhatsappMarkup` probado con casos reales. Falta repetir la conversación por WhatsApp.

## Estado actual (2026-09-13) — Egresos imprimibles, filtros en la URL, Canales separado de IA, y el acoplamiento skills ↔ código

Ronda larga de producto. Lo importante para quien retome:

**⚠️ Acoplamiento `ai_skills.prompt_fragment` ↔ código de las herramientas (observación pedida explícitamente por el usuario).** `ai_skills` es un catálogo **global**: el `prompt_fragment` de cada habilidad lo reciben los asistentes de **todos los tenants** que la tengan activa. Ese fragmento DESCRIBE lo que las herramientas hacen de verdad (`_shared/aiTools.ts` + `whatsapp-ai-tools`). Entonces: **si cambia el comportamiento de una herramienta, hay que actualizar el fragmento en la misma ronda**. Si no, los agentes de los otros tenants siguen instrucciones que ya no corresponden y responden mal sin que nadie se entere — el modelo no tiene cómo darse cuenta solo. Caso concreto de hoy: la foto de producto ahora viaja con la ficha en el **pie de foto** (`buildProductImageCaption` en whatsapp-ai-tools: nombre, precio, referencias/variantes o SKU, descripción; tope 1024 caracteres de WhatsApp), y el fragmento de `catalogo` se actualizó para decirle al modelo que NO repita esos datos en su texto. Si mañana el caption cambia de campos, ese fragmento queda mintiendo.

**Corolario del mismo problema, sin resolver:** el fragmento de la habilidad `ventas` tiene metida una **regla de negocio de un tenant** ("Este negocio no maneja cotizaciones de cara al cliente — solo pedidos/ventas"), y eso le aplica hoy a todos. Es exactamente el pendiente del 2026-08-27 (flag por tenant para elegir venta directa / cotización / carrito+tienda). Lo específico de un negocio va en el `system_prompt` de su asistente, nunca en el fragmento global.

**Prompt del asistente de TecnoNova Colombia (el tenant de QA) reescrito** (migración `20260913220000`). El anterior estaba desactualizado: hablaba de "Tenant QA Uno" (nombre viejo), daba instrucciones para `set_lead_stage` (herramienta eliminada el 2026-08-27) y derivaba quejas a "gestión de casos" (PQR, eliminado el 2026-08-17). El nuevo es vendedor de punta a punta, no repite la ficha que ya va en el pie de la foto, y ante una consulta ambigua comparte la **tienda pública** del tenant (`https://leadly.lexycolombia.com/tienda/tecno-nova`) además de mostrar opciones. Ese enlace está **hardcodeado en el prompt de ese asistente**, que es lo correcto para algo de un solo negocio; generalizarlo (que cualquier asistente sepa si su tenant tiene tienda y cuál es su URL) exige una herramienta nueva o inyectarlo en el contexto, y eso sí sería un cambio global.

**Resto de la ronda**: consecutivo `EGR-N` + impresión de egresos (tirilla térmica y PDF con el diseño de la factura, sin QR, con bloque de firmas); soporte de pago imprimible en cartera y el número de pedido de un cargo como enlace al pedido; filtros/búsqueda/página de las listas espejados en la URL (`lib/urlFilters.ts`, en Órdenes/Productos/Clientes/Gastos); **Canales** separado de "IA & Agentes" como módulo propio del CRM con sus permisos (`channels.view`/`channels.manage`), y las plantillas de Meta movidas a Campañas.

## Estado actual (2026-09-11) — Auditoría de impuestos de Barriles (todo ya estaba correcto), catálogo de impuestos duplicado, y ajustes de UI en detalle de pedido/POS

**Reporte del usuario**: "en el pos me salen 2 líneas de impuesto, una con 8% y otra INC 8%" + pedido explícito de revisar todas las órdenes de Barriles de la sexta (las que quedaron con IVA 19% dejarlas así, el resto debía ser INC 8%) y corregir el tipo de impuesto del producto donde aparece un "impuesto al consumo genérico".

**Auditoría completa contra la base real** (productos, `sales_order_items`, `sales_invoice_items` del tenant) — **resultado: ya estaba todo correcto**, sin nada que corregir en los datos:
- **108/108 productos activos** en INC 8% (`tax_type_code='04'`, `tax_rate=8`), ninguno en IVA ni en ningún otro código.
- **`sales_order_items`**: 408 ítems / 179 pedidos en INC 8%, exactamente **1** ítem en INC 19% (el pedido #28172 / factura FE-13, ya `accepted` por la DIAN — la excepción que el usuario pidió dejar así), y los ~29.288 ítems de la migración de Fudo sin impuesto (`tax_rate=0`, correcto, ver la barrida del 2026-09-10). Cero filas en IVA (código `01`).
- **`sales_invoice_items`**: mismo patrón, 100% INC 8% salvo la misma factura FE-13 ya aceptada en INC 19%.
- Es decir: la barrida masiva del 2026-09-10 dejó los datos exactamente como debían quedar, y nada se desvió desde entonces. No se tocó ninguna fila de pedidos/facturas en esta ronda porque no había nada que tocar.

**Causa real encontrada: el catálogo `tax_types` tiene dos filas que suenan a lo mismo.** `'02'` = "Impuesto al Consumo (genérico)" y `'04'` = "Impuesto Nacional al Consumo (INC)" — las dos con `category='impuesto'`, así que las dos aparecían como opciones en el selector de impuesto de la ficha de producto (`ProductDrawer.tsx` vía `listTaxTypes()`, que solo filtra por categoría). Confirmado que **ningún producto de ningún tenant usa `'02'` hoy** (`select ... where tax_type_code='02'` → 0 filas en toda la plataforma), y que ese código nunca tuvo soporte real: no está en `VALID_TAX_RATES` de `_shared/invoicing/taxRates.ts` (que solo valida `'01'`/`'04'`) — es un resabio del catálogo original, nunca terminado. Fix: migración `20260911172433_deactivate_generic_consumption_tax_type.sql` pone `is_active=false` en el código `'02'` — como `listTaxTypes()` ya filtra `is_active=true`, alcanza para sacarlo del selector sin tocar código de frontend. Ahora el selector de impuesto de producto muestra un solo tipo de impuesto al consumo ("INC"), no dos.

**Sobre las "2 líneas" reportadas en el POS**: no se pudo reproducir contra la base real (que está limpia) ni contra el código de agrupación (`computeOrderTotals.ts` agrupa por `tax_type_code + tax_rate`, funciona como se espera). La hipótesis más probable es que el usuario vio esto en un carrito/pedido de un momento anterior a que la barrida del 2026-09-10 terminara de propagarse, o en un estado de navegador desactualizado — pendiente que el usuario confirme si el duplicado sigue apareciendo después de este cambio; si sigue, hace falta el pedido/carrito puntual para poder reproducirlo.

**Ajustes de UI de la misma sesión, sin relación con lo anterior**:
- **Desborde del detalle de pedido en pantallas ≤14"** (`OrderDetail.tsx`, `sidebar.tsx`): ítems de grid sin `min-w-0` (el card de Cliente/Envío/Oportunidad no podía encogerse por debajo del ancho de su contenido) — agregado en `StatCard`, las 3 columnas internas, `ThreadColumn` y `SidebarInset` (este último en el shell compartido de toda la app, no solo esta pantalla).
- **Historial de documentos DIAN rediseñado**: el motivo de rechazo/error pasó de texto inline (se partía palabra por palabra en la columna angosta del panel lateral) a un ícono con Popover (`RejectionReasonButton`); los botones de acción (correo, nota crédito, verificar estado, reenviar, descargar PDF) pasaron a ser solo-ícono con Tooltip real (`IconActionButton`), agrupados a la derecha de cada fila; número de documento arriba, fecha/monto debajo.
- **POS · Cuentas abiertas**: la fecha/hora de apertura de la cuenta se agregó al detalle de la cuenta (`PosTabAccount.tsx`, junto al botón "Volver"), no a las tarjetas de la grilla — se había puesto ahí primero y se sacó a pedido del usuario.



**Hito real, verificado contra el servidor de producción de la DIAN** (no habilitación): la factura `FE12` de Barriles de la sexta (NIT 902086977, resolución 18764113695661, rango FE 1-1000) quedó `accepted` con `status_detail = "Procesado Correctamente."` y CUFE real (`1787aca6da747ecc...`) el 2026-09-10 19:46 UTC, en el intento 6 de esa misma factura. Esto cierra el pendiente que el roadmap venía arrastrando ("confirmar si ya se probó/aceptó un envío real en producción, no solo habilitación") — la respuesta ahora es sí.

Los 5 rechazos previos de ese mismo día son la parte útil de documentar, porque cada uno destapó un problema distinto y ninguno era adivinable desde habilitación (el ambiente de pruebas nunca los disparó):

1. **Intento 2 — "Empresa emisora con NIT 902086977 no se encuentra autorizada para enviar documentos por los lotes"**: se estaba usando `SendBillAsync`, que es el envío POR LOTES del Anexo Técnico y exige una autorización de cuenta aparte que ningún tenant nuestro tiene ni necesita. Para una factura individual en producción el método correcto es `SendBillSync`, que además es SÍNCRONA — el veredicto viene en la misma respuesta, sin `ZipKey` ni `GetStatusZip` posterior. Habilitación no lo detecta porque ahí el camino es `SendTestSetAsync`, otro método por completo.
2. **Intento 3 — FAD07 + FAJ71**. `FAD07`: `cbc:UUID/@schemeID` (el CUFE) llevaba `"2"` hardcodeado; correcto por accidente en habilitación (donde el ambiente TAMBIÉN es 2) y roto en producción. La DIAN recalcula el CUFE con el ambiente que ese atributo declara, así que además arrastraba un FAD06. `FAJ71`: faltaba `cac:Contact/cbc:ElectronicMail` del emisor — opcional en el esquema, pero de rechazo real.
3. **Intentos 4 y 5 — FAD06 solo, "Valor del CUFE no está calculado correctamente"**, ya con todo lo anterior corregido. Este es el que costó, ver abajo.

**Causa raíz del FAD06 persistente: la clave técnica es del RANGO, no del tenant ni del software.** El tenant había cargado a mano, el 3 de septiembre, la clave técnica de su set de habilitación (rango sintético con prefijo SETP) y nunca la volvió a tocar al pasar a producción con su resolución real. La DIAN recalcula el CUFE con la clave que ella tiene registrada para ese rango, así que el hash nunca podía cerrar — y el mensaje de rechazo no puede decir "tu clave está vieja", solo que el CUFE no cuadra. El Anexo Técnico v1.9 lo dice textual en el numeral 11.6 ("está asociada al Rango de Facturación del que tomó el prefijo y el número de la factura"), con una advertencia explícita de que un rango nuevo trae una clave técnica DISTINTA a la del anterior. Y el numeral 7.15.1 aclara por qué habilitación no ayuda: `GetNumberingRange` "estará disponible únicamente en el ambiente de producción en operación", porque en habilitación es el propio catálogo de participantes el que fabrica el rango y su clave.

Fix estructural, no un campo corregido a mano: **`_shared/invoicing/resolveTechnicalKey.ts` (nuevo)**. En producción la clave deja de leerse del campo manual de Integraciones y se le pide a la DIAN con `GetNumberingRange`, eligiendo el rango que de verdad contiene el consecutivo que se va a emitir (matchea prefijo + límites). El campo manual queda solo como respaldo si el web service falla o no devuelve un rango que coincida, para que una caída puntual de la DIAN no bloquee la facturación. En habilitación no consulta nada (no existe ese servicio ahí) y el valor cargado a mano sigue siendo la única fuente posible. Caché por isolate de 10 minutos (es un round-trip SOAP con mTLS y el rango no cambia entre dos facturas seguidas) que expira sola, para no reintroducir el mismo problema de un valor congelado para siempre. El origen real de la clave (`dian` vs `stored`, y el motivo del fallback) se guarda en `sales_invoices.dian_response.technicalKey` — la factura aceptada registra `{"source":"dian"}`, que es la confirmación de que el mecanismo nuevo fue el que la hizo pasar.

**Bug de contrato en `GetNumberingRange`, encontrado al probar el botón "Sincronizar con la DIAN"**: respondía `"El código del software no corresponde al NIT: ."` — con la cadena VACÍA justo después de los dos puntos, que es el dato clave. El código mandaba solo `accountCode` (NIT del obligado a facturar) y `softwareCode`, omitiendo `accountCodeT` porque el XSD lo marca "Optional"; pero el TEXTO del anexo (numeral 7.15.2, tabla de la página 357) marca los tres como requeridos, y `accountCodeT` es el NIT del **dueño del software**, contra el que la DIAN valida el `softwareCode`. Contra un dueño vacío no hay software que corresponda nunca. En el modelo de esta plataforma (`dian_directo` = "software propio": cada tenant registra su propio software y `sts:SoftwareProvider/ProviderID` ya llevaba su propio NIT) el dueño ES el tenant, así que va el mismo NIT que `accountCode`.

**Aclaración de modelo, a raíz de una pregunta explícita del usuario** ("a un proveedor tecnológico se le da el set de pruebas de habilitación y nada más, ¿por qué debo cambiar cosas acá?"): la plataforma NO opera como Proveedor Tecnológico y el paquete de habilitación no es configuración de producción, es un andamio que se descarta. Ser PT centralizaría el set de pruebas, el Software ID y el PIN (uno solo, de Lexy, en vez de uno por tenant) y `sts:SoftwareProvider/ProviderID` pasaría a llevar el NIT de Lexy — pero **la clave técnica seguiría siendo por rango de cada cliente en los dos modelos**, nunca se centraliza. La única diferencia concreta en el código de hoy sería `accountCodeT` (pasaría a ser el NIT de Lexy mientras `accountCode` sigue siendo el del tenant); está anotado como tal en el comentario de cabecera de `getNumberingRange.ts`.

**Bug de zona horaria en los tres emisores (`sendInvoiceToDian`/`sendCreditNoteToDian`/`sendToDian`)**, corregido con `_shared/invoicing/colombiaTime.ts` (nuevo): la hora de emisión se armaba como `new Date().toISOString().slice(11, 19) + "-05:00"` — toma la hora UTC y le PEGA el offset colombiano sin convertir nada, así que el documento declaraba una hora cinco horas adelantada (una factura emitida 13:48 en Colombia salía como `18:48:49-05:00`). Peor entre las 19:00 y la medianoche hora local: el `slice(0, 10)` de la misma cadena UTC ya había caído al día siguiente, así que `cbc:IssueDate` adelantaba la fecha un día entero. Ambos valores entran al CUFE/CUDE y se imprimen en la representación gráfica. Colombia es UTC-5 fijo, así que alcanza con desplazar el instante y leerlo como UTC — sin `Intl` ni tz database.

**Limpieza puntual en notas crédito**: `sendCreditNoteToDian` leía `technical_key` y abortaba si faltaba, con un comentario que afirmaba que la usaba para firmar el sobre WS-Security. Era falso — el sobre se firma con el certificado y el CUDE de una nota crédito usa el Software-PIN, no la clave técnica (Anexo Técnico numeral 11.4). Se quitó: era un bloqueo por un dato que ese camino nunca usa.

**Servicio de notificaciones (toasts) para toda la app** (`contexts/ToastContext.tsx` + `components/ui/toast.tsx`, ambos nuevos): hasta ahora cada pantalla resolvía "salió bien" a su manera, casi siempre un `useState` con un texto verde al lado del botón Guardar que el usuario tenía que ir a buscar con la vista y que desaparecía al cerrar el formulario. Ahora hay un `useToast()` con `success`/`error`/`info`, cola con máximo 3 visibles, auto-descarte (los errores duran el doble que los éxitos — un éxito solo confirma algo que el usuario hizo a propósito, un error tiene texto que hay que leer) y `durationMs: 0` para dejar uno fijo. **Sin dependencias nuevas** (nada de sonner/react-hot-toast): mismo patrón de contexto que `AuthContext`/`LanguageContext`, estilizado con los tokens del propio sistema de diseño. El provider vive en la raíz (dentro del de idioma, fuera del de sesión) — eso es justamente lo que permite cerrar un drawer y que la confirmación siga en pantalla, porque el toast lo renderiza la raíz y no el componente que lo emitió. Accesibilidad: los errores se anuncian como `alert`/`assertive`, los éxitos como `status`/`polite`, para no interrumpir a un lector de pantalla por una confirmación.

Primer consumidor, pedido explícito del usuario: **`DianDirectoCredentialDrawer` ahora se cierra solo al guardar bien** y muestra la notificación; si falla, la notificación trae el error del backend y el drawer **no** se cierra (lo que se escribió tiene que seguir ahí para poder corregirlo), con el error también en línea como antes. Verificado en el navegador de punta a punta contra la sesión real del tenant. Pendiente explícito: los otros cuatro drawers de Integraciones (Wompi/HubSpot/Shopify/LaFactura) siguen con el texto verde viejo al lado del botón — no se tocaron a propósito, el pedido era este drawer.

**Bug pre-existente encontrado y NO corregido** (fuera del alcance de esta ronda, anotado para no perderlo): `FAZ09` sigue apareciendo como notificación blanda en cada factura ("Debe existir el grupo de información de identificación del bien o servicio") porque `cac:StandardItemIdentification` exige un código de un estándar externo acreditado (GTIN/UNSPSC/EAN) que esta plataforma no tiene. Se llena `cac:SellersItemIdentification` con el SKU real cuando existe. Inventar un scheme ahí sería peor (dispara FAZ10/FAZ11, esos sí de rechazo).

**Consulta de estado: se estaba usando la operación equivocada para producción** (`getDianStatus.ts`). El Anexo Técnico tiene DOS operaciones de consulta que no son intercambiables: `GetStatusZip` (numeral 7.12) recibe el `zipKey` de un envío asíncrono y devuelve un array (uno por documento del ZIP), mientras que `GetStatus` (numeral 7.11) recibe "el valor del CUFE o TrackId del documento consultado". El código usaba siempre `GetStatusZip`. Funcionaba en habilitación, donde `SendTestSetAsync` sí devuelve un zipKey — pero en producción `SendBillSync` no produce ninguno y lo que queda guardado en `dian_tracking_id` es el CUFE, así que **el botón "Verificar estado" no podía resolver NINGUNA factura de producción que quedara colgada en `sent`**: preguntaba por un lote que no existe. Ahora la operación se elige comparando el `dian_tracking_id` contra el `cufe`/`cude` de la propia fila (son las dos únicas formas que puede tomar ese campo), en vez de adivinar por formato — los dos son cadenas hex largas. Acción nueva `lookup_dian_document` en `dian-submit`: consulta de SOLO LECTURA que funciona sobre cualquier factura sin importar su estado y **no escribe nada** en `sales_invoices` — deliberadamente distinta de `check_invoice_status` (que exige estado `sent` y aplica el veredicto sobre la fila), porque sirve para responder "¿la DIAN de verdad tiene este documento?" y ahí una discrepancia entre lo guardado y lo que dice la DIAN hay que poder verla antes de que algo la sobrescriba. Acepta `invoice_id` o un `track_id` suelto. Con ella se confirmó en vivo que la DIAN responde `IsValid=true`, `StatusCode=00`, "La Factura electrónica FE-12, ha sido autorizada" — el documento existe y está autorizado del lado de la DIAN, así que "no la veo en el portal" no era un problema de emisión. **Nota para quien busque un documento en el portal**: el buscador público de `catalogo-vpfe.dian.gov.co` exige captcha, así que no se puede automatizar; y el catálogo de habilitación es un sitio distinto (`-hab` en el dominio) que se ve igual.

**Nota crédito: 5 problemas reales en el mismo rechazo** (`buildCreditNoteXml.ts`/`sendCreditNoteToDian.ts`), la primera vez que se mandó una a producción. Ninguno se había visto antes porque toda la validación previa se había hecho sobre facturas, que son otro esquema:
1. **ZB01 (rechazo, error de esquema)**: `cac:DiscrepancyResponse` y `cac:BillingReference` se emitían DESPUÉS de las partes, cuando la secuencia de `UBL-CreditNote-2.1.xsd` los exige antes (el validador se quejaba de encontrar `DiscrepancyResponse` donde esperaba `TaxTotal`/`LegalMonetaryTotal`). Orden correcto, confirmado elemento por elemento contra `dian-reference/ejemplos-xml/CreditNote.xml`: `LineCountNumeric` → `DiscrepancyResponse` → `BillingReference` → `AccountingSupplierParty` → `AccountingCustomerParty` → `PaymentMeans` → `TaxTotal` → `LegalMonetaryTotal` → `CreditNoteLine`.
2. **CAN01 (rechazo)**: faltaba `cac:PaymentMeans` por completo — grupo obligatorio (1..N) en una nota crédito, por contraintuitivo que suene en un documento que no cobra nada. Se arma con el pago REAL de la venta que la nota corrige, mismo catálogo UNCL4461 que la factura. **Decisión explícita**: aunque la venta original haya sido a crédito se informa `cbc:ID` = "1" (contado), porque CAN04 exige `PaymentDueDate` cuando es "2" y esta plataforma no guarda esa fecha por pago — mandar "2" sin ella sería rechazo seguro, y la nota crédito no es el documento que fija el plazo de pago de nada.
3. **CBF04 (notificación)**: `cbc:Description` llevaba el texto libre que el usuario escribe en el drawer, que por definición nunca va a estar en la lista de conceptos. Ahora lleva la descripción canónica del código elegido (constante `REASON_DESCRIPTION`, las mismas cadenas que ya muestra el selector del frontend, `einvoicing.creditNote.reason.*`) y el texto del usuario sigue yendo en la descripción de la línea, que es texto libre legítimo. ⚠️ La tabla oficial (13.2.4) **no está en el PDF del Anexo** — vive en un xlsx aparte de la "Caja de Herramientas" de la DIAN, así que los textos de los códigos 5 ("Descuento") y 6 ("Otros") están sin verificar contra la fuente y son los primeros sospechosos si CBF04 persiste.
4. **CBF02 (notificación)**: faltaba `cac:DiscrepancyResponse/cbc:ReferenceID` (número de la factura corregida) — estaba solo dentro de `BillingReference`, la DIAN lo quiere en los dos lugares.
5. **CAK55 (notificación)**: falta el correo del COMPRADOR. En la factura el correo obligatorio es el del emisor (FAJ71) y `partyBlock` solo lo emitía para ese lado; en la nota crédito la tabla de reglas también lo pide para el adquiriente. El dato ya venía en `buyer_snapshot.email` desde `queueInvoiceGeneration` y no se estaba usando.
Verificado renderizando una nota crédito de prueba y comparando el orden resultante contra el ejemplo oficial, sin gastar ningún envío real. **Sin probar todavía contra la DIAN** — pendiente que el usuario reintente la emisión.

**Nota crédito ACEPTADA en producción** el mismo día, después de las 5 correcciones de arriba: `NC1` de Barriles de la sexta quedó `accepted` con "Procesado Correctamente." y CUDE real. Con esto los dos documentos electrónicos (factura y nota crédito) están confirmados de punta a punta contra el servidor real de producción de la DIAN.

**Entrega del documento al adquiriente por correo + conservación del XML** (migración `20260910230000_sales_document_delivery.sql`, Edge Function nueva `send-document-email`, módulos nuevos `_shared/email/resend.ts`, `_shared/invoicing/storeSignedXml.ts`, `sendDocumentEmail.ts`, `documentEmailTemplate.ts`, `renderInvoicePdf.ts`). El Anexo Técnico numeral 9.3 lista el correo a la dirección suministrada por el adquiriente como uno de los medios AUTORIZADOS de entrega -- no era una comodidad pendiente, era un requisito sin cubrir.

- **El XML firmado se descartaba.** `sales_invoices.xml_storage_path` existía desde el 2026-09-03 y hasta figuraba en los tipos del frontend, pero **ningún código lo escribía nunca**: el documento electrónico de generación se construía, se firmaba, se transmitía y se perdía. `sales_credit_notes` ni siquiera tenía la columna. Ahora `storeSignedXml.ts` lo guarda en un bucket privado nuevo (`sales-documents`) **ANTES de transmitir**, no después de que la DIAN acepte: así quedan los bytes exactos que se iban a transmitir aunque el envío reviente, cada `attempt_number` tiene su propio archivo sin pisar al anterior, y el guardado no puede fallar "después de que la factura ya existe". Nunca bloquea la emisión -- la existencia legal del documento la establece la aceptación de la DIAN, no nuestro bucket; si Storage falla, `xml_storage_path` queda null, que es un hueco visible y consultable. El bucket es privado (mismo criterio que `tenant-certificates`) y **sin policy de escritura a propósito**: solo sube el pipeline con service role, ningún usuario debe poder inyectar a mano un XML "firmado" en el expediente fiscal de su tenant.
- **`sales_document_emails`** -- bitácora append-only (solo select/insert, mismo criterio que `stock_movements`): destinatario, asunto, adjuntos REALES, estado, id del mensaje en el proveedor, motivo del error, `triggered_by` (`auto`/`manual`) y quién lo pidió. "Le entregamos este documento a este correo tal día" es evidencia de cumplimiento del numeral 9.3, no un registro editable; un reenvío es una fila nueva.
- **Envío automático** al aceptar la DIAN, colgado de `applyDianVerdict.ts` (rama `accepted`). Mezclar un efecto saliente con "escribir el veredicto" es discutible, pero hay CUATRO caminos por los que un documento puede terminar aceptado (envío de factura, envío de nota crédito, y los dos "Verificar estado" manuales) y todos convergen exactamente ahí -- colgarlo de cada caller era reintroducir el mismo problema que ya obligó a centralizar la confirmación de venta en triggers de DB el 2026-09-03. **Botón "Enviar por correo"** (tenant_admin) en la factura y en cada nota crédito del detalle del pedido, para el caso real de "no me llegó" o "le corregí el correo al cliente después de emitir".
- **Sin correo del cliente NO es un error**: se registra el motivo y se devuelve un aviso accionable. El propio numeral 9.3 lo contempla ("si el adquirente no informa o señala el medio de recepción... se deberá expedir una Impresión de representación gráfica") -- que es exactamente el tiquete que ya imprime el POS. Cubre las ventas de mostrador a Consumidor Final, que son la mayoría.
- **⚠️ Conflicto de dependencias real, encontrado probando el botón en vivo**: la primera versión puso la entrega como una acción más de `dian-submit` y falló con `process.nextTick is not a function`. Esa función importa `nodeCompatShim.ts` como su primerísima línea (reemplaza `process` por un objeto mínimo, sin el cual pkijs/node-forge revientan al importarse) y **pdf-lib llama a `process.nextTick`, que ese objeto mínimo no tiene**. Se podía parchear el shim, pero se optó por separar: la entrega vive en su propia Edge Function liviana (`send-document-email`), que nunca importa la pila de firma. Además de resolver el conflicto, mantiene pdf-lib fuera de la función que firma y transmite documentos fiscales -- que ya tuvo un "CPU Time exceeded" real por acumular trabajo pesado en una sola invocación. `applyDianVerdict` la invoca por HTTP con el service role key **esperando la respuesta** (una Edge Function mata cualquier fetch no esperado al retornar, pitfall ya documentado acá).
- **`renderInvoicePdf.ts`**: el armado del PDF se extrajo de `sales-invoice-pdf/index.ts` sin cambiar ningún criterio. Motivo: el correo necesita EXACTAMENTE el mismo documento, y duplicar el armado -- que entre otras cosas decide si el documento es factura o remisión -- garantizaba que las dos copias se desincronizaran a la primera corrección. El endpoint quedó con solo la autorización.
- **Correo**: Resend, mismo proveedor y mismo dominio ya verificado que usa Lexy legal (`noreply@lexycolombia.com`), sin SDK (la API es un POST con JSON, y evitar la dependencia importa en funciones que ya cargan pdf-lib). El remitente muestra el nombre del comercio sobre el dominio de la plataforma y el reply-to es el correo real del comercio: el comprador reconoce a quién le compró y responder le llega a quien vendió. Plantilla en `documentEmailTemplate.ts`, aprobada por el usuario sobre una vista previa renderizada, con tablas y estilos en línea (Outlook ignora hojas de estilo). Solo declara "Documento validado por la DIAN" con la prueba concreta (estado validado + CUFE/CUDE real), mismo criterio que `isRemision` en el PDF: nunca aparentar un comprobante que no existe.
- Verificado en vivo contra la factura FE-12 real: destinatario resuelto, asunto armado, PDF generado y adjuntado -- **el flujo entero corre y se detiene solo en el envío**, porque falta cargar el secreto `RESEND_API_KEY` (no se pudo extraer del proyecto de Lexy: vive como secreto de Supabase, que no se puede leer de vuelta). **Pendiente del usuario**: `supabase secrets set RESEND_API_KEY=...`.
- **Límites conocidos, no ocultos**: (1) el XML solo se adjunta en documentos emitidos DESPUÉS de este cambio -- los ya emitidos se transmitieron cuando no había dónde guardarlos, y a esos les va el PDF solo; (2) NO se construye el `AttachedDocument` (numerales 6.4/8.5, el contenedor XML que envuelve el documento junto con el ApplicationResponse de validación) que exige la SEGUNDA modalidad de entrega del 9.3 -- la primera, representación gráfica por correo, se cumple igual por sí sola; (3) un rebote posterior del correo no se entera (haría falta el webhook de Resend).

## Estado actual (2026-09-11) — Aurora reposicionada para vender la plataforma Lexy, y aviso de lead nuevo por WhatsApp

**Diagnóstico sobre conversaciones reales, no sobre intuición.** De las 21 conversaciones del tenant Lexy Colombia, **13 mueren en el segundo mensaje**, todas en el mismo punto: el cliente escribe "quiero información" o "¿cuánto cuesta la plataforma Lexy?" (viene de un anuncio click-to-WhatsApp), y Aurora contesta con el menú de cuatro nombres de producto y un "¿cuál te interesa?". Nadie vuelve a escribir. El caso que cierra el argumento es de ese mismo día: un cliente preguntó el precio, recibió el menú y se despidió con **"Ahhh... lo siento... pensé que era un Control de ventas e inventarios"** -- era exactamente el cliente de esta plataforma (POS, inventario, ventas) y se fue creyendo que se había equivocado de empresa, porque el menú describía el producto como "CRM de WhatsApp con IA".

**Decisiones de producto del usuario en esta ronda**: la plataforma se llama **Lexy** de cara al cliente (el nombre "Leadly" desaparece del prompt; sigue siendo el nombre del repo y del esquema); y **no tiene lista de precios pública** -- el precio depende de la operación y se define con un asesor, así que Aurora nunca da cifras y lleva a reunión.

**Qué se cambió en el `system_prompt`** (23.955 → ~32.000 caracteres, editado por secciones sobre la fila de `ai_assistants`, no reescrito de cero: las secciones 2, 3 y 24 -- las reglas de no inventar, que son la mejor parte del prompt -- y toda la base de Lexy Legal quedaron intactas):
- **1.1 Cómo abrir una conversación**: reemplaza el menú de productos por presentar la plataforma y preguntar a qué se dedica el negocio y qué proceso quiere dejar de hacer a mano. Una sola pregunta, fácil de contestar.
- **1.2 A qué solución enrutar** (nueva): mapa de sector/palabra clave → producto, para que el cliente no tenga que conocer la nomenclatura interna. **Lexy es el default explícito** ante la duda; antes el default de facto era Lexy Legal, solo porque es el producto del que hay más información cargada.
- **1.3 Si la conversación se estanca** (nueva): después de dos respuestas cortas o vagas, dejar de explicar y hacer UNA pregunta concreta de tres palabras de respuesta ("¿hoy en qué llevas las ventas: cuaderno, Excel, o algún programa?"), para identificar qué ofrecerle.
- **20.1 Fuente de verdad de Lexy** (nueva, el bloque más grande): la ficha real de la plataforma al mismo nivel de detalle que las secciones 4-19 de Lexy Legal -- POS, inventario multi-bodega con kardex, facturación electrónica DIAN, cartera, despachos, devoluciones, gastos, tienda en línea, WhatsApp con IA, equipo e informes. Antes eran tres viñetas congeladas en el MVP de agosto. Incluye qué NO afirmar (integraciones, migración automática, app móvil, tiempos, prueba gratis) y cómo responder la pregunta de precio sin quedarse en "depende".
- **32. Estilo**: trato de **tú** (Colombia, coherente con el cambio de voseo→tuteo de la app), y **formato de WhatsApp**: negrita con *un* asterisco. Bug real y visible en las conversaciones -- Aurora venía mandando `**Lexy Legal**` y al cliente le llegaban los asteriscos dobles literales.

**Bug de fondo corregido: medio motor comercial le hablaba a herramientas que no existen.** Las secciones 26 y 27 ordenaban `create_note` y `set_lead_stage` en 6 de 8 reglas -- las dos se eliminaron el 2026-08-27 (ver esa entrada: la habilidad `leads` se fusionó con oportunidades y las tools de PQR se borraron). Con las habilidades que este asistente tiene activas (`calendario` + `oportunidades`) las herramientas reales son exactamente 8. De ahí venía el síntoma clásico de narrar en vez de ejecutar, visible en una conversación real: *"te informo que ya registré tu interés en el Plan Profesional y lo pasé a la etapa de negociación"* -- que además viola la sección 25 del propio prompt. La 26 se reescribió contra las 8 tools reales, con la tabla de señal → acción, y la 27 (etapas del lead) se eliminó entera por ser letra muerta.

**Formato obligatorio de la descripción de la oportunidad** (`Sector:` / `Necesita:` / `Solución:`): no es cosmético, es el contrato de datos del aviso de abajo. El prompt lo explica con ese motivo, para que el modelo entienda por qué importa llenarlo.

**Aviso de lead nuevo por WhatsApp** (pedido explícito: "con eso no quedo ciego ni tengo que estar revisando la plataforma todo el tiempo"). Piezas nuevas:
- Plantilla **`lexy_solicitud_contacto`** (UTILITY, es, 4 variables: cliente, teléfono, sector, qué requiere), creada en Meta y registrada en `whatsapp_message_templates`. **En PENDING al cierre de esta ronda.**
- ⚠️ **La primera versión se llamaba `lexy_lead_interesado` y hubo que borrarla y recrearla.** No fue por falta de ejemplos de las variables (se mandaron correctamente desde el primer POST, como `example.body_text` con el array anidado que exige Meta, y la consulta a la Graph API confirmó que quedaron registrados con `rejected_reason: "NONE"`): **Meta la RECLASIFICÓ sola de UTILITY a MARKETING**. La causa es la redacción -- "Nuevo interesado en *Lexy*" más un "Entra a la plataforma" se lee como promoción de marca con llamada a la acción, no como notificación de cuenta. Una plantilla MARKETING pasa por una revisión más lenta, cuesta más por mensaje y queda sujeta a que el destinatario no haya desactivado marketing.
- Dos cosas que Meta NO deja hacer, y que definen el procedimiento si esto vuelve a pasar: (1) **no se puede cambiar la categoría de una plantilla mientras está en revisión** -- hay que borrarla y crear otra; (2) **el nombre de una plantilla borrada queda reservado por semanas**, así que la nueva no puede reusarlo (el intento devuelve el mismo `error_subcode 2388025` con el texto "No puedes cambiar la categoría... mientras se está eliminando el contenido actual", que persiste aunque se espere). De ahí el nombre distinto.
- Redacción que sí quedó en UTILITY: sin nombre de marca, sin llamada a la acción promocional, en lenguaje de notificación de cuenta ("Se registró una nueva solicitud de contacto en tu cuenta... Puedes responderle desde tu panel de atención").
- `tenants.sales_notification_phone` (nullable, opt-in) y `opportunities.lead_alert_sent_at` (un aviso por oportunidad, nunca repetido) + índice parcial para el barrido.
- Edge Function **`notify-sales-lead`** + cron `send-sales-lead-alerts` cada 5 minutos, mismo patrón exacto que `send-task-reminders` (secreto `cron_reminder_secret` desde Vault, desplegada con `--no-verify-jwt` -- sin ese flag el gateway devuelve 401 antes de llegar a la función, pitfall ya conocido acá y que volvió a aparecer en esta ronda).
- **Por qué cron y no un trigger inmediato**: Aurora puede crear la oportunidad con "Sector: por confirmar" y completarla uno o dos turnos después, cuando el cliente contesta. Avisar en el instante del insert mandaría una alerta a medio llenar. El colchón de 3 minutos deja que la descripción termine de armarse.
- **Degrada sin perder el aviso**: si falta el número, la línea activa, el token o la aprobación de la plantilla, la oportunidad **no se marca** -- el aviso sale solo en cuanto se configure lo que falta, en vez de perderse. Y si la plantilla local está en PENDING, la función le vuelve a preguntar a Meta y actualiza el estado, así empieza a funcionar sola el día que la aprueben sin que nadie sincronice nada a mano.
- **Backfill obligatorio**: todas las oportunidades existentes quedaron con `lead_alert_sent_at = now()`. Sin eso, el primer barrido habría mandado un aviso por cada oportunidad histórica del tenant.
- Verificado de punta a punta hasta donde la aprobación de Meta lo permite: con una oportunidad de prueba real, la función responde `{"scanned":1,"sent":0,"skipped":1}` -- la detecta, la saltea por la plantilla en PENDING y no la marca, exactamente como está diseñado.
- **Pendiente**: que Meta apruebe la plantilla (queda una oportunidad de prueba viva a propósito, para que el primer aviso real llegue solo cuando eso pase), y que `sales_notification_phone` tenga UI en Configuración -- hoy se setea por SQL. Verificado después del cambio de plantilla: la función sigue respondiendo `{"scanned":1,"sent":0,"skipped":1}`, o sea que resuelve la plantilla nueva por su nombre nuevo y la saltea por estar en PENDING, sin marcar la oportunidad.

## Estado actual (2026-09-10, ronda posterior) — FAU04/FAX14: el XML no cuadraba con líneas sin impuesto, y 3 productos de Barriles con una tarifa que no existe

Rechazo real en producción del pedido #28197 de Barriles de la sexta (factura FE-16): `FAU04` (rechazo) + `FAX14`/`FAZ09`/`RUT01` (notificaciones). Dos causas independientes, una de código y una de datos — el descuento del pedido no tenía nada que ver (los descuentos ya vienen restados dentro de `sales_order_items.subtotal`, así que `LineExtensionAmount` sale neto y ninguna regla de totales los ve).

**FAU04 — bug de código, tercera causa distinta para la misma regla.** Una línea con `tax_type_code` nulo no emitía NINGÚN `cac:TaxTotal` de línea, pero su monto sí entraba a `LineExtensionAmount`/`TaxExclusiveAmount` de cabecera — que son los dos lados de la igualdad que compara esa regla ("Base Imponible es distinto a la suma de los valores de las bases imponibles de todas líneas de detalle"). Con el pedido real: bases de línea 47.222,22 contra un `TaxExclusiveAmount` de 64.222,22, diferencia exacta de los 17.000 de la línea sin impuesto. El arreglo NO es sacar esa línea del total: la DIAN quiere que **toda** línea declare su grupo de impuesto, y una sin impuesto se informa como **IVA al 0,00% con su base completa** — literalmente lo que hace el ejemplo oficial `dian-reference/ejemplos-xml/Exento de IVA.xml`. Con eso la igualdad se cumple por construcción y deja de depender de que ningún producto quede sin impuesto cargado. En la misma ronda se quitó el filtro `taxAmount === 0` de la agregación de cabecera (reintroducía la misma asimetría por otro camino: una línea al 0% emitía su `TaxSubtotal` pero no sumaba en cabecera — no contradice FAS01b, que prohíbe un `TaxTotal` para un código que NINGUNA línea usa, no uno al 0% que una línea real sí declara) y `TaxExclusiveAmount` pasó a calcularse como la suma real de bases en vez de reutilizar el subtotal. `TaxInclusiveAmount` se dejó como estaba, derivado de `LineExtensionAmount + impuestos` — confirmado contra `Generica.xml`, donde `TaxExclusiveAmount` vale un valor distinto de los dos. Mismo arreglo aplicado a `buildCreditNoteXml.ts`/`sendCreditNoteToDian.ts`, que tenían el problema simétrico (una nota crédito sobre una venta sin impuesto no emitía ningún grupo y su base de cabecera no tenía contra qué cuadrar).

**FAX14 — dato inválido, no bug de XML.** La plataforma deja elegir tipo de impuesto y tarifa como dos campos independientes en la ficha del producto, así que se podía guardar "IVA 8%" (tarifa que existe para INC, no para IVA) o "INC 19%" (al revés). Eso pasa por todo el flujo de venta sin ruido y recién aparece contra el servidor de la DIAN. **`_shared/invoicing/taxRates.ts` (nuevo)** replica la tabla 13.3.9 desde las listas oficiales que ya estaban en el repo (`TarifaImpuestoIVA-2.1.gc`: 0/5/16/19; `TarifaImpuestoINC-2.1.gc`: 2/4/8/16) y valida la combinación **antes de firmar y transmitir**, en los dos emisores, con un mensaje que nombra el producto a corregir — más barato que quemar un envío real. ICA no se valida: su tarifa es municipal y por mil, no hay lista contra la cual comparar.

**Datos corregidos en producción** (tenant Barriles de la sexta): 3 productos que quedaban fuera de INC 8% — "Mini paisa - Ejecutivo" (estaba IVA 8%), "Canastas de atun - ejecutivo" (sin tipo de impuesto) y "costilla bbq especial" (INC 19%) — y los snapshots de impuesto de 43 pedidos pendientes de facturar (65 ítems), que congelaban la configuración vieja del producto y por eso no se arreglaban solos al corregir la ficha. Los ítems al 8% solo necesitaron el código (`01`/nulo → `04`, montos ya correctos para esa tarifa); el único ítem al 19% se recalculó (base y monto sobre el mismo precio, que incluye el impuesto, así que el total cobrado no cambia) junto con el `tax_total` de su pedido.

**Bug de entrega: el correo de una NOTA CRÉDITO adjuntaba el PDF de la FACTURA** (reportado por el usuario: "al generar nota crédito se envía por email es la factura"). `sendDocumentEmail.ts` resolvía el adjunto con `renderInvoicePdfForOrder` para los DOS tipos de documento, llegando al pedido de la nota a través de su factura -- el asunto y el cuerpo del correo sí decían "Nota crédito" (la plantilla siempre distinguió por `kind`), pero el adjunto era la representación gráfica de otro documento. Confirmado en la bitácora real: la fila de `sales_document_emails` del envío de NC-1 tiene asunto "Nota crédito NC-1 · Barriles de la sexta" y `attachments = ["FE-12.pdf"]`.
- Arreglo: **`renderCreditNotePdf.ts` (nuevo)**, extraído de `sales-credit-note-pdf/index.ts` por el mismo argumento que ya obligó a extraer `renderInvoicePdf.ts` -- el correo necesita EXACTAMENTE el mismo documento que el botón de descarga, y dos copias del armado se desincronizan a la primera corrección. El endpoint quedó solo con la autorización (select del id con el cliente del caller, que es donde aplica la RLS; el armado usa el admin client, que necesita leer `tenants`/`integration_credentials`).
- Si el PDF de la nota no se puede armar, **no se tumba la entrega**: se loguea y se manda el XML firmado solo, que ya cumple el numeral 9.3 por sí mismo (y si tampoco hay XML, el chequeo que ya existía corta antes de mandar un correo vacío).
- **Sin verificar en el navegador**: la sesión del portal estaba cerrada en este entorno y no hay credenciales acá. Verificado por tipos (`deno check` limpio) y por ser una extracción literal del armado que ya venía funcionando en el endpoint. Los correos ya entregados con el PDF equivocado NO se reenvían solos -- hay que usar el botón "Enviar por correo" de la nota (queda una fila nueva en la bitácora, que es append-only).

**Barrida completa del tenant, a pedido explícito ("revisa todos los pedidos, a excepción de ese todos deben tener impuesto del 8% ... corresponde al inc")**: además de los 43 pedidos pendientes de la primera pasada, se normalizaron a INC 8% **todos** los ítems del tenant. El grueso eran 2.543 ítems en 1.235 pedidos migrados de Fudo que venían sin tipo de impuesto Y con tarifa 0 (y, de yapa, con `taxable_base` en 0 en vez de igual al subtotal -- otro desprolijo de esa migración). Se recalculó base y monto sobre el precio, que YA incluye el impuesto, así que **ningún total cobrado cambió**: solo cambia cómo se discrimina el impuesto adentro del mismo precio. Se recalcularon los `tax_total` de cabecera y se alinearon los snapshots de las facturas todavía no transmitidas (`sales_invoice_items` + totales de `sales_invoices`), que si no mostraban el impuesto viejo en la vista previa y el PDF hasta que `refreshInvoiceSnapshot` los pisara al enviar. Quedaron 107 productos, 2.943 ítems de pedido y todas las facturas pendientes en INC 8%, con `base + impuesto = valor de la línea` en el 100% de los ítems.

**Criterio de exclusión, más amplio que el pedido**: se excluyó todo pedido con un documento electrónico ya transmitido (`sent`/`accepted`), no solo el #28172 que nombró el usuario -- editar el snapshot de una factura ya emitida dejaría la base de datos diciendo algo distinto de lo que la DIAN tiene registrado. En la práctica coincidió: de los 5 pedidos con factura aceptada, 4 ya estaban en INC 8% y el único que no era justamente el #28172.

**⚠️ La validación de FAX14 NO se aplica a notas crédito, a propósito** (se había puesto en la primera pasada y se sacó al encontrar el caso real): una nota crédito tiene que declarar el MISMO impuesto que el documento que corrige, aunque esa combinación sea inválida. Hay dos intentos fallidos de nota crédito contra la factura FE-13, ambos con INC 19% heredado de ella -- que es lo correcto. Bloquearlos habría dejado al tenant sin forma de arreglar una factura ya emitida, peor que la notificación que la validación evita.

**Pedidos migrados de Fudo: línea única "Servicio de restaurante", SIN impuesto** (decisión explícita del usuario: "en ese entonces no debíamos cobrar impuestos" -- el tenant empezó a cobrar INC recién con el POS propio, así que ponerle INC 8% retroactivo a la historia de Fudo, como había hecho la barrida anterior, era declarar un impuesto que nunca se cobró). Alcance: los 28.017 pedidos con `fudo_migration_id`, ninguno de los cuales tiene documento electrónico emitido -- verificado antes de escribir, igual que la ausencia de triggers sobre `sales_order_items` (borrar/insertar ahí no dispara ningún efecto de stock).
- **26.754 pedidos venían SIN ninguna línea de detalle** -- la migración importó solo la cabecera. Eso no es un problema de impuestos sino de facturabilidad: `sendInvoiceToDian` aborta con "La factura no tiene ítems". Se les creó una línea única `product_name = 'Servicio de restaurante'`, `product_id = null` (a propósito: NO se creó un producto en el catálogo, que aparecería en el POS y la tienda pública sin que nadie lo pidiera -- `product_name` existe justamente para ser el snapshot histórico), cantidad 1, por el total del pedido. Cuadra exacto con la cabecera sin tocarla: esos 26.754 tenían envío 0, descuento 0 y `subtotal = total`. Incluye los 1.036 con total $0 (decisión explícita: uniformidad, y un pedido en $0 no se factura igual).
- **1.263 pedidos SÍ tenían detalle real** (nombres de platos). Decisión explícita del usuario: **conservarlos**, solo quitarles el impuesto. Se prefirió no perder el histórico aunque queden dos formatos distintos de pedido de Fudo.
- "Sin impuesto" en la base es `tax_type_code` nulo, tarifa 0, monto 0 y `taxable_base = subtotal`. En el XML eso sale como IVA 0,00% con la base completa gracias al fallback de la ronda anterior -- verificado renderizando uno: una línea, `LineExtension` = `TaxExclusive` = `TaxInclusive` = `Payable` = el total, idéntico en forma al ejemplo oficial "Exento de IVA.xml".
- ⚠️ **Si alguien EDITA un pedido histórico de Fudo desde el portal, `calculate-order` le va a recalcular el impuesto con la configuración de HOY** (INC 8%) -- estos pedidos quedan correctos mientras nadie los toque. No se construyó ningún congelamiento por fecha; no hacía falta para el pedido concreto y sería una regla de negocio nueva.

**⚠️ Problema PREEXISTENTE encontrado de paso, no tocado**: 161 pedidos (160 de la migración de Fudo, todos confirmados) tienen la cabecera desalineada de sus propias líneas -- `total` no es igual a la suma de los ítems más el envío, con diferencias de hasta $38.000 (ej. el pedido 21962: cabecera $6.000, líneas $44.000). Son exactamente 160 de los 1.263 de Fudo que conservaron su detalle, así que la línea única de "Servicio de restaurante" no los alcanzó. No lo causó esta ronda (no se tocó ningún `total` ni `subtotal` de línea); es la migración, que importó cabecera y detalle de fuentes que no cuadran. Importa para facturación porque el XML se arma con las LÍNEAS: si alguno de esos se factura, el `PayableAmount` no va a ser lo que muestra el pedido en pantalla. Arreglarlo exige decidir cuál de los dos lados es la verdad -- decisión del usuario, no asumida acá.

**⚠️ Hallazgo que NO se tocó, para decisión del usuario**: el pedido **#28172 ya tiene factura FE-13 ACEPTADA por la DIAN declarando INC al 19%**. `FAX14` es notificación, no rechazo, así que pasó igual. Es un documento fiscal ya emitido: corregirlo es una nota crédito, no un `update` — editar su snapshot dejaría la base de datos diciendo algo distinto de lo que la DIAN tiene registrado.

**Sin verificar contra la DIAN todavía**: el XML corregido se renderizó localmente con los datos exactos del pedido #28197 y cuadra (`LineExtension` 64.222,22 = 47.222,22 + 17.000; `TaxExclusive` = suma de bases de línea; dos `TaxTotal` de cabecera, INC 8% e IVA 0%, cada uno respaldado por su línea), pero el reenvío real lo tiene que disparar el usuario — consume un intento contra un documento fiscal. `FAZ09` sigue como notificación abierta por lo ya documentado (falta un código de estándar externo acreditado; estos productos además no tienen SKU, así que tampoco sale `SellersItemIdentification`) y `RUT01` es informativa de la propia DIAN.

## Estado actual (2026-09-06) — Módulo de Gastos, rediseño de Catálogo/Calendario, agendamiento real de citas, y el fix correcto de validación de stock en POS

**Nuevo módulo "Gastos"** (migraciones `20260906035718_expenses_and_categories.sql`, `035734_expense_inventory_entries.sql`, `035831_..._hardening.sql`, `190000_expenses_summary_aggregate.sql`, dentro del commit `6a2dc04`): `expense_categories` + `expenses` (tenant-scoped, soft-delete, RLS estándar de 4 policies, mismo patrón que `product_categories`/`suppliers`). El motivador real no es solo la feature en sí sino una migración de datos concreta: el tenant "Barriles de la sexta" trae 6072 gastos históricos de Fudo (su POS anterior) — de ahí `external_source`/`external_id` en `expenses` (permite un `on conflict` idempotente si el script de import se corre más de una vez) y que `expense_categories` se puebla importando tal cual el nombre que trae Fudo, sin una lista fija definida de antemano. `payment_method` queda como texto libre a propósito, no como catálogo administrable — no fue pedido y Fudo solo trae un nombre por gasto. Módulo `expenses` agregado al catálogo de módulos togglables, deshabilitado por defecto — el superadmin lo prende manualmente cuando esté listo para probarse en ese tenant.

Segunda pieza del módulo: **"dar entrada de inventario" desde una línea de un gasto** (`add_expense_inventory_entry`, producto + bodega + cantidad + costo → genera un movimiento de kardex real vía `stock_movements` con `reference_type='compra'` y sobrescribe `purchase_price` del producto/variante). Regla de negocio explícita del usuario: la suma de las líneas de entrada de inventario de un gasto nunca puede superar `expenses.amount` — se valida con un `for update` que bloquea el header del gasto durante la transacción, para que dos altas simultáneas sobre el mismo gasto no lean el mismo total parcial y pasen ambas el tope. `expense_inventory_entries` es append-only a propósito (solo policies de select/insert, mismo criterio que `stock_movements`): un error se corrige con un ajuste manual de stock aparte, no editando la línea. Decisión explícita del usuario: `purchase_price` se **sobrescribe** con el costo de esta línea, sin ponderar contra compras anteriores; `wholesale_price`/`retail_price` nunca se tocan. Nueva Edge Function `list-expenses` replica el mismo problema y solución que `list-sales-orders` del 09-05: paginación real con `.range()` + `get_expenses_summary` (RPC agregada en Postgres) para no depender de traer más de 1000 filas a ningún lado — el viejo `listExpenses` paginaba en el navegador y con 6072 filas el resto quedaba invisible sin ningún error.

**Bug real de producción en el flujo de cobro del POS, corregido en dos rondas porque la primera interpretación fue al revés de lo que hacía falta**: cargar un producto sin stock al carrito no avisaba nada, y al "Cobrar" la venta quedaba en `cotizacion` (el trigger de confirmación la bloqueaba por falta de stock) con el carrito intacto en pantalla — sin protección contra reintentos, cada click repetido en "Cobrar" insertaba otra cotización huérfana más.
- **Ronda 1 (parcial)**: se interpretó "no debería validar stock aquí" como "sacar el chequeo de stock de la confirmación de venta para POS" — hecho así (`guard_sales_order_confirmation` salta el chequeo de stock cuando `sales_channel='pos'`, migración `20260906120000_pos_orders_skip_stock_check_and_idempotency.sql`, mismo criterio que ya usaba para saltar direcciones) y se agregó idempotencia real: `sales_orders.checkout_token` (uuid único por tenant) que el frontend genera una vez por intento de cobro y reenvía en cada reintento — si ya existe un pedido con ese token, `pos-checkout`/`create-order` retoman esa fila (confirman/cobran lo que falte) en vez de crear una nueva. Esto sigue vigente y es correcto; el stock se sigue descontando igual al confirmar. ⚠️ **Corrección del 2026-09-21:** esta nota decía que `product_stock` "puede quedar negativo, intencionalmente", y es falso -- `apply_stock_movement` usa `greatest(0, …)` (verificado en la función viva y en la migración `20260825143849`): el kardex (`stock_movements`) registra la salida completa pero `product_stock` se queda en 0, así que vender de más deja el inventario desalineado del kardex sin ninguna señal. Hoy no hay filas negativas.
- **El bug grave que destapó el error**: con el chequeo sacado de la confirmación y sin agregar ningún chequeo en su lugar, una orden se guardó sin que nadie validara el inventario en ningún punto — el hueco quedó completamente abierto. El usuario aclaró el diseño real: **la validación debe pasar al CARGAR el producto** (mientras se arma la cuenta, nada prometido todavía — ahí es donde se puede resaltar y forzar una corrección), **no al confirmar** (una vez cobrado se asume que el cliente ya tiene el producto en sus manos). Regla explícita adicional: "no debo hacer validaciones en front de nada en estos componentes de pos ni en orden" — el frontend nunca decide esto por su cuenta, siempre lo dice el backend.
- **Ronda 2 (diseño correcto)**: `_shared/orders/stockAvailability.ts` (nuevo, compartido) expone `findStockShortfalls()` — por cada línea de un producto `track_inventory=true`, suma el stock real (por la bodega puntual de la línea si la tiene, o todas las bodegas del tenant si no, para cubrir tanto `OrderItemsEditor` como la venta rápida sin selector de bodega) y devuelve las que piden más de lo disponible. `calculate-order` (el único endpoint que guarda un carrito/borrador, usado por POS y por Órdenes del portal) corre este chequeo en cada guardado y en `preview`, y devuelve la lista como `stock_shortfalls` **sin rechazar el guardado** — el borrador se guarda tal cual, con la línea problemática incluida, para que el frontend la pinte en rojo (`OrderItemsEditor` ya tenía el prop `shortfalls`) y deshabilite "Cobrar"/"Crear pedido" mientras exista alguna, sin ocultarla ni revertirla sola. `create-order` y `pos-checkout` (los que crean la venta real) sí validan **siempre, antes de insertar cualquier fila** — si algo no alcanza, rechazan la creación entera (no queda ninguna cotización huérfana) y el carrito sigue abierto para seguir editándolo. Frontend: se revirtió el chequeo cliente que la ronda 1 había agregado en `PosFastCheckout.tsx` (comparaba contra un `available` cacheado) — ahora usa `stockShortfalls` de la misma llamada de preview que ya pedía para los totales (`useOrderTotalsPreview`, extendido); `PosTabAccount.tsx` guarda el carrito al instante (sin el debounce de 2s) apenas se agrega un producto; `OrderDetail.tsx` quedó alimentado por el mismo mecanismo sin tocar el diálogo de confirmación pre-existente (`StockShortfallDialog`, chequeo cliente grandfathered). Verificado: `deno check`/`npm run build` limpios, 3 Edge Functions redesplegadas — **no probado en navegador** (sin acceso a browser en este entorno), pendiente que el usuario confirme en vivo.

**Catálogo rediseñado** (commits `6a2dc04`/`6447564`): el storefront público ganó filtros de precio mínimo/máximo, conteo de productos por categoría, y URLs por `slug` en vez de por `id` — nueva migración `20260905200000_products_auto_slug.sql` que autogenera `products.slug` (usando `unaccent` + normalización propia) porque hasta ahora era un campo 100% manual que ningún flujo real leía; con backfill de todo el catálogo existente y resolución de colisiones con sufijo numérico. `get_product` del storefront ahora acepta tanto el id real (compatibilidad con links viejos y el carrito) como el slug, distinguiéndolos por formato UUID, y expone stock disponible en el detalle (antes solo la grilla lo mostraba). En el portal del tenant, `Products.tsx` ganó `QuickStockPopover`: click-to-edit del stock directamente desde la lista (mismo espíritu que el switch de Activo/Inactivo de al lado), que como el modelo real es un kardex y no un campo "cantidad", registra la diferencia como `ajuste_positivo`/`ajuste_negativo` vía `recordStockMovement` — solo se ofrece para productos sin variantes. Se extrajo `ProductSearchBox` (molécula compartida entre el POS y el editor de ítems de orden) con navegación completa por teclado (flechas + Enter), pedido explícito del usuario para no depender del mouse al escanear/tipear productos en el mostrador.

**Ventas pasa a ser la pantalla de aterrizaje post-login** (`6447564`): la ruta índice `/app` dejó de ser Conversaciones (movida a `/app/conversations`) para redirigir a `/app/sales` — pedido explícito del usuario, sin más contexto de negocio documentado en el commit.

**Citas: agendamiento real y limpieza de una promesa de WhatsApp que nunca se podía cumplir** (`7f9a4e2`, migraciones `20260906200000_appointments_scheduling.sql` y `220000_unschedule_appointment_reminders.sql`): hasta ahora una cita era un único instante (`scheduled_at`) sin hora de fin, sin forma de dibujar un bloque real en una grilla horaria. Se agregó `appointments.ends_at` (trigger de default `scheduled_at + 30min`, sin tocar el tool-calling de la IA, que sigue mandando solo `scheduled_at`) y `appointments.opportunity_id` (mismo patrón que `tasks.opportunity_id`). Nuevo hook `useOpportunityStageGate`: completar una tarea o cita con oportunidad vinculada exige elegir su etapa antes de dejar completar (cableado en Calendar, AppointmentDetailDrawer, OpportunityPanel, ClientDetail). **Se desactivó el cron de recordatorio automático de citas por WhatsApp** (`cron.unschedule('send-appointment-reminders')`): el mecanismo manda texto libre, que solo entrega dentro de la ventana de servicio al cliente de 24h de Meta — un recordatorio "1 hora antes" de una cita agendada con días de anticipación casi nunca cae dentro de esa ventana, y nunca se construyó la plantilla HSM aprobada que haría falta (mismo requisito ya resuelto para el OTP de checkout de la tienda pública). Se sacó la promesa de "te recordamos por WhatsApp" del formulario y la ficha del cliente; la Edge Function y el trigger quedan como código muerto pero inofensivo, reactivable con una sola migración el día que exista la plantilla.

**Calendario rediseñado** (`4cf7a99`): se descompuso `Calendar.tsx` en piezas reutilizables — `agendaShared.ts` (lógica de color/estado compartida, documentando el feedback del 09-02 sobre los 7 colores confusos), `CalendarEntryRow.tsx` (fila unificada de cita/tarea con acciones siempre visibles, no ocultas tras `:hover` — mobile-first, el hover no existe en touch), `TimeGrid.tsx` (grilla horaria de 6am-10pm con detección de solapamiento) y `AgendaSidePanel.tsx`/`CalendarListView.tsx`.

## Estado actual (2026-09-05) — Paginación server-side, endurecimiento de seguridad, reversión de stock al cancelar, y refinamiento del ticket POS

**Paginación real y fuga de datos entre tenants corregida** (`409e9aa`): nueva Edge Function `list-sales-orders` + RPC `get_sales_orders_summary`, que paginan con `.range()` en la DB y agregan el resumen de ventas en Postgres, reemplazando el fetch completo sin límite que se cortaba en 1000 filas (límite de `db.max_rows` de PostgREST) — un tenant real ya tenía 28k+ pedidos. **Bug de seguridad real corregido**: `calculate-order`/`create-order`/`sales-order-items` resolvían `tax_type_code`/`tax_rate` de `products` **sin filtrar por `tenant_id`** — un tenant podía terminar leyendo la configuración fiscal de un producto de otro tenant; tampoco se validaba que `contact_id`/`opportunity_id`/direcciones del body pertenecieran al tenant del caller antes de escribirlos. Se retiró el bypass `DIAN_SUBMIT_TEST_TOKEN` de `dian-submit`. Se eliminaron de `sales_orders` las columnas `pos_table`/`pos_room`/`party_size`/`server_name`/`sale_type`/`payment_method_label` (texto libre heredado de una integración con Fudo nunca poblada de verdad) a favor de `pos_point_id → pos_points`.

**Fix de raíz: el stock no se revertía al cancelar una venta** (`ede37f5`, migración `20260905190000_sales_order_cancel_stock_reversal.sql`): confirmar una venta descuenta stock desde el 25 de agosto, pero cancelarla nunca lo devolvía — encontrado al limpiar pedidos de prueba de la base real. Se agregó el trigger simétrico que faltaba.

**Facturación DIAN, corrección grave** (`0eca278`): tanto el ticket POS como el PDF mostraban "Factura electrónica de venta" + resolución DIAN + un número (con fallback al número interno del pedido) para facturas que la DIAN **nunca validó** (`pending`/`rejected`/`error`), sin CUFE ni QR — aparentando una factura legalmente válida que no lo era. Se corrigió `isRemision` para exigir que la factura puntual esté `sent`/`accepted` con CUFE e `invoice_number` reales; cualquier otro estado es remisión sin excepción.

**Refinamiento del ticket térmico**, varias rondas por feedback directo de impresión real: `ReceiptPrintPortal` corrigió un `@page` inválido (`"80mm auto"` no es CSS válido, hacía caer al navegador en hoja Carta) con medidas fijas y el alto real calculado, agregó márgenes reales y subió legibilidad tipográfica. Se agregó `PosPendingReceiptTicket` para imprimir "lo que tengo cargado" antes de cobrar, siempre marcado como cuenta sin cobrar. Se corrigió que el ticket mostrara "Subtotal" en vez de "Base gravable" con impuesto discriminado, se agregó margen de seguridad y `break-inside: avoid` para evitar una segunda página casi en blanco (papel perdido real reportado por el usuario), y el logo pasó a alto fijo (90px). Ajustes finos posteriores redujeron el margen de cortina de 10mm a 4mm y el margen lateral de 3/2mm a 1mm (evitar duplicar el margen no imprimible del driver Epson APD).

**Otros ajustes del día**: `Órdenes` ocultó el selector de bodega cuando el tenant tiene una sola; se rediseñó la tarjeta de cliente/direcciones en `ClientPickerCard` (componente único compartido con el POS, reemplaza el bloque "Contacto" y `PosCustomerCard.tsx`, que se borró) con el resumen usando el desglose real de impuestos por tarifa; se corrigió que el Select de tipo de impuesto ya elegido apareciera en blanco al abrir el drawer de Productos (limitación de Radix Select); y se renombró la etiqueta de estado "Cancelada" a "Anulada" en contactos/oportunidades/órdenes por terminología del mercado colombiano.

## Estado actual (2026-09-04) — Módulo POS completo, impresión de tickets térmicos, carrito unificado y limpieza de datos

**Módulo POS** (`12b4a8f`, ~10 migraciones `20260904055*`/`145*`): nuevo módulo togglable `pos` con permisos dedicados (`pos.view`, `pos.checkout`). Dos flujos de venta: venta rápida de un solo viaje vía `pos-checkout` (crea, confirma y cobra en una sola llamada), o —si el tenant activa "Cuentas abiertas" (`tenants.pos_allow_open_tabs`)— un flujo de carritos (`carts`/`cart_items`) donde varias ventas quedan sin cobrar a la vez; recién al cobrar, `create-order` convierte el carrito en un pedido real. Puntos de venta opcionales (`pos_points`, gestionables desde Configuración), escaneo de código de barras, cliente "Consumidor Final" sembrado por tenant, y `sales_channel`/`cart_id`/`pos_point_id`/`label` en `sales_orders` para que un pedido de mostrador se distinga y se confirme sin exigir direcciones de envío.

**Cálculo de totales unificado** (`2148fd6`): se extrajo `computeOrderTotals` de `persistOrderItems` para que cualquier caller pueda pedir el mismo cálculo sin escribir nada, vía `calculate-order` en modo `preview`. `OrderTotalsSummary`/`useOrderTotalsPreview` reemplazan las cifras sueltas de cada pantalla. `PaymentDrawer` se rediseñó: la creación del pedido se difiere hasta que se guarda el pago — **bug real corregido**: antes el pedido se creaba al abrir el drawer, así que cerrarlo sin pagar dejaba la venta hecha y la cuenta sin sus productos.

**Impresión de tickets térmicos** (`4e494bc`): ticket imprimible vía `usePosReceiptPrinter` (React Portal + diálogo de impresión del sistema — no hay impresión 100% silenciosa sin un agente local tipo QZ Tray). Configuración por tenant (`pos_receipt_paper_width` 58/80mm, `pos_auto_print`, `pos_receipt_footer_message`). El ticket refleja el estado fiscal real: sin DIAN configurada imprime "TICKET", con resolución muestra el número/rango, y solo con la DIAN aceptado agrega CUFE y QR.

**Carrito unificado y reconciliación de migraciones** (`befee55`, `e3bbb04`): `storefront_carts`/`storefront_cart_items` se eliminó a favor de las mismas tablas de Órdenes/POS (`carts.origin='storefront'`, `carts.session_token` para el visitante anónimo). Se detectó que 7 migraciones de carts/POS ya estaban aplicadas en producción pero nunca se habían guardado como archivo en el repo — se reconstruyeron con el SQL exacto ya corrido.

**Otros ajustes del día**: se sacó `einvoicing` del catálogo de módulos togglables por redundante con la propia integración DIAN. `clients.phone` se partió en `phone_prefix` + `phone`. Settings se reorganizó por categoría (Empresa/Tienda/Documentos/POS/Bodegas/Despachos/Devoluciones). `sales_channel` pasó a ser escrito por los cuatro caminos que crean un pedido, con Orders.tsx ganando filtro de Origen y filtros con Aplicar/Borrar. Se cerró el ciclo de facturación en el detalle del pedido (tarjeta "Factura DIAN" con estado y acción, descarga del PDF, bloqueo del pedido recién cuando algo irreversible pasó). Se reemplazó voseo rioplatense por tuteo en toda la app, alineado al mercado colombiano real del producto.

## Estado actual (2026-09-03) — Facturación electrónica DIAN: impuestos, esquema, XML firmado, y el bug real que centralizó la confirmación de venta en la DB

Base fiscal completa para poder emitir factura electrónica ante la DIAN colombiana, con el módulo apagado por defecto para todo tenant.

**Impuestos** (`48f202a`): `tax_types` es el catálogo de códigos DIAN (01 IVA, 02 IC, 03 ICA, 04 INC, 05-07 retenciones), referenciado por FK. `products` gana tipo y tarifa de impuesto; `clients` gana el flag de autorretenedor y el tipo de documento del catálogo DIAN. `sales_order_items` copia impuesto/tarifa/base gravable **por línea** en el momento de la venta — si el producto cambia de tarifa después, un pedido ya emitido no se entera. El precio ya incluye el impuesto — se extrae, no se suma; el cálculo vive en el servidor, el frontend solo lo replica para la vista previa.

**Configuración DIAN por tenant** (`4c14551`): régimen fiscal, resolución autorizada (prefijo, rango, vigencia), software ID y consecutivo. El certificado de firma (.p12) va a un bucket privado propio, no al de logos. El PIN y la clave técnica se guardan como secretos de la credencial (mismo patrón que Wompi). Nuevo proveedor `dian_directo` — cada tenant es su propio facturador electrónico, Leadly construye y firma el XML pero nunca actúa como Proveedor Tecnológico.

**Esquema, XML firmado y envío por SOAP** (`60d9ad4`): `sales_invoices`/`_items`/`_withholdings`. Un reintento es una fila **nueva** con `attempt_number+1`, nunca se edita el intento rechazado (auditoría legal). CUFE (SHA-384), UBL 2.1 firmado con XAdES-EPES, sobre SOAP firmado con WS-Security y envío por mTLS. El transporte se confirmó en vivo contra el ambiente de habilitación de la DIAN: SOAP plano con el ZIP en base64 inline, **no MTOM** (esa variante da 415).

**Representación en PDF** (`cf60afb`): Factura (con DIAN activa) o Remisión `REM-<pedido>` (sin resolución/CUFE/QR, mismo alto). Se arma con los datos **en vivo** del pedido, no con el snapshot de la factura. El QR solo aparece si la DIAN aceptó el documento.

**Bug real de producción, y su fix estructural** (`20260903180000_sales_order_confirm_triggers.sql`): confirmar una venta descontaba stock, movía la oportunidad a Ganado y reservaba la factura desde el código de **cada caller** (IA, tienda pública, portal) — tres copias de la misma regla. Encontrado en vivo: el botón "Confirmar" del portal hacía un UPDATE directo a `sales_orders` **sin pasar por ese código compartido** — una venta confirmada a mano en el portal nunca chequeaba stock de verdad, nunca movía la oportunidad ni reservaba su factura. Se centralizó en triggers de base de datos (`guard_sales_order_confirmation` `BEFORE UPDATE`, `apply_sales_order_confirmed_effects` `AFTER UPDATE`) para que aplique sin importar quién confirme, incluso por SQL directo.

## Estado actual (2026-09-02) — Rebrand a Lexy, Dashboard/Calendario con datos reales, e integridad de la IA

**Rebrand** (`990d17a`): `Logo.tsx` pasa a renderizar la marca real de Lexy (ícono + wordmark, variantes claro/oscuro) en lugar del wordmark de texto "Leadly" — cambio de identidad visual y de copy de marketing, **no** un rename del código (el repo/tablas/`CLAUDE.md` siguen diciendo "Leadly"). El panel de marketing del login se reescribió sacando el framing de "CRM" por una descripción del alcance real (conversaciones con IA, ventas+inventario+crédito, organización de equipo/tareas) — reposiciona el producto de CRM a plataforma de operaciones.

**Calendario y Dashboard rediseñados alrededor de datos reales** (`7a03ea1`): el Calendario tenía 7 colores distintos repartidos en 4 mapas, sin leyenda (feedback del usuario: "no se como manejarlo"). Se reemplazó por un esquema donde el color codifica **una sola cosa a la vez**: cita=celeste, tarea=violeta; vencida gana y pinta todo rojo sin importar el tipo; completada/cancelada tienen color fijo propio. El Dashboard ganó 4 tarjetas KPI (ventas/pedidos/despachos/cartera) conectadas a datos reales, un feed de "Actividad reciente" y un filtro de rango de fechas con delta real "vs período anterior" — Cartera queda sin filtrar a propósito, es un saldo vivo, no un flujo de período.

**Integridad del tool-calling de la IA y tracking automático de actividad** (`31ac2f7`): se extendió la regla de integridad para que el modelo no pueda decir "voy a verificar" y quedarse ahí sin llamar la tool en el mismo turno (encontrado en vivo: una oportunidad quedó sin confirmar, cliente esperando). Nuevo trigger: toda oportunidad abierta nueva genera automáticamente una tarea de seguimiento vinculada (vence en 24h) — cubre IA y creación manual. Se agregaron `tasks.completed_at`/`completed_by`, `appointments.assigned_to` y `get_agent_activity_summary()` — base para medir si un agente cumple lo asignado a tiempo. Nueva Edge Function `send-task-reminders` + cron (15 min) avisa por WhatsApp al agente cuando una tarea vence.

## Estado actual (2026-08-29) — Roles y permisos granulares por acción (en construcción, hitos 1-2 de 5 completos)

**Pedido explícito del usuario**: reemplazar el modelo actual de roles (`profiles.role`: `superadmin`/`tenant_admin`/`tenant_agent`, donde `tenant_agent` tiene exactamente el mismo CRUD que `tenant_admin` tanto en rutas de `/app` como en RLS — indistinguibles) por permisos **por acción dentro de cada módulo** ("no un permiso general por todo el módulo"), con **cada tenant creando sus propios roles** (ej. "Vendedor", "Soporte") y asignándoles el subconjunto de acciones del catálogo que quiera. Decisiones cerradas con el usuario: permisos configurables por tenant (no un set fijo de plataforma); enforcement en frontend **y** RLS; se construye para todos los módulos operativos de una vez, no un piloto; `tenant_admin` se mantiene como hoy — un rol "dueño" fijo con acceso total no editable (Integraciones/Facturación/Configuración de empresa/gestión de usuarios y roles siguen admin-only) — los roles personalizados aplican solo a usuarios `tenant_agent`, sobre los módulos operativos (Conversaciones, Clientes, Oportunidades, Productos, Ventas, Crédito, Devoluciones, Calendario/Tareas, Campañas, IA & Agentes).

Dado el tamaño real (nueva capa de esquema + UI de administración + gating en ~11 módulos + reescritura de RLS en ~20 tablas), se planificó en 5 hitos secuenciales, cada uno verificable antes de pasar al siguiente — **no** una migración ciega que reescriba todo RLS de un saque. Plan completo (catálogo de acciones, esquema, ejemplo de RLS, inventario de archivos de frontend) en el historial de la sesión; no se guardó como archivo aparte.

**Hito 1 — Esquema, completo y aplicado** (`20260829120000_role_permissions_foundation.sql`):
- `permission_actions` — catálogo fijo de 48 acciones (ej. `contacts.view`/`create`/`edit`/`delete`/`manage_credit`, `sales.confirm`, `pipeline.manage_pipelines`, `aiAgents.manage`...) sobre 11 `module_key` (mismas claves que `lib/modules.ts`). De código, nadie lo edita desde la UI — agregar una acción nueva es una migración.
- `tenant_roles` — los roles que cada tenant crea. A diferencia de `tenant_enabled_modules`/`ai_assistant_skills` (superadmin-only write), **el tenant se autogestiona esto** (`is_tenant_admin()` puede escribir sobre su propio tenant). Tiene identidad de negocio real (un usuario queda asignado a la fila) → soft-delete, no DELETE real.
- `tenant_role_permissions` — bridge presence-based (rol × acción), sin identidad propia → DELETE real al desmarcar, igual que `ai_assistant_skills`.
- `profiles.tenant_role_id` (nullable) — a qué rol de su tenant pertenece un `tenant_agent`; `tenant_admin`/`superadmin` lo ignoran.
- `has_permission(action_key)` (función `security definer`, mismo patrón que `is_tenant_admin()`) — `true` incondicional para superadmin/tenant_admin, si no existe la acción en el rol asignado. **Todavía no se usa en ninguna policy de RLS** (eso es el hito 5) — hoy solo la usa el frontend indirectamente vía `listMyPermissionKeys`.
- Trigger `seed_default_tenant_role()` (`after insert on tenants`, mismo patrón que `seed_default_pipeline`/`seed_default_warehouse`) — todo tenant nuevo arranca con un rol "Agente" con el catálogo completo otorgado. Backfill: cada uno de los 4 tenants existentes recibió su rol "Agente" con las 48 acciones, y los `tenant_agent` existentes (0 en producción hoy) quedaron asignados — **sin esto, cualquier agente real habría perdido acceso de golpe el día del deploy**, ya que hoy tiene acceso total de facto.
- Verificado: `get_advisors` sin alertas nuevas sobre las tablas creadas.

**Hito 2 — Tipos, API, `AuthContext` y UI de gestión, completo**:
- `types/domain.ts`: `PermissionAction`, `TenantRole`, `Profile.tenant_role_id`.
- `lib/api/permissions.ts` (nuevo): catálogo, CRUD de `tenant_roles` (soft-delete), `setRolePermissions` (diff simple, no delete-all+insert-all), `listMyPermissionKeys` (superadmin/tenant_admin → todo el catálogo sin ir a la tabla; tenant_agent → vía su `tenant_role_id`).
- `AuthContext.tsx`: mismo mecanismo que `enabledModules` — `permissions: Set<string> | null`, cargado en paralelo con tenant/módulos, falla cerrado. Hook nuevo `usePermission(actionKey)` (solo para mostrar/ocultar UI — el límite real es RLS, que todavía no lo aplica, ver hito 5).
- **"Roles y permisos"** — sección nueva en `Settings.tsx` (tenant_admin only, junto al resto de `CardSection`s de la empresa): `RolesSection.tsx` (lista + crear/editar/eliminar) + `RoleDrawer.tsx` (nombre/descripción + checklist de `permission_actions` agrupado por módulo, con checkbox "seleccionar todo el módulo").
- **"Usuarios"** — sección nueva en `Settings.tsx`, cablea `UsersTable`/`UserInviteDrawer` (`pages/shared/`, hasta ahora solo montados desde el backoffice) para que el tenant se autogestione sus propios usuarios por primera vez.
- `UserInviteDrawer.tsx`: cuando el rol de plataforma es `tenant_agent`, aparece un segundo select obligatorio para elegir su `tenant_role_id` (de los roles vivos del tenant).
- Edge Function `admin-create-tenant-user` actualizada y redesplegada: acepta `tenant_role_id`, lo exige si `role='tenant_agent'`, y valida que el rol pertenezca al mismo `tenant_id` antes de usarlo (nunca confía en el id que manda el caller sin comprobar dueño primero — mismo criterio que `resolveOrderAddress` del storefront).
- Verificado: `npm run build` limpio.

**Hito 3 arrancado el mismo día, adelantado a pedido explícito del usuario** ("para qué pones permisos si no los vas a validar... si lo hago debe salirme un candado"): hasta acá los permisos se guardaban pero no restringían nada real -- se adelantó la parte de enforcement a nivel de ruta (bloquear con candado, no solo esconder un botón) antes de seguir con el resto del hito 3 (gating de botones individuales).
- `RequireModule` (`routes/guards.tsx`) ganó un prop opcional `action` -- además de `enabledModules.has(moduleKey)`, ahora también exige `permissions.has(action)` cuando se pasa; si falta, mismo `<AccessDenied/>` ("Acceso denegado... contacta a tu administrador") que ya usaba el gating por módulo, no una pantalla nueva.
- Cableado en `App.tsx` para cada ruta que tiene una acción `*.view` en el catálogo: `conversations.view` (Inbox), `contacts.view`, `pipeline.view`, `products.view` (+ categorías/marcas/proveedores/detalle), `sales.view` (+ detalle), `credit.view`, `returns.view`, `calendar.view`, `campaigns.view`, `aiAgents.view`. Dashboard/Facturación/Integraciones/Configuración quedan sin `action` -- siguen fuera del sistema de permisos granulares (son admin-only o de plataforma, no módulos operativos, ver el alcance acordado en el hito 1).
- **Bug real encontrado al armar esta lista**: `conversations` no tenía ninguna acción `.view` en el catálogo original (solo `reply`/`assign`/`manage_tags`/`delete`) -- no había forma de gatear "puede abrir el Inbox". Migración `20260829150000_permission_actions_conversations_view.sql`: agrega `conversations.view` (acción #49) y hace backfill otorgándosela a todo `tenant_role` que ya tuviera `conversations.reply` (los 4 roles "Agente" existentes, con el catálogo completo, no perdieron nada).
- `lib/modules.ts` (`TenantModuleDefinition`) ganó `viewAction?: string` -- mismo criterio, `TenantLayout.tsx` ahora también filtra el nav por `permissions?.has(module.viewAction)` además de `enabledModules`/`adminOnly`: un agente sin el permiso de ver un módulo ni siquiera ve el ítem en el menú, no solo lo tiene bloqueado si entra por URL directa.
- **Pendiente explícito, todavía no es un límite real de seguridad**: esto es enforcement de **frontend únicamente** -- alguien llamando la API/Supabase directo (no a través de la UI) todavía puede leer/escribir esas tablas, porque RLS sigue sin usar `has_permission()` (eso es el hito 5, sin empezar). El resto del hito 3 (ocultar/deshabilitar botones de crear/editar/eliminar/etc. dentro de cada página, no solo el candado de entrada) tampoco arrancó todavía.

**Correcciones de UX el mismo día, pedidas explícitamente por el usuario**: (1) `UserInviteDrawer.tsx` pedía el rol dos veces (un select de rol de plataforma `tenant_admin`/`tenant_agent`, y si era agente, un segundo select de `tenant_role_id`) -- se fusionó en **un solo select "Rol"** con la opción "Administrador" (mapea a `tenant_admin`, sin `tenant_role_id`) seguida de los `tenant_roles` vivos del tenant (mapean a `tenant_agent` + ese `tenant_role_id`) -- `ADMIN_VALUE` como sentinela que nunca colisiona con un uuid real. (2) Las vistas nuevas `Users.tsx`/`Roles.tsx` perdieron el `Card` envolvente (el usuario no quería un recuadro general alrededor del contenido, solo el borde propio de la tabla/lista) y el párrafo de descripción arriba del botón "Nuevo rol" (sin subtítulo, mismo criterio que el resto de vistas de lista). (3) El botón "Nuevo rol" tenía `variant="ghost"` (blanco) mientras el resto de botones "crear" de la app son primarios (azul, sin variant) -- corregido para que use el mismo `Button` por defecto.

"Usuarios" y "Roles y permisos" salieron de ser `CardSection`s dentro de Configuración y pasaron a ser **vistas propias con su propio ítem en el menú lateral** (`pages/tenant/Users.tsx`/`Roles.tsx`, rutas `/app/users`/`/app/roles`), y **ambas solo visibles para `tenant_admin`** -- ni en el nav ni por URL directa (`RequireRole allowed={['tenant_admin']}`, no `RequireModule` -- no son un módulo togglable por el superadmin, todo tenant las tiene siempre). `lib/modules.ts` (`TenantModuleDefinition`) ganó dos flags nuevos: `alwaysEnabled` (bypasea `tenant_enabled_modules` por completo -- estas dos entradas nunca tienen fila en esa tabla) y `adminOnly` (oculta el ítem del nav para `tenant_agent`, chequeado contra `profile.role` en `TenantLayout.tsx`, no contra RLS/módulos). `TenantModulesSection.tsx` (tab "Módulos" del backoffice) filtra `!module.alwaysEnabled` para no mostrarlas como togglables ahí -- tocarlas no tendría ningún efecto. El título de página se resuelve solo (mismo mecanismo que el resto de la app, `AppShell` deriva el `<h1>` del label del nav item que matchea la ruta), así que ninguna de las dos vistas nuevas renderiza su propio título.

**Pendiente explícito, en orden** (ver plan de la sesión para el detalle completo):
3. Gating de botones (usePermission) en los ~11 módulos operativos — hoy `usePermission` existe pero **ningún botón de la app todavía lo usa**, un agente sigue viendo todo igual que un admin en la UI.
4. Reemplazar los `isAdmin` sueltos que en realidad son una acción de módulo (`AiAgents.tsx`, `Opportunities.tsx` pipelines, `ChatPanel.tsx` tags) por `usePermission`.
5. RLS módulo por módulo (empezar por `clients`, patrón: reemplazar las 4 policies `clients_select/insert/update/delete` para exigir `has_permission('contacts.view'|'create'|'edit'|'delete')` además del tenant scoping actual), cada uno verificado con `get_advisors` + prueba manual antes de seguir al siguiente. **Hasta que esto no se haga, destildar un permiso en el editor de roles no restringe nada de verdad** — solo hoy es una gestión de datos sin efecto de seguridad.

## Estado actual (2026-08-28) — Clientes: se sacaron industria/sitio web/dirección/ciudad, se agregó país + tipo de documento

Se eliminaron `industry`/`website`/`address`/`city` de `clients` (columna + campo del formulario, `ContactDrawer.tsx`) — ninguno se usaba en ningún lado real, y la dirección real de envío/facturación ya vive en `contact_addresses` (múltiples direcciones por cliente). Se agregó `country` (mismo criterio que `tenants.country`, código curado desde `COUNTRIES`) y se expusieron en el formulario `document_type`/`document_number` (columnas que ya existían desde el 24-08 para uso de la IA, nunca añadidas al formulario manual) — mismo patrón de selects que `TenantFormFields.tsx`. Migración `20260828130000_clients_country_drop_stale_fields.sql`, aplicada. `ClientDetail.tsx`/`Clients.tsx` actualizados para dejar de referenciar `city`.

## Estado actual (2026-08-27) — Revisión de habilidades de IA: PQR eliminado, leads fusionado con oportunidades

**Contexto**: al auditar qué le llega hoy a la IA (`_shared/aiTools.ts`, 40 tools en 11 `ai_skills`), aparecieron dos problemas reales que el usuario pidió resolver ya mismo, dejando **`ventas` deliberadamente sin tocar** para una ronda aparte (la idea que quedó planteada: un flag en Configuración para que el tenant decida si la IA cierra venta directa, arma cotización, o llena el carrito y redirige a la tienda pública — sin definir todavía).

**`pqr` era código 100% muerto**: el módulo PQR se había borrado de la base el 17 de agosto (`ai_skills` ya no tenía esa fila, con un FK `on delete restrict` que impide que exista ningún `ai_assistant_skills` con `skill_key='pqr'`) pero las 6 tools (`create_pqr`, `create_note`, `add_pqr_update`, `update_pqr_status`, `get_pqr_status`, `send_attachment`) seguían declaradas en `_shared/aiTools.ts` sin ningún `case` que las ejecutara en `whatsapp-ai-tools` (ya limpiado ahí desde esa misma reorganización). Pedido explícito del usuario: "pqr debemos eliminar cualquier cosa relacionada con esto". Se borraron las 6 definiciones de `_shared/aiTools.ts`, y en `whatsapp-ai-respond` se sacó todo el pipeline de `pendingAttachment`/`ATTACHABLE_TOOLS` (existía únicamente para adjuntarle a un PQR una foto que el cliente mandaba por WhatsApp -- sin PQR, era peso muerto: el `Set` de tools adjuntables quedaba vacío para siempre) incluidas las columnas `media_storage_path`/`media_mime_type`/`media_size_bytes` que solo se seleccionaban para eso.

**`leads` se fusionó con `oportunidades`**: `set_lead_stage` (skill `leads`) escribía `clients.stage` (lead/contactado/negociación/cliente/perdido, clasificación libre a nivel de contacto), mientras que las tools de oportunidades escriben `opportunities.stage_id` (etapa real de un pipeline) -- dos clasificaciones de avance independientes y potencialmente desincronizadas del mismo contacto. Decisión explícita del usuario, con una razón de negocio concreta: "no hay lead sin oportunidad" -- un contacto puede tener varias oportunidades abiertas a la vez, así que una sola etapa plana por cliente no tiene sentido como concepto. Se optó por **eliminar `clients.stage` por completo** (no solo dejar de exponerla a la IA) -- pedido explícito: "esa columna en la db sobra". Alcance real de la limpieza:
- Backend: `set_lead_stage` borrado de `_shared/aiTools.ts` y su `case` + la constante `LEAD_STAGES` borrados de `whatsapp-ai-tools/index.ts`. La skill `oportunidades` no necesitó cambios de prompt -- su `prompt_fragment` ya era autocontenido, sin ninguna referencia a "leads".
- Migración `20260827120000_remove_leads_skill_and_client_stage.sql`: borra las filas de `ai_assistant_skills` con `skill_key='leads'` (el FK `on delete restrict` exige hacerlo antes), borra la fila `leads` de `ai_skills`, y recién ahí `alter table clients drop column stage` (el check constraint de la columna cae solo). TecnoNova Colombia era el único tenant con `leads` habilitada -- no perdió nada real (ya tenía `oportunidades` activa también).
- Frontend: `ClientStage`/`stage` salieron de `types/domain.ts` (`Client`) y `lib/api/clients.ts` (`ClientInput`). `ContactDrawer.tsx` perdió el select de "Etapa" y `STAGE_LABEL` -- de paso se corrigió un bug real encontrado en el mismo archivo: el bloque "Agente asignado" estaba **duplicado** (una copia en el grid cuando `!showStage`, y otra copia idéntica siempre visible debajo cuando `showStage` -- quedaba repetido dos veces con el módulo Pipeline activo). `Clients.tsx` perdió el filtro/columna/badge de etapa (`STAGE_BADGE_CLASS`, `stageFilter`). `ClientDetail.tsx` perdió el badge de etapa del header, pero **mantuvo** el gate `enabledModules.has('pipeline')` -- renombrado a `showPipelineModule` -- para el campo "Oportunidades abiertas" del resumen, que es un concepto distinto (visibilidad del módulo Pipeline, no la etapa del cliente) que compartía la misma variable por accidente. `LinkClientDrawer.tsx`/`NewConversationDrawer.tsx` (auto-creación de contacto desde el Inbox) dejaron de mandar `stage: 'lead'` al crear un contacto. i18n: se borraron las claves `contacts.stage.*`, `contacts.filters.stage.*`, `contacts.filters.labels.stage`, `contacts.table.stage`, `contacts.drawer.fields.stage` (es/en).
- Verificado: `npm run build` limpio, `deno check` limpio en `whatsapp-ai-respond`/`whatsapp-ai-tools` (los mismos 8 errores preexistentes de tipos implícitos en el filtrado de `enabledSkills`, no introducidos por este cambio, ya documentados en rondas anteriores), `get_advisors` sin ninguna alerta nueva sobre `clients`/`ai_skills`/`ai_assistant_skills`. Ambas Edge Functions redesplegadas.

**Calendario**: revisado a pedido del usuario si "un evento es una tarea por cumplir" -- confirmado que **no**, `appointments` (citas de `book_appointment`/`list_contact_appointments`/`cancel_appointment`) y `tasks` (tareas, incluida la que crea `flag_interest_for_followup`) son dos tablas completamente separadas sin ningún FK entre sí. El usuario decidió explícitamente **dejarlo así** ("lo de calendario asi como esta actualmente esta perfecto") -- no se tocó nada de este módulo.

**Pendiente explícito**: la revisión de `ventas` (flag de configuración para elegir el modo de venta de la IA -- directa/cotización/carrito-y-redirección a la tienda pública) quedó para el final, todavía sin diseñar.

## Estado actual (2026-08-27) — Bug real de producción: la confirmación de pago de Wompi rechazaba SIEMPRE el webhook

**Encontrado probando el primer pago real de sandbox de la tienda pública** (tarjeta de prueba de Wompi, 4242 4242 4242 4242): el checkout redirigía bien a Wompi, cobraba bien, y redirigía bien de vuelta a `?payment=return` -- pero la página nunca confirmaba, cayendo siempre en "Todavía estamos confirmando tu pago" (timeout), y el pedido tampoco aparecía como pagado en el portal.

**Causa raíz, confirmada por logs de `query_logs`**: Wompi sí llamó a `payment-webhook-wompi` (`POST 401`), pero la función lo rechazó con `"invalid signature"` / `"checksum mismatch"`. `verifyWebhookSignature` (`_shared/payments/wompi.ts`) estaba usando el secreto guardado como `integrity_key` para validar el checksum del webhook -- pero Wompi tiene **dos secretos distintos** en su dashboard: el **"Secreto de integridad"** (firma la creación de un Web Checkout del lado del cliente, formato widget -- esta integración no lo usa, crea el link server-side vía `/payment_links` con la llave privada) y el **"Secreto de eventos"** (un secreto totalmente separado, en la sección Eventos/Webhooks del dashboard de Wompi, que es el que realmente firma el checksum de cada webhook). El código nunca pedía este segundo secreto -- el checksum iba a fallar siempre con cualquier cuenta real de Wompi, sin importar qué se hubiera guardado en "Integrity Key".

**Fix aplicado** (cuatro copias del mismo chequeo, cada una corregida -- `isTenantWompiConnected` está deliberadamente duplicada entre `whatsapp-ai-respond`, `whatsapp-ai-tools` y `storefront/index.ts` para mantener cada Edge Function desplegable por separado, ver comentarios en el código):
- `_shared/payments/wompi.ts::verifyWebhookSignature` ahora lee `events_key` (no `integrity_key`) para el checksum.
- Nuevo campo **"Secreto de eventos"** en `WompiIntegrationDrawer.tsx` (con su propio hint explicando que no es el mismo que "Integrity Key"), guardado con el mismo `setPaymentCredentialSecret` genérico (sin migración -- `payment_credential_secrets` acepta cualquier `secret_name`).
- Los 4 chequeos de "Wompi conectado" (`WompiIntegrationDrawer`, `IntegrationsGrid.tsx`, `PaymentDrawer.tsx`, y las 3 copias de `isTenantWompiConnected` en las Edge Functions) pasaron a exigir `private_key` + `events_key` en vez de `private_key` + `integrity_key` -- `integrity_key` no tiene ningún efecto funcional hoy (nunca se lee para crear el link de pago), queda como campo opcional sin uso real, sin quitarlo de la UI por ahora.
- Único tenant con Wompi configurado (TecnoNova Colombia, sandbox) solo tenía `private_key`/`integrity_key` guardados -- **falta que el usuario entre a su dashboard de Wompi sandbox, copie el "Secreto de eventos" (no el de integridad) y lo pegue en el campo nuevo de Integraciones** antes de reintentar un pago. El pedido #23 (el de esta prueba) va a seguir viéndose sin pagar en el portal -- Wompi ya intentó notificar y fue rechazado, hay que generar un pedido nuevo después de cargar el secreto correcto.
- Verificado: `deno check` limpio en las 4 funciones tocadas (los 8 errores de tipo que aparecen en `whatsapp-ai-respond` son preexistentes, en otra sección del archivo, no introducidos por este fix), `npm run build` limpio, las 4 Edge Functions redesplegadas (`payment-webhook-wompi` con `--no-verify-jwt` preservado, las otras 3 con `verify_jwt` sin cambios respecto a antes del deploy).

## Estado actual (2026-08-26, sesión posterior) — Tienda pública por tenant (marketplace)

**Pivote explícito del usuario**: el "carrito editable" documentado en el bloque de abajo (link por pedido, `public-cart`) quedó **reemplazado, no complementado** — al plantear la implementación, el usuario pidió algo distinto: "esto del link es un link generico o un tipo de catalogo para que el tenant lo comparta con sus clientes y ellos puedan comprar desde alli?... quiero que el link sea como un marketplace". Son arquitecturas distintas (un link por pedido asume una orden y un cliente ya identificados; una tienda pública arranca sin ninguno de los dos) y forzar una sobre la otra complicaba ambas, así que se **borró por completo** lo anterior: Edge Function `public-cart`, tabla `sales_order_checkout_links`, tool `send_cart_link` y la habilidad `carrito` (nada de esto tenía ninguna fila real en producción usándolo — `carrito` nunca se habilitó para ningún asistente). Se mantuvo sin cambios `_shared/orders/confirmSalesOrder.ts` y todo `_shared/payments/salesOrderPayments.ts` (infraestructura genuinamente compartida entre `confirm_quote` y el checkout nuevo).

**Verificación de identidad — pregunta real del usuario, no un detalle menor**: "si el cliente ya existe en el tenant, ¿qué validación se hace para asegurarnos que es el cliente del tenant el que está usando el catálogo?". Un checkout que solo pidiera teléfono como texto libre dejaría que cualquiera se hiciera pasar por un cliente real — grave en particular si ese cliente tiene crédito (fiado) habilitado. Se resolvió con un código de un solo uso por WhatsApp (`storefront_phone_verifications`, atado a `cart_id`+`phone`, vence a los 10 minutos, máximo 5 intentos, rate-limited a 3 códigos por teléfono cada 15 minutos) — uniforme para cliente nuevo o existente. **Hallazgo real durante el diseño**: Meta no deja mandar texto libre a un número que nunca le escribió antes al negocio (ventana de 24h) — hace falta una plantilla HSM aprobada, categoría `AUTHENTICATION`, infraestructura que **ya existía parcialmente** (`whatsapp_message_templates`, `whatsapp-manage-templates`, `sendWhatsappTemplate` en `_shared/whatsapp.ts`) y se reutilizó tal cual. Consecuencia real y no oculta: **cada tenant necesita tener/crear una plantilla `AUTHENTICATION` aprobada por Meta antes de que su tienda pueda verificar teléfonos** — `request_checkout_otp` falla con un mensaje claro ("escribinos por WhatsApp para completar tu compra") si no existe ninguna, no hay fallback inseguro. Tenant QA Uno hoy no tiene ninguna plantilla `AUTHENTICATION` — probado end-to-end simulando la verificación por SQL, pendiente probar el envío real del código en cuanto exista una plantilla aprobada.

Esquema nuevo (migraciones `20260826000004_drop_per_order_cart_link.sql`, `20260826000005_storefront.sql`):
- `products.is_visible_in_catalog` (opt-in, default `false` — mismo criterio cauteloso que `tenant_enabled_modules`, ningún producto existente aparece en la tienda hasta que el tenant lo marque a propósito). `is_active` sigue significando "vendible en general" (por IA o un agente), no lo mismo.
- `tenants.storefront_slug` (único **globalmente**, no por tenant — identifica `/tienda/:slug`) + `tenants.storefront_enabled` (default `false`).
- `storefront_carts`/`storefront_cart_items` — carrito de invitado, existe ANTES de que haya cliente identificado (por eso no es una fila de `sales_orders`). `session_token` vive en `localStorage` del visitante. Un carrito `active` viejo sin `converted_order_id` **es** un carrito abandonado — la métrica de abandono que el usuario pidió a futuro sale de una query directa sobre esta tabla, sin trabajo extra.
- `storefront_phone_verifications` — ver arriba.

Edge Function pública nueva `storefront` (sin JWT, dispatcher por `action`, tenant siempre resuelto por `slug` o por el `tenant_id` del carrito — nunca por un id que mande el cliente): `get_storefront`/`list_categories`/`list_products`/`get_product` (catálogo público, sin stock — mismo criterio que `list_catalog_products`), `get_cart`/`add_to_cart`/`update_cart_item`/`remove_cart_item`, `request_checkout_otp`/`verify_checkout_otp`, `checkout` (exige verificación vigente → resuelve/crea `clients` por teléfono → crea `sales_orders`+`sales_order_items` → `confirmSalesOrder` → resuelve pago con el mismo criterio Wompi/crédito que ya usa `confirm_quote` → marca el carrito `converted`). Probado extremo a extremo contra Tenant QA Uno (catálogo, carrito, y un checkout completo con verificación simulada por SQL — la orden quedó `confirmada` y generó un link real de Wompi), datos sintéticos limpiados después.

**Frontend, primera versión**: ruta pública `/tienda/:slug/*` en `leadly-app` — `StorefrontLayout` + `StorefrontCatalog` + `StorefrontProductDetail` + `StorefrontCart`, construida inicialmente con el mismo set `atoms`/`molecules` que usa `Login.tsx` (la otra página pública del app).

**Rehecho el mismo día, pedido explícito del usuario tras probar la primera versión**: "el catalogo debe usar estilo de shadcn", filtros "profesionales" en un drawer a la derecha (marca, árbol de categorías, ordenamiento, borrar/aplicar), carrito como ícono con badge (no un link de texto), errores en modal, y marcar **todos** los productos del tenant visibles en el catálogo para poder probar con datos reales. Reconstruido:
- `StorefrontCatalog`/`StorefrontProductDetail`/`StorefrontCart` migradas a `components/ui/*` (shadcn) — `Sheet` para el drawer de filtros, `Select` para marca/orden, `Dialog` para el modal de error único (uno solo en `StorefrontLayout`, expuesto a las páginas hijas vía `showError()` en el outlet context), `Badge` para el contador del carrito sobre un ícono `ShoppingCartIcon`.
- El drawer de filtros reutiliza `CategoryTreeFilter` + `descendantIds` (`lib/api/productCategories.ts`) **tal cual los usa el catálogo interno del tenant** (`Products.tsx`) — mismo árbol de categorías, mismo comportamiento de "seleccionar un nodo incluye todo su subárbol". Los filtros son un borrador local dentro del drawer: no afectan el catálogo hasta tocar "Aplicar" (o "Borrar", que limpia y aplica al instante) — igual que un filtro de e-commerce real, no un refiltrado en cada click.
- `storefront` (Edge Function) ganó `list_brands` y `list_categories` ahora devuelve la fila completa de `product_categories` (antes solo `id`/`name`) para que el frontend pueda armar el árbol; `list_products` cambió de `category` (nombre) a `category_ids` (resueltos client-side) + `brand_id` + `sort` (`name_asc`/`price_asc`/`price_desc`/`newest`). Límite subido de 24 a 120 (el catálogo real del tenant tiene 101 productos).
- **Bug real encontrado probando en el navegador**: un `session_token` en `localStorage` que ya no resolvía a ningún carrito (uno viejo limpiado del lado del servidor) hacía fallar `add_to_cart` con "Este carrito no existe" — desde la perspectiva del visitante solo quiso agregar un producto, no le importa que su carrito anterior haya expirado. Fix: `add_to_cart` ahora arma un carrito nuevo en silencio si el token no resuelve, en vez de tirar error.
- **Otro hallazgo real, cosmético pero visible**: varios productos del catálogo tienen `product_images` apuntando a archivos que ya no existen en el storage — la URL se construye igual (`getPublicUrl` no valida existencia), así que el navegador mostraba el ícono roto genérico. `StorefrontImage` (`components/storefront/StorefrontImage.tsx`) nuevo: `<img>` con `onError` que cae a un placeholder consistente, reutilizado en las 3 páginas.
- Todos los productos activos de Tenant QA Uno (101) quedaron con `is_visible_in_catalog = true` para poder probar con el catálogo real completo.
- Plantilla `AUTHENTICATION` real creada y **aprobada automáticamente por Meta** para Tenant QA Uno (`leadly_verificacion_compra`, vía POST directo a la Graph API con el token ya guardado en Vault de esa línea — no hizo falta ninguna sesión nueva) — `request_checkout_otp` ya puede resolver una plantilla real. Falta probar el envío real de un código a un teléfono de verdad (no se mandó ninguno todavía para no molestar a un número real sin permiso explícito).
- Verificado en el Browser pane de punta a punta: catálogo con las 101 fotos reales, drawer de filtros (marca + árbol de categorías con drill-down + orden), agregar al carrito, editar cantidad, badge del carrito, modal de error. `npm run build` limpio.

**Tercera ronda, mismo día -- acciones rápidas desde la grilla**: pedido explícito del usuario -- filtros activos también como chips removibles (no solo el badge numérico del botón "Filtros"), botón "Agregar al carrito" y stock visible directo en cada card (sin entrar al detalle), selector rápido de variante para productos que la tienen, y animaciones de carga en la búsqueda para que nunca se sienta "roto".
- `list_products` ahora devuelve `available` (stock real, sumado por warehouse -- **decisión explícita distinta a `list_catalog_products` de la IA**, que nunca expone stock; acá el usuario lo pidió a propósito para la tienda web) y, para productos con variantes, `variants: [{id, label, price, available}]` -- todo en un único viaje batched (`loadStock`/`loadVariantsByProduct` en `storefront/index.ts`), no una consulta por producto.
- Card del catálogo: badge "Agotado" (destructivo, botón deshabilitado) o "Quedan N" (solo si ≤5, para no saturar visualmente productos con stock cómodo) -- `available: null` (tenant sin `track_inventory`) no muestra nada, se trata como siempre disponible. Producto sin variantes -> botón "Agregar" hace el alta directo (con un estado transitorio "Agregado" ✓ de ~1.2s); producto con variantes -> botón "Elegir opción" abre un Popover con cada variante y su propio stock/alta individual, para no tener que adivinar cuál comprar ni entrar al detalle.
- Chips de filtro activos (`FilterChip`, `Badge` + botón X) debajo del buscador, con animación de entrada (`animate-in zoom-in-95`) -- cada chip saca *solo* ese filtro al tocar la X, sin reabrir el drawer.
- Animaciones de búsqueda: un ícono de carga (`Loader2Icon` girando) reemplaza la lupa mientras hay una consulta en vuelo, y la grilla baja a `opacity-50` con transición suave en vez de qeudarse estática -- sin esto la búsqueda no daba ninguna señal visual de estar procesando, lo que se sentía "roto".
- Probado en vivo: chip de marca aplicado y removido correctamente, alta directa desde la card, selector de variante (Xbox Series: azul/negro/Rojo, cada uno con su stock propio), y un producto real con stock en 0 mostrando "Agotado" con el botón deshabilitado -- confirma que el gate de stock funciona con datos reales, no solo en el caso feliz.

**Cuarta ronda -- checkout rediseñado, catálogo paginado, y perfil del tenant** (continuación tras una interrupción por límite de uso de la sesión, retomada y verificada después): esta ronda avanzó bastante más allá de lo que quedó documentado arriba; lo que sigue es lo que se encontró ya construido al retomar, revisado y verificado antes de seguir.

- **Rediseño del checkout, pedido explícito del usuario**: "cuando dije confirmar orden... el OTP no llevaba a ningún lado -- se verificaba el teléfono y de ahí igual había que tipear nombre/dirección de cero, cliente nuevo o no" (paráfrasis del razonamiento documentado en el propio código). Ahora son 4 pasos: `cart` → `identify` (documento + teléfono, nunca la dirección todavía) → `otp` (código de 6 dígitos, `InputOTP` de shadcn con auto-submit al completarse) → `details` (nombre + direcciones, precargado si ya es cliente). **Decisión de seguridad deliberada**: `verify_checkout_otp` recién revela si ese teléfono ya es cliente del tenant (nombre, documento, direcciones guardadas) *después* de confirmar el código correcto -- nunca antes, para que el checkout no sea un oráculo de "¿este teléfono ya es cliente?" para cualquiera que solo sepa el número.
- **Envío y facturación son direcciones independientes** (pedido explícito: "la facturación es de donde saco los datos para la factura") -- por default van a la misma dirección (`billing_same_as_shipping`, checkbox), pero se puede facturar a una distinta, guardada o nueva. `checkout` (Edge Function) ahora resuelve las dos por separado vía `resolveOrderAddress` (mismo helper para ambas, solo cambian los flags `is_shipping`/`is_billing` de una dirección nueva) y siempre valida que una dirección reusada (`address_id`/`billing_address_id`) sea realmente del cliente que está comprando -- nunca se confía en el id que mande el visitante sin comprobar dueño primero.
- **`clients` gana `document_type`/`document_number`** en el flujo de checkout (columnas que ya existían desde `20260824200633_clients_document_fields.sql`) -- si el cliente ya existía sin documento cargado, se completa con lo que mandó el checkout sin pisar uno que ya tenía; el índice único `clients(tenant_id, document_number)` se traduce a un mensaje claro ("ese documento ya está registrado con otro cliente") en vez de un 500 crudo.
- **Bug real de Meta encontrado en vivo**: las plantillas `AUTHENTICATION` con el botón estándar "Copiar código" son, del lado real de Meta, un botón tipo `URL` con una URL dinámica (`.../otp/code/?...&code={{1}}`), no el `OTP`/`COPY_CODE` que se manda al crearla -- sin un componente `button` aparte (con el mismo código como parámetro) en el envío, Meta rechaza con `(#131008) Required parameter is missing`. Se corrigió `request_checkout_otp` para mandar ese componente cuando la plantilla tiene un botón `URL`, y se actualizó el `buttons` guardado de la plantilla de Tenant QA Uno para reflejar la forma real que devuelve Meta.
- **Catálogo con scroll infinito** (pedido explícito: la carga inicial de hasta 120 productos se sentía pesada) -- `list_products` pasó a paginado (`offset`/`limit`, `has_more` calculado pidiendo `limit + 1` filas y recortando una, sin una consulta de `count` aparte), el frontend acumula con un `IntersectionObserver` (`rootMargin: 400px` para pedir la próxima tanda antes de tocar el fondo real).
- **Cantidades con guardado debounced** (`useDebouncedQuantity`, `lib/useDebouncedQuantity.ts`) en toda card/fila del carrito -- cada click de +/- o tecla tipeada en el input actualiza la UI al instante (sin esperar red) y recién manda UN commit al servidor 500ms después del último cambio, en vez de una llamada por click; un ícono girando en la esquina indica "guardando" sin bloquear más clicks mientras tanto. `add_to_cart` también se optimizó con 2 tandas de `Promise.all` en vez de ~7 round-trips secuenciales -- la causa real de la lentitud percibida al agregar un producto.
- **`refreshCartCount` ya no pide `get_cart` de nuevo** después de cada mutación -- toda acción de carrito (`add`/`update`/`remove_cart_item`) ya devuelve el `items` actualizado en su propia respuesta, así que se le pasa directo en vez de un round-trip extra solo para el badge del header.
- **Perfil del tenant de prueba completado y renombrado** (pedido explícito, "esta pobre" y "ponle un nombre fake"): Tenant QA Uno pasó a llamarse **TecnoNova Colombia** (`legal_name`, NIT, contacto, país/ciudad y dirección de facturación fake pero plausibles, ya que estaban todos vacíos) -- incluye el `storefront_slug`, que se actualizó a `tecno-nova` para quedar consistente con el nuevo nombre (la tienda pública vive ahora en `/tienda/tecno-nova`, no `/tienda/qa-uno-test`).
- **Sección "Tienda pública" en `Settings.tsx` del portal del tenant** (cierra el pendiente #1 de la ronda anterior): vive como una `CardSection` más dentro del mismo `Card` del perfil de empresa (mismo `tenant` ya cargado ahí, sin otra llamada) -- campo de dirección/slug con guardado propio (botón "Guardar", solo si cambió), interruptor "Tienda activa" de aplicación instantánea (deshabilitado sin slug guardado, mismo patrón que `TenantModulesSection`), y un link "Ver tienda" cuando está activa. `updateTenantStorefront`/`STOREFRONT_SLUG_TAKEN_CODE` nuevos en `lib/api/tenants.ts`; el conflicto de slug duplicado (único globalmente) se traduce a un mensaje claro. **No se pudo verificar en el navegador** (sin credenciales de login en este entorno, mismo límite documentado desde el inicio del proyecto) -- sí se verificó por código: la RLS de `tenants` ya permite que un `tenant_admin` actualice su propia fila (`is_superadmin() OR (id = auth_tenant_id() AND is_tenant_admin())`), sin necesidad de ninguna policy nueva.
- Verificado en el Browser pane (sin login, solo la tienda pública): catálogo con scroll infinito, alta al carrito con el stepper apareciendo al instante, carrito reflejando el ítem agregado, y el paso "identify" del checkout (documento + teléfono) renderizando y validando correctamente -- **no se llegó a probar el envío real de un código OTP** (se detiene ahí a propósito para no mandarle una plantilla de WhatsApp real a un número sin permiso explícito del usuario). `npm run build` limpio, `deno check` limpio, Edge Function `storefront` redesplegada con este código.
- **Retorno desde Wompi reconstruye la vista de confirmación** (pieza que había quedado sin verificar tras la interrupción de esta sesión por límite de uso -- confirmado completo al retomar, ya estaba bien construido): `checkout` acepta un `redirect_url` opcional (`_shared/payments/salesOrderPayments.ts::createSalesOrderPaymentLink` lo agregó como 4° parámetro, opcional a propósito -- los otros dos callers que generan link de pago, `generate_payment_link` de la IA y el link manual del portal, no tienen un browser real del comprador al que volver, así que siguen sin mandarlo). El frontend arma `redirect_url` apuntando a `/tienda/:slug/carrito?payment=return` y **no limpia el `session_token` del carrito** cuando el método es Wompi -- tiene que sobrevivir el viaje completo de ida y vuelta a un dominio externo, donde cualquier estado de React se pierde por la recarga completa del browser. Al volver, `StorefrontCart.tsx` detecta `?payment=return`, entra en un estado `checking` y hace polling a `get_order_status` (nuevo, resuelve el carrito por `session_token`, nunca por un id de orden en la URL) cada 2s hasta 4 reintentos, mostrando "confirmado" o resignándose a "seguimos confirmando, te avisamos por WhatsApp" si el webhook de Wompi tarda más que eso. Verificado: `deno check` limpio, `npm run build` limpio, y el contenido de la función `storefront` ya desplegada en Supabase coincide byte a byte con el código local (no hacía falta redeploy).

**Pendiente explícito**:
1. Probar el envío real de un código OTP por WhatsApp a un teléfono real (la plantilla ya está aprobada y el bug del botón `URL` ya se corrigió, falta el primer envío de punta a punta con un número real).
2. Verificar en el navegador la sección "Tienda pública" de `Settings.tsx` en cuanto haya credenciales de login disponibles en el entorno -- por ahora solo revisado por código + RLS.
3. Probar en el navegador el flujo completo de pago con Wompi y el retorno a la tienda (`?payment=return`) contra un pago real o de sandbox -- por ahora solo revisado por código y confirmado desplegado, no ejecutado de punta a punta.
4. Métricas de carrito abandonado (`storefront_carts` con `status='active'` y `created_at` viejo) — la tabla ya lo soporta, falta construir la vista/reporte.

## Estado actual (2026-08-26) — Pagos por WhatsApp (Wompi + crédito) y carrito editable

**Cobro automático al confirmar una venta (Wompi + crédito de clientes)**: hasta esta ronda, `generate_payment_link` (herramienta de IA) existía pero estaba completamente desconectado de `sales_orders` — un pago exitoso de Wompi no tenía dónde quedar registrado. Se construyó el puente completo: tabla `sales_order_payment_links` (liga un checkout de Wompi a un pedido puntual) + columnas nuevas en `sales_order_payments` (`provider_key`, `provider_transaction_id`, `provider_reference`, `payment_link_id`) + `'wompi'` como método válido. `_shared/payments/salesOrderPayments.ts` centraliza `createSalesOrderPaymentLink()`/`chargeSalesOrderToCredit()`/`applySalesOrderPaymentWebhookEvent()` — el monto de cualquier cobro **siempre** se calcula como el saldo pendiente real del pedido (total menos lo ya pagado), nunca un parámetro que el modelo (ni un humano) pueda inventar. `payment-webhook-wompi` ahora también resuelve pagos contra `sales_order_payment_links` cuando `payment_invoices` (facturación de Leadly a sus propios tenants, un concepto distinto) no matchea. Cobro a crédito (`charge_sale_to_credit`, habilidad nueva `credito`) es un simple insert en `sales_order_payments` con `method: 'credito'` — el trigger ya existente `apply_credit_payment_charge()` (`20260822020001_customer_credit.sql`) es quien valida `credit_enabled` y crea la fila de `credit_charges`, no se duplicó esa lógica.

**Rediseño "directo a la venta" (pedido explícito del usuario, 2026-08-25)**: "la IA no genera cotizaciones sino solo ventas... si tiene link de wompi lo genera y lo envía de una vez, a menos que el cliente tenga crédito habilitado da la opción de cargarlo a crédito o genera el link de pago, si no hay wompi quedan los pagos pendientes para que el agente humano lo confirme". Se reescribieron los `prompt_fragment` de `ventas`/`wompi`/`credito` para que la IA nunca muestre un paso intermedio de "¿confirmás la cotización?" una vez que el cliente ya expresó intención de compra — pasa directo por `create_quote`+`confirm_quote` en el mismo turno. **Encontrado en vivo dos veces que la instrucción de prompt sola no alcanzaba** para que el modelo hiciera una segunda acción obligatoria en el mismo turno (mismo patrón de bug que ya se había visto con el envío de fotos de producto, ver `20260825182500_ai_skills_catalog_auto_image.sql`): (1) el modelo confirmaba la venta y respondía al cliente sin generar el link de pago, recién lo generaba un turno después cuando el cliente preguntaba; (2) llamadas duplicadas a `create_quote` para la misma intención dejaron un pedido huérfano (#015) mientras se confirmaba otro (#016). Fix aplicado (no el de la duplicación, ver carrito abajo): la resolución de pago se movió **adentro** de la propia ejecución de `confirm_quote` (`resolvePaymentAfterConfirm` en `whatsapp-ai-tools/index.ts`) — el resultado de la herramienta ya trae `payment_method`/`checkout_url`/`payment_charged`/`payment_options`/`payment_pending`, así que el modelo solo tiene que leer el resultado, no acordarse de llamar una segunda tool salvo cuando genuinamente hay que preguntarle al cliente qué método prefiere (`payment_options`).

**Bug real encontrado y corregido**: `isTenantWompiConnected` (en `whatsapp-ai-respond` y duplicado en `whatsapp-ai-tools`) llamaba a la RPC `payment_credential_configured_secrets`, cuyo cuerpo `SECURITY DEFINER` gatea con `is_superadmin()`/`auth_active_tenant_id()` — ambas resuelven de `auth.uid()`, que es `null` para un caller con service role, así que la RPC devolvía silenciosamente un arreglo vacío siempre, sin importar el tenant. La habilidad `wompi` se estaba descartando en cada turno sin ningún error visible. Fix: leer `payment_credential_secrets` directo con el admin client (que sí puede saltarse RLS como service_role) en vez de la RPC.

**Carrito editable — SUPERADO ese mismo día por la tienda pública por tenant (ver arriba)**: tras el bug de `create_quote` duplicado, se llegó a construir un diseño intermedio (link de pedido editable: tabla `sales_order_checkout_links`, Edge Function pública `public-cart`, tool de IA `send_cart_link`, habilidad `carrito`) antes de que el usuario pidiera algo distinto — un marketplace público, no un link por pedido (ver la sección de arriba). Todo eso se borró sin dejar ninguna fila real en producción usándolo. Se conservó de esa ronda `_shared/orders/confirmSalesOrder.ts` (extraída del `case "confirm_quote"` de `whatsapp-ai-tools`, sigue siendo la lógica compartida de "confirmar una venta" que hoy también usa el checkout de la tienda pública).

**Otros fixes de esta ronda, menores pero reales**: bug de imágenes de producto en formato WebP que WhatsApp no podía entregar (Graph API respondía éxito pero el mensaje nunca llegaba) — `resizeImageForUpload()` ahora genera JPEG; se corrigieron las 8 fotos ya subidas en producción. Bug de mensajes duplicados de IA (`whatsapp-ai-respond` sin try/catch de nivel superior — una excepción después de ya responderle al cliente hacía que `whatsapp-webhook` reintentara y generara una segunda respuesta independiente). Componente `PhoneInput`/`lib/phone.ts` nuevo para separar código de país + número en los formularios que capturan teléfono (`ContactDrawer`, `NewConversationDrawer`, `CampaignFormDrawer`), con `formatPhoneDisplay` aplicado en toda vista que mostraba un teléfono crudo — verificado solo con build limpio, no en navegador (sin credenciales de login en este entorno). Se quitó `'otro'` como método de pago seleccionable (tanto en `sales_order_payments` como `credit_payments`, cero filas existentes lo usaban).

## Estado actual (2026-08-15) — Pivote a ERP

**Decisión de producto explícita del usuario**: Leadly deja de ser "solo" un CRM conversacional y pasa a construirse como un ERP completo para que el tenant gestione toda su empresa (clientes, oportunidades, productos, inventario, despachos, facturación, cartera/crédito), sin perder nada de lo que ya funciona hoy en producción (contactos, pipeline, catálogo, pedidos/cotizaciones — todo lo documentado en 3.4/3.6/3.7 abajo, que sigue vigente y **no se toca**).

**Reglas del pivote (no re-litigar, decisiones ya cerradas)**:
1. ~~Ninguna tabla `crm_*` existente se renombra~~ — **reabierta y revertida el 2026-08-15** (mismo día, sesión posterior). Ver "Migración de nomenclatura `crm_*` → esquema ERP sin prefijo" más abajo para la decisión vigente.
2. **Las tablas nuevas del ERP van sin prefijo** (`warehouses`, `stock_movements`, `dispatches`, `invoices`, `customer_credit_accounts`, etc.) — ya no tiene sentido seguir prefijando con `crm_` algo que ya no es solo CRM.
3. **Rama `develop`** (creada 2026-08-15, apunta a `main`) es la base de todo este trabajo; cada módulo del ERP se construye en una rama `feature/erp-<modulo>` propia desde `develop`, con PR de vuelta a `develop`. `develop` se mergea a `main` módulo por módulo, solo después de validarlo — no hay una rama/proyecto de Supabase separado para esto (un solo proyecto real `leadly-portal`), así que las migraciones nuevas se aplican directo ahí; es seguro porque son 100% aditivas y cada módulo queda invisible por tenant vía `tenant_enabled_modules` hasta que el superadmin lo habilite manualmente (mismo mecanismo de feature-gating que ya existe, ver la ronda del 2026-08-11 más abajo).
4. **Orden de construcción**: Inventario (bodegas + kardex) → Despachos → Facturas (al cliente del tenant, **no confundir con `payment_invoices`**, que es Leadly facturándole al tenant su suscripción) → Cartera/crédito de clientes. Cada capa depende de datos que produce la anterior.
5. Exponer estos módulos nuevos al tool-calling de IA (Aurora) es una fase deliberadamente posterior (Fase 5), una vez cada módulo esté validado en la UI — mismo patrón ya usado (`ai_skills`/`ai_assistant_skills`/`_shared/aiTools.ts`/`whatsapp-ai-tools`), no se mezcla con la construcción inicial para no multiplicar el alcance de cada PR.

**Migración de nomenclatura `crm_*` → esquema ERP sin prefijo (2026-08-15, rama `feature/erp-core-schema` desde `develop`)**: el usuario pidió reabrir la regla 1 — no alcanza con que lo *nuevo* del ERP vaya sin prefijo, quiere dejar de usar `crm_*` del todo, incluido lo que ya existe (contactos, productos, oportunidades, tareas, ventas, PQR). Se modeló un **esquema paralelo completo sin prefijo**, 1:1 con las 19 tablas `crm_*` vivas (columna por columna, verificado contra todos los `ALTER TABLE` posteriores a cada `CREATE TABLE`, no solo la migración original): `contacts`/`notes`/`contact_addresses` (`20260815010001`), `products`/`product_categories`/`suppliers`/`product_images` (`20260815010002`), `pipelines`/`pipeline_stages`/`opportunities`/`opportunity_stage_history` (`20260815010003`), `tasks` (`20260815010004`), `sales_orders`/`sales_order_items`/`sales_order_payments`/`sales_order_comments` (`20260815010005`), `pqrs`/`pqr_updates`/`attachments` (`20260815010006`). Ya aplicado al proyecto real (`leadly-portal`), sin advertencias en `get_advisors`.

Reglas de esta migración (distintas de la Fase 1 de Inventario, que sí referenciaba `crm_products` por FK):
- **Cero `ALTER`/`DROP` sobre ninguna tabla `crm_*`** — siguen intactas, el frontend y las Edge Functions (`whatsapp-ai-tools`, `Contactos.tsx`, `Productos.tsx`, etc.) las siguen usando exactamente igual, nadie nota el cambio todavía.
- **Sin migración de datos** — las tablas nuevas nacen vacías. Copiar filas es una decisión de cutover explícita aparte, no asumida acá.
- **Esquema autocontenido** — las tablas nuevas se referencian solo entre sí, nunca a una `crm_*`. Consecuencia conocida: `product_stock`/`stock_movements` (Fase 1 Inventario, ya en prod) siguen apuntando a `crm_products`, no a `products` — van a quedar desalineadas hasta un cutover futuro que las repunte.
- **Una sola desviación deliberada del 1:1**: `products` no tiene `stock_quantity`/`reserved_stock` (el contador plano que sí tiene `crm_products`). Como se está rediseñando de cero, se aprovechó para no recrear la doble fuente de verdad que Fase 1 ya dejó pendiente — el stock de `products` vive únicamente en `product_stock`. `sales_order_items` sí lleva `warehouse_id` (nullable) para cuando exista ese trigger de efecto de stock — no se construyó todavía porque depende de cómo Fase 2 (Despachos) defina reservas por bodega, y era mejor no inventar esa semántica ahora.
- `sales_orders` (no `orders` a secas — el ERP va a tener despachos/compras a proveedor más adelante, y "orders" sin calificar es ambiguo entre venta y compra).
- Funciones/triggers con nombre igual al original (`seed_default_pipeline`, `log_opportunity_initial_stage`, `log_opportunity_stage_change`) se portaron con sufijo `_core` (`seed_default_pipeline_core`, etc.) — redefinirlas con `create or replace` habría sobrescrito las que ya usan los triggers de `crm_opportunities`/`tenants` en producción. `seed_default_pipeline_core()` corre en paralelo a `seed_default_pipeline()` (ambos disparan `after insert on tenants`), así que todo tenant nuevo arranca con pipeline poblado en las dos tablas mientras dure la transición.
- **Pendiente explícito, no bloquea nada**: el cutover en sí (frontend/Edge Functions apuntando a las tablas nuevas), la migración de datos existentes, el efecto de stock de `sales_order_items` contra `product_stock`, y la decisión de cuándo (o si) se borran las `crm_*` — todo eso es trabajo futuro después de probar este modelo, no asumido por esta migración.

**Fase 1 (Inventario multi-bodega + kardex) — en construcción, rama `feature/erp-inventory`**: hoy el único modelo de stock es `crm_products.stock_quantity`/`reserved_stock` (un número plano por producto, sin bodegas), mantenido por los triggers existentes sobre `crm_order_items`/`crm_orders` (`adjust_product_stock()` y compañía) que el flujo de ventas por WhatsApp sigue usando tal cual. Tablas nuevas: `warehouses` (una "Bodega Principal" por tenant vía trigger `seed_default_warehouse()`, mismo patrón que `seed_default_pipeline()`), `product_stock` (stock por producto+bodega) y `stock_movements` (kardex append-only, trigger `apply_stock_movement()` mantiene `product_stock` al día). Backfill: una entrada `carga_inicial` por producto con stock actual, sin tocar `crm_products`. La reconciliación futura (si `product_stock` pasa a ser la fuente de verdad única) es una decisión explícita pendiente, no asumida.

**Nota de desfase**: el bloque "Estado actual (2026-08-09)" de abajo también quedó desactualizado — entre esa fecha y este pivote se construyó un módulo de Productos/Ventas completo (`crm_products`, `crm_product_images`, `crm_product_categories`, `crm_suppliers`, `crm_orders`, `crm_order_items`, `crm_order_payments`, `crm_order_comments`, `crm_contact_addresses`; páginas `Productos.tsx`/`ProductoDetalle.tsx`/`Ventas.tsx`/`VentaDetalle.tsx`) y el módulo de Integraciones (Wompi/HubSpot/Shopify/LaFactura). Ninguno de los dos está descrito en las secciones numeradas de abajo todavía — tratar el código como la fuente de verdad hasta que se documenten a fondo.

## Estado actual (2026-08-09)

**Ronda del 2026-08-11 (módulos por tenant)**: nuevo sistema de feature-gating a nivel de tenant, pedido explícito del superadmin — hasta ahora el nav del panel de cliente (`TenantLayout.tsx`) era el mismo catálogo fijo de 14 ítems para todos los tenants, sin forma de restringir qué ve cada uno. `tenant_enabled_modules` (`20260811000001_tenant_modules.sql`) es una tabla puente presence-based (sin catálogo aparte, mismo shape que `ai_assistant_skills`: una fila = módulo habilitado, sin fila = deshabilitado; RLS de escritura solo superadmin, lectura propia + superadmin) contra el catálogo fijo de módulos que ahora vive en `leadly-app/src/lib/modules.ts` (`TENANT_MODULES`, única fuente de verdad para label/ícono/ruta, usada tanto por el nav como por el guard como por la tab nueva). **Los 14 ítems del nav son togglables, incluidos Dashboard/Conversaciones/Configuración** — decisión explícita del superadmin de no dejar ningún módulo protegido de fábrica. El gating es real en dos capas: `TenantLayout.tsx` filtra el nav por `AuthContext.enabledModules` (cargado junto con el perfil/tenant), y cada ruta de `/app` en `App.tsx` está envuelta en el guard nuevo `RequireModule` (`routes/guards.tsx`, mismo patrón "lock en el lugar, no redirect" que `RequireRole`) para que un link directo a una ruta deshabilitada tampoco funcione. Backoffice: tab nueva "Módulos" en `ClienteDetalle.tsx` (`TenantModulesSection.tsx`) con un switch por módulo, aplica al instante (sin botón "Guardar"), mismo patrón que la sección "Habilidades" de IA. **Backfill: al ser presence-based, la tabla arrancó vacía** — decisión explícita del superadmin de que todos los tenants existentes (ya en producción real) quedaran sin ningún módulo habilitado tras aplicar la migración, para reactivarlos él mismo manualmente desde la tab nueva en vez de auto-habilitar todo.

**Nota de desfase**: el bloque "Estado actual (2026-08-06)" de abajo (y la sección 3.6) quedó desactualizado por dos días de trabajo no documentado antes de esta sesión — el modelo B2B (`crm_accounts`/`Empresas.tsx`/`EmpresaDetalle.tsx`) se **revirtió el 2026-08-08** (fusionado de vuelta en `crm_contacts`, ver `leadly-db/supabase/migrations/20260808172403_merge_contacts_accounts.sql`) y se construyó un pipeline completo de catálogo → cotizaciones → ventas (`crm_products`, `crm_orders`/`crm_order_items`, habilidades de IA `catalogo`/`ventas`) entre el 08-08 y el 08-09, ya en producción y afinado con pruebas reales por WhatsApp. Tratar ese bloque como historial, no como estado vigente, hasta que se reescriba a fondo.

**Ronda del 2026-08-09** (seguimiento inteligente de oportunidades + direcciones + ficha de cliente completa):
- **`crm_orders` confirmada → oportunidad "Ganado" centralizado en un trigger de DB** (`20260809170000_crm_orders_confirmed_moves_opportunity.sql`): antes solo pasaba si la IA confirmaba la venta (`confirm_quote` en `whatsapp-ai-tools`) — un agente humano confirmando desde `Ventas.tsx` (`updateOrderStatus`) dejaba la oportunidad desactualizada. Ahora es un trigger `after update of status on crm_orders` que cubre ambos caminos por igual; la función Deno equivalente (`moveLinkedOpportunityToWon`) se borró.
- **Calificación de interés en tiempo real + tarea de seguimiento automática** (`20260809170100_ai_skill_oportunidades_qualified_followup.sql`, nueva tool `flag_interest_for_followup`): pedido explícito del usuario — la IA no puede ofrecer/crear una cotización ante interés ambiguo. Tres niveles: (1) interés ambiguo → sigue creando la oportunidad (barra baja sin cambios) pero en vez de cotizar llama `flag_interest_for_followup` (crea una `crm_tasks` asignada al agente del contacto si tiene uno) y le avisa al cliente que un agente lo va a contactar; (2) intención clara pero falta info → pregunta lo que falta; (3) intención clara y completa → pregunta explícitamente si quiere comprar o prefiere una cotización. `create_quote` ahora exige confirmación explícita del cliente antes de llamarse (`20260809170200_ai_skill_ventas_confirmation_and_notes.sql`) — revierte puntualmente la instrucción "avanzá vos misma sin esperar confirmación" que tenían las rondas anteriores de `ventas`, solo para este paso.
- **Observaciones bidireccionales en la cotización**: `get_quote_status` ahora devuelve `notes` (la IA se las comunica al cliente cuando son relevantes); nueva tool `add_quote_note` para que la IA registre una observación que el cliente da en la conversación, sumándola sin pisar lo que un agente ya escribió.
- **Módulo de direcciones** (`20260809170300_crm_contact_addresses.sql`): tabla nueva `crm_contact_addresses` — libreta por contacto, reutilizable entre cotizaciones (`is_shipping`/`is_billing` en la misma fila, no un enum rígido; `is_default` para no repetir la dirección en cada venta). `crm_orders` ganó `shipping_address_id`/`billing_address_id`. Tools nuevas `list_contact_addresses`/`save_contact_address` bajo la habilidad `ventas` (no una habilidad aparte — solo importan junto al flujo de venta). Prompt (`20260809170400_ai_skill_ventas_addresses.sql`): después de `confirm_quote`, si el pedido implica envío físico, la IA consulta las direcciones guardadas y confirma con el cliente en vez de volver a pedirlas si ya hay una default.
- **`ContactoDetalle.tsx` enriquecido**: pasó de 3 tabs (Actividad/PQR/Conversaciones) a 7 — se agregaron **Resumen** (cards de oportunidades abiertas, ventas confirmadas, cotizaciones pendientes, tareas pendientes, próxima cita; nuevo tab por defecto), **Oportunidades** (abre el `OpportunityPanel` existente), **Ventas** (abre `OrderDrawer`, que ganó un prop `defaultContactId`) y **Direcciones** (CRUD vía `AddressDrawer` nuevo). Esto es lo que reemplaza al viejo `EmpresaDetalle.tsx` (ver nota de desfase arriba) — decisión explícita del usuario de enriquecer Contactos en vez de revivir el modelo B2B. `OrderDrawer` también ganó selects de dirección de envío/facturación y una sección de solo lectura de tareas relacionadas (vía la oportunidad vinculada); `OpportunityPanel` ganó una sección de cotizaciones/ventas vinculadas dentro de su tab Resumen. Todas las listas nuevas están paginadas (regla del proyecto, ver memoria de la sesión).
- **Pendiente**: todo lo anterior compila y pasó `npm run build`, pero el comportamiento conversacional de la IA (los 3 niveles de calificación, el gate de confirmación, el flujo de direcciones) todavía necesita una ronda de prueba real por WhatsApp — mismo patrón que toda la iteración de `ventas`/`oportunidades` de los días previos (ver migraciones `20260809*` de esa ronda). No asumir que el prompt quedó perfecto al primer intento.

## Estado actual (2026-08-06)

**Addenda desde el 2026-08-04** (el bloque original de abajo sigue siendo válido, esto es lo que cambió después):
- **Despliegue de `leadly-app` pendiente de definir** (2026-09-06: se dejó de usar el hosting anterior — sin reemplazo elegido todavía). Supabase Auth (Site URL + Redirect URLs) y el SDK de Facebook (dominios permitidos) van a necesitar apuntar al dominio final una vez se decida dónde se aloja.
- **Meta App Review en curso**: formulario de `whatsapp_business_management`/`whatsapp_business_messaging` completado (procesadores de datos: Supabase/OpenAI/Gemini, todos en EE.UU.; responsable: Lexy Colombia SAS), llamadas de prueba a la Graph API ya ejecutadas. Embedded Signup: plantilla correcta identificada ("token que caduca en 60 días", acceso completo — no la de "solo lectura"), `config_id` obtenido (`992764367102956`) y ya en `.env`. Migrar un número real de la app de WhatsApp Business al Cloud API requiere el flujo de "Crear cuenta de WhatsApp Business" del Embedded Signup con el mismo número (dispara OTP de migración automáticamente) — no hace falta una migración manual aparte.
- **Habilidades de IA (`ai_skills`/`ai_assistant_skills`, 2026-08-06)**: el tool-calling de PQR (antes: mismo set de tools mandado siempre a todos los asistentes, con las instrucciones de uso escritas a mano en cada `system_prompt`) ahora es la primera "habilidad" activable por asistente desde el backoffice. Ver sección 3.5.
- **CRM ampliado (`crm_accounts`, `crm_pipelines`/`crm_pipeline_stages`/`crm_opportunities`, `crm_tasks`, 2026-08-06)**: capas nuevas sobre `crm_contacts` — empresas B2B, oportunidades con pipeline/etapa/valor (reemplaza el `stage` fijo a nivel de contacto para seguimiento de negociaciones reales), y tareas. Backend + UI completos: pantallas **Empresas** (`/app/empresas`), **Oportunidades** (`/app/oportunidades`, lista con cambio de etapa inline, sin kanban todavía) y **Tareas** (`/app/tareas`, con toggle de completada) en el nav del tenant. Decisión explícita: no se migró a un modelo genérico multicanal/multi-pipeline — ver sección 3.6 para el razonamiento.
- **Limpieza de esquema**: `whatsapp_messages.error_message` eliminado (columna muerta desde el fix de "mensajes fantasma" del 2026-08-05 — un mensaje solo se inserta si Meta confirmó la entrega, así que nunca más se escribía ahí).
- **Switches (`components/ui/Switch.tsx`) tenían un bug visual** (el knob se salía del track) que afectaba a toda la app — corregido con un `left-0` explícito.
- **`EmpresaDetalle.tsx` (ficha 360 de Empresa, 2026-08-06)**: reemplazó un primer intento de UI fea/desconectada por un diseño validado por el usuario (header con avatar+badge, tabs con contador, cards de resumen con datos reales). Ver 3.6.
- **🚧 Fase A en curso (empezada el 2026-08-06, no terminada)**: Kanban de Oportunidades pipeline-céntrico — reemplaza la lista simple de `/app/oportunidades` por un tablero drag-and-drop, agrega historial de cambios de etapa (para poder calcular conversión/tiempo promedio real), panel lateral de oportunidad, y saca "Tareas" del nav a favor de un widget en el Dashboard. **Spec completa + checklist de progreso en la sección 3.7 — si estás retomando este proyecto en una conversación nueva, empezá ahí.**
- **Pasada de restyling visual sobre una referencia de diseño (2026-08-06)**: el usuario trajo una captura de referencia del look que quiere para el Kanban de Oportunidades y el sidebar. Se ajustó `Oportunidades.tsx` (barra de métricas como un solo contenedor dividido en vez de tiles sueltos, cards del Kanban con avatar de iniciales + fila de iconos teléfono/correo/calendario + fecha, botón "+ Nueva oportunidad") y `OpportunityPanel.tsx` (chip de empresa + badge de estado sobre las tabs, footer fijo con Editar/Eliminar, barra de progreso en la tab Tareas) — **se mantuvieron exactamente las 3 tabs ya decididas en 3.7** (Resumen/Conversaciones/Tareas), no se agregaron Archivos/Actividad/IA aunque la referencia las mostraba, sigue fuera de alcance. También se alineó el sidebar del tenant a la referencia: "Clientes" pasó a llamarse **"Contactos"** en el nav (mismo componente/ruta `/app/clientes`, solo cambió la etiqueta), se agregó la card de upsell "Prueba Leadly Pro" (`AppShell` ganó un prop `showUpsell`, solo lo usa `TenantLayout`) y se sumaron 4 ítems de nav nuevos como placeholders bloqueados (mismo patrón `LockedFeature` que Campañas/Catálogo): **Calendario**, **Reportes**, **Automatizaciones**, **IA & Agentes** — decisión explícita del usuario de sumarlos ya al nav aunque el motor de automatizaciones y los reportes como módulo propio siguen fuera de alcance de ingeniería real (ver 3.6), es solo para que el nav se vea completo. Pendiente: el usuario todavía tiene que verificarlo en el navegador (sin acceso a navegador en este entorno).
- **Adjuntos en Tareas + tareas visibles en el Kanban (2026-08-06, migración `20260806000012`)**: `crm_attachments` era exclusivo de PQR/seguimientos (solo imágenes, ver 3.4) — se agregó `task_id` (nullable, exactamente uno de `pqr_id`/`pqr_update_id`/`task_id`) para poder adjuntar un archivo a una tarea, ej. "se envió una propuesta". A diferencia de los adjuntos de PQR, acá también se permite `application/pdf` (una propuesta casi seguro es un PDF, no una foto) — el bucket `crm-attachments` amplió su `allowed_mime_types` acorde. Sección nueva "Archivos adjuntos" en `TaskDrawer.tsx`, visible solo al editar una tarea ya existente (necesita el `id` real de la tarea). Además, `Oportunidades.tsx` ahora trae el arreglo completo de tareas del tenant (antes solo el conteo para el tile del header) y muestra una etiqueta con ícono + cantidad de tareas pendientes directamente en cada card del Kanban que tenga alguna — para verla sin tener que abrir el panel lateral (que ya tenía su tab "Tareas" desde la Fase A).
- **`ai_assistants` desacoplado de `whatsapp_lines` (2026-08-06, migración `20260806000011`)**: hasta ahora un agente de IA pertenecía 1:1 a una línea (`ai_assistants.whatsapp_line_id not null unique`) — un tenant con 3 líneas queriendo "el mismo agente de ventas" en las 3 tenía que configurar (y mantener sincronizados a mano) 3 prompts idénticos. Pedido explícito del usuario: "a una línea le puedo asignar un agente, pero este mismo agente lo puedo configurar en varias líneas" — M:1 (varias líneas → un agente), no M:N (una línea nunca tiene más de un agente respondiendo a la vez). Se invirtió la FK: ahora `whatsapp_lines.ai_assistant_id` (nullable) apunta al agente asignado, en vez de que el agente apunte a su única línea. `ai_assistants` ganó `tenant_id` propio (antes derivaba el tenant únicamente a través de la línea) y `name` (ya no tiene sentido identificarlo por "la línea tal", es una persona reutilizable). RLS de `ai_assistants` reescrita para usar `tenant_id` directo, y de paso se abrió `insert`/`delete` al propio tenant (antes solo superadmin, un resabio de cuando crear un agente era un efecto secundario de que superadmin creara una línea) — mismo patrón que `crm_accounts`/`crm_opportunities`. `ai_assistant_skills` no necesitó cambios de esquema (ya estaba modelada correctamente por `ai_assistant_id`, nunca por línea), solo se simplificó su policy de select. Migración 100% aditiva/backfill: cada agente existente sobrevive con su `tenant_id`/`name` derivados de su línea de origen antes de borrar la columna vieja. **Los 3 edge functions que resolvían el agente vía `whatsapp_line_id` (`whatsapp-ai-respond`, `whatsapp-ai-tools`, `whatsapp-embedded-signup`) se actualizaron y redesplegaron en el mismo momento que la migración** — necesario, si no la IA se hubiera quedado sin poder resolver ningún agente apenas se aplicara el `alter table drop column`. `createWhatsappLine` (frontend) sigue auto-provisionando un agente privado al crear una línea nueva (no cambia esa UX), solo que ahora lo enlaza escribiendo `whatsapp_lines.ai_assistant_id` en vez de la relación inversa. UI: `IaAgentes.tsx` (ver debajo) pasó de una tarjeta por línea con "Configurar" inline a dos secciones separadas -- catálogo de Agentes (CRUD real) y Líneas con un selector para asignar/reasignar el agente de cada una.
- **"Asistentes de IA" se movió de Configuración a "IA & Agentes" (2026-08-06)**: esa sección (gestión de líneas de WhatsApp + su asistente, incluyendo "Conectar tu WhatsApp") vivía dentro de `Configuracion.tsx` (tenant) mezclada con el logo y las tags de conversación, aunque el nav ya tenía un ítem "IA & Agentes" separado (agregado el mismo día como placeholder bloqueado). Se sacó entera a `pages/tenant/IaAgentes.tsx`, que ahora es el contenido real de esa ruta (`/app/ia-agentes`, dejó de ser `LockedFeature`). `Configuracion.tsx` (tenant) quedó solo con Logo + Tags de conversación.
- **Dashboard real, backoffice y tenant (2026-08-06)**: reemplaza el placeholder `ComingSoon` del backoffice y agrega un Dashboard nuevo para el tenant (no existía ninguna pantalla de resumen ahí antes, el índice de `/app` siempre fue directamente el Inbox). Nuevo `lib/api/dashboard.ts` con `getBackofficeDashboardStats`/`getTenantDashboardStats` (conteos vía `count: 'exact', head: true`, sin traer filas). Backoffice (`pages/backoffice/Dashboard.tsx`, en la ruta índice `/backoffice`, el nav ya tenía el ítem "Dashboard" apuntando ahí): clientes activos/total, líneas de WhatsApp activas/total, conversaciones con actividad hoy, + lista de clientes recientes. Tenant (`pages/tenant/Dashboard.tsx`, ruta nueva `/app/dashboard`, nav ganó un ítem "Dashboard" primero en la lista -- **decisión deliberada de no tocar el índice de `/app`**, que sigue siendo el Inbox, para no cambiar el landing por defecto sin que el usuario lo pida explícitamente): conversaciones abiertas, "esperando respuesta" (abiertas en modo humano -- no hay una columna de "no leído" real, es la mejor aproximación honesta sin una query pesada por conversación), valor del pipeline abierto, tareas pendientes, + widgets de "Tareas próximas" y "Conversaciones recientes". Pendiente/spike abierto: mover "Tareas" del nav a un widget de este Dashboard sigue sin hacerse (ver 3.7) -- ahora que el Dashboard ya existe, es la próxima decisión a tomar.
- **Colores por etapa del pipeline + demo de tareas del QA tenant (2026-08-06, migración `20260806000010`)**: `crm_pipeline_stages.color` vivía con el mismo gris por defecto para las 6 etapas de todo tenant — ahora cada etapa tiene un color distinto (Nuevo violeta, Contactado azul, Propuesta ámbar, Negociación naranja, Ganado verde, Perdido rojo), aplicado tanto a los pipelines existentes (UPDATE) como a los de tenants futuros (`seed_default_pipeline()` reescrita con los mismos colores). Además, los 6 casos demo de `20260806000009` (Ana Gómez, Carlos Ruiz, Marcela Torres, Andrés Peña, Laura Méndez, Jorge Salas) tenían cero `crm_tasks` y todos compartían la misma fecha de cierre (+15 días) y la misma descripción genérica sin importar la etapa — se agregaron 14 tareas realistas (2 por caso + 2 generales, mezclando pendiente/en_proceso/completada) y se corrigieron fecha de cierre/descripción de cada oportunidad para que tengan sentido con su etapa real (los casos Ganado/Perdido ahora cierran en el pasado, no en el futuro). **Nota de proceso**: se había arrancado por error a construir un tema de "restaurante y casa de banquetes" con empresas/líneas de WhatsApp nuevas para este mismo pedido — el usuario corrigió que no era esa la intención antes de escribir nada en la base, el alcance real era mucho más acotado (tareas sobre lo que ya existía + coherencia).

---

## Estado actual (2026-08-03, actualizado en la sesión de implementación — demo el 2026-08-04)

**Fase 0, Fase 1, Fase 2 (backend + Inbox) y un CRM de contactos completo ya están construidos y probados end-to-end**. Proyecto de Supabase real: `leadly-portal` (ref `kkdtrkfcnyvuefazndnj`, org "Lexy"). Si retomas desde la terminal, esto es lo que ya existe:

- `leadly-db/supabase/migrations/` — 17 migraciones aplicadas (`supabase db push` desde `leadly-db/`): esquema completo de `tenants`/`profiles`/RLS, catálogo `ai_models`, `whatsapp_lines`/`ai_assistants`/`whatsapp_conversations`/`whatsapp_messages` con RLS y hardening de privilegios, `self_register_tenant` (auto-registro), enforcement de desactivación de tenant (`auth_active_tenant_id()`), Vault para el access token de Meta (escritura: `set_whatsapp_line_access_token`; lectura: `get_whatsapp_line_access_token`), campos extendidos de tenant + bucket `tenant-logos`, `whatsapp_conversations`/`whatsapp_messages` en la publicación `supabase_realtime`, y **`crm_contacts`/`crm_notes`** (ver 3.4) con `whatsapp_conversations.contact_id` y una policy de insert para que un agente pueda iniciar una conversación desde el panel (antes solo el webhook, vía service_role, podía crear conversaciones).
- `leadly-db/supabase/functions/` — 5 Edge Functions desplegadas (`supabase functions deploy <nombre> --use-api`, sin Docker): `self-delete-account`, `admin-create-tenant-user`, `whatsapp-webhook` (público, `--no-verify-jwt` — **ojo:** cada redeploy sin ese flag reactiva la verificación de JWT del gateway y rompe el webhook con 401, pasó una vez en esta sesión), `whatsapp-ai-respond` (interno, solo invocable con el service role key), `whatsapp-send-human` (JWT del propio agente/tenant_admin). `whatsapp-webhook` ahora también auto-crea/vincula un `crm_contacts` por teléfono en cada mensaje entrante (select-then-insert, no upsert, para no pisar el nombre si el tenant ya lo editó). Lógica compartida de Meta en `functions/_shared/whatsapp.ts`.
- `leadly-app/` — corre con `npm run dev` (puerto 5173). Auth completa, backoffice con CRUD de Clientes (tenants) + Líneas de WhatsApp anidadas + Asistente de IA + Usuarios, Changelog estático. Panel del tenant (`/app`) ahora es un **CRM real**, no solo un inbox:
  - **Conversaciones** (`/app`, `pages/tenant/Inbox.tsx`): lista + chat con burbujas (contacto/IA/agente), toggle IA/humano en tiempo real, envío manual, botón **"Nueva conversación"** (elige un cliente existente o crea uno nuevo + línea, `pages/tenant/inbox/NewConversationDrawer.tsx`), mobile-first.
  - **Clientes** (`/app/clientes`, `pages/tenant/Contactos.tsx` + `ContactoDetalle.tsx`): listado de contactos del CRM del tenant (buscador, etapa, etiquetas) con drawer crear/editar (`pages/tenant/contacts/ContactDrawer.tsx`), y detalle con info + **notas manuales** (`crm_notes`) + **historial de conversaciones de WhatsApp vinculadas** (deep-link a `/app?c=<id>` que auto-selecciona la conversación en el Inbox).
  - **Campañas** y **Catálogo** (`/app/campanas`, `/app/catalogo`): visibles en el nav con tag "Beta" y una pantalla bloqueada (`pages/tenant/LockedFeature.tsx`) — sin funcionalidad real a propósito, quedan para una fase futura (pausas publicitarias / catálogo de productos de WhatsApp).
  - **Categoría de conversación**: select en el header del chat (venta/soporte/consulta/reclamo/otro, `whatsapp_conversations.category`), visible como badge en la lista de conversaciones.
  - **Mi cuenta** (`/backoffice/perfil` y `/app/perfil`, `pages/shared/MiCuenta.tsx`, enlazado desde el nombre al pie del sidebar en `AppShell`): cualquier usuario logueado edita su nombre/teléfono y cambia su contraseña. No existía ninguna vista de perfil antes del 2026-08-04.
  - **Configuración de IA** (`/backoffice/configuracion`, superadmin únicamente, `pages/backoffice/Configuracion.tsx`): setea las API keys de OpenAI/Gemini **compartidas por toda la plataforma** (no por tenant, ver 1 "API keys de IA") sin usar la terminal. Antes del 2026-08-04 esas keys solo existían como `Deno.env` secrets de Edge Function (`supabase secrets set`) — ahora viven cifradas en Vault (`platform_ai_keys`, mismo patrón write-only que el token de Meta) y `whatsapp-ai-respond` las lee vía `get_platform_ai_key` (RPC solo para `service_role`) en vez de `Deno.env.get`.
  - **Conectar tu WhatsApp (Embedded Signup)** (`/app/asistente`, botón visible solo para `tenant_admin`): permite que un tenant vincule **su propia** cuenta de WhatsApp Business (WABA) en vez de que el superadmin le asigne manualmente una línea del Business Manager de Leadly — decisión de negocio del 2026-08-04: los costos de Meta (plantillas, conversaciones) quedan a nombre del tenant, no de Leadly. Piezas nuevas: `leadly-app/src/lib/metaEmbeddedSignup.ts` (carga el SDK de Facebook, `FB.login` con `config_id`, captura `code`+`waba_id`+`phone_number_id` vía `postMessage`), Edge Function `whatsapp-embedded-signup` (intercambia el `code` por access token con el App Secret — nunca en el browser —, crea la línea + asistente por defecto, guarda el token en Vault, suscribe la app a los webhooks de esa WABA), y `set_whatsapp_line_access_token` ahora acepta `service_role` además de superadmin (antes solo el backoffice podía guardar un token). **Bloqueado hasta que Meta apruebe el "Tech Provider App Review"** (estaba "En revisión" el 2026-08-04) y el usuario cree la "Embedded Signup Configuration" en el dashboard de Meta y me pase el `config_id` (`VITE_META_WHATSAPP_CONFIG_ID`, hoy vacío en `.env`) — el botón ya está construido y probado hasta ese punto (muestra el error claro de qué falta), pero no se puede completar una conexión real todavía. `META_APP_ID` (`2438721193212979`) ya está en `.env`/`VITE_META_APP_ID` y como secreto `META_APP_ID` de Supabase.
- Sistema de diseño propio en `leadly-app/src/components/ui/` — **pendiente**: rediseño visual (referencia nunca compartida) queda diferido al final, decisión explícita del usuario.
- **Pitfalls descubiertos en esta sesión** (ver también sección 6): (1) Edge Functions matan cualquier `fetch()` fire-and-forget no esperado al retornar la `Response` — usar siempre `await`. (2) `SUPABASE_SERVICE_ROLE_KEY` puede venir en formato JWT clásico o `sb_secret_...` — comparar por string, no decodificar como JWT. (3) Redesplegar `whatsapp-webhook` sin `--no-verify-jwt` explícito en cada `deploy` reactiva la verificación de JWT del gateway (no es "sticky" desde el primer deploy).
- **Falta explícitamente**: Dashboard real (backoffice y cliente, hoy placeholder), Changelog como tabla editable, rediseño visual pendiente, pipeline visual tipo kanban para las etapas de `crm_contacts` (hoy solo texto/badge en la tabla y el detalle), y las dependencias externas del usuario para producción real: keys de OpenAI/Gemini y una línea de WhatsApp Business verificada por Meta (todo probado con payloads simulados firmados y credenciales placeholder — falla con gracia como se espera).

---

## 1. Decisiones de producto (cerradas)

| Tema | Decisión | Notas |
|---|---|---|
| Nombre | **Leadly** (código/repo) | El branding de cara al usuario pasó a ser **Lexy** desde el 2026-09-02 (logo real de marca en `Logo.tsx`, copy de marketing reposicionado como "plataforma de operaciones", no "CRM") — nombre del repo/proyecto sin cambios, es un rebrand de UI, no un rename de código. |
| Organización de Supabase | **Lexy** | |
| Proyecto de Supabase | `leadly-portal` (ref `kkdtrkfcnyvuefazndnj`), nuevo y dedicado | |
| Integración de WhatsApp | **Meta Cloud API directo** | `graph.facebook.com`, `phone_number_id`, `business_account_id`, webhook propio con `X-Hub-Signature-256` + `verify_token`. No hay agregador (Twilio/360dialog). Implica que cada tenant debe pasar por verificación de negocio de Meta — se documenta como requisito de onboarding, no como bloqueante técnico. |
| Quién paga los costos de Meta (plantillas/conversaciones) | **Cada tenant paga directo a Meta con su propia WABA**, no Leadly (decisión del 2026-08-04, revierte el modelo inicial "una sola WABA de Leadly con clientes como líneas") | Requiere que cada tenant conecte **su propia** cuenta de WhatsApp Business vía Embedded Signup (ver "Conectar tu WhatsApp" en Estado actual) — Meta factura al Business Manager dueño de la WABA, no hay forma de fraccionar el cobro dentro de una WABA compartida. El modelo alternativo más simple (Leadly paga y lo traslada dentro de su propio plan/suscripción) queda descartado por ahora pero documentado como Camino A si se necesita algo más rápido en el futuro. |
| API keys de IA | **Leadly usa keys propias compartidas** (OpenAI y Gemini) | Costeado por Leadly, facturado al tenant por plan (límite de mensajes/tokens). Se deja la puerta abierta a que un tenant traiga su propia key en el futuro (columna nullable, no v1). |
| Proveedor de IA | **Multi-proveedor desde el día 1**: OpenAI (ChatGPT) o Google (Gemini), seleccionable por asistente | Ver sección 3.3 — esto descarta usar el objeto "Assistant" del dashboard de OpenAI como fuente de verdad (Gemini no tiene equivalente), así que la config vive en nuestra propia tabla y las llamadas a los LLM son *stateless* (chat/responses por turno), no threads persistentes del lado del proveedor. |
| Auth | Email/password + Google OAuth (Supabase Auth), **registro libre abierto** | `disable_signup` se deja en `false` a propósito (decisión revertida el 2026-08-02: se evaluó cerrarlo y se decidió lo contrario). Cualquiera puede crear una cuenta; lo que la separa de "tener acceso" es la fila en `profiles` (ver siguiente fila). |
| Multi-tenancy | Dos caminos para crear un tenant, ambos terminan en la misma tabla `tenants`+`profiles`: (1) el superadmin lo crea desde el backoffice y asigna línea de WhatsApp, o (2) **auto-registro**: un usuario nuevo (Google o email/password) sin fila en `profiles` aterriza en la pantalla de onboarding `/create-company`, y al enviar el nombre de su empresa se crea el tenant y queda como su `tenant_admin` vía la función `self_register_tenant` (RPC `SECURITY DEFINER`, `leadly-db/supabase/migrations/20260802000008_self_register_tenant.sql`). Si fue sin querer, puede borrar su cuenta ahí mismo (`self-delete-account`, Edge Function) — solo funciona si esa cuenta *todavía* no tiene perfil, nunca sobre un tenant ya activo. | La app (`AuthContext.unprovisioned` + `RequireAuth`) es la que decide mostrar `/create-company` en vez de contenido real cuando `auth.uid()` no tiene fila en `profiles`. Un tenant auto-registrado queda con el mismo `status='active'` que uno creado por el backoffice — sin distinción por diseño (decisión del usuario). No pueden usar WhatsApp real hasta que el superadmin les asigne una línea manualmente. |

### Spikes que quedan abiertos (no bloquean el MVP, se resuelven en Fase 2+)

- ~~Alcance de "herramientas"/function-calling por tenant~~ — resuelto el 2026-08-06 como "Habilidades" (`ai_skills`, ver 3.5): catálogo activable por asistente, no genérico-configurable ni fijo-por-vertical.
- Modelo de facturación/planes de Leadly a sus tenants (límites de mensajes/tokens). Se diseña en Fase 3.
- Cifrado en reposo de credenciales (Meta token, API keys) — Supabase Vault/`pgsodium`. Se resuelve como parte de Fase 0 (no se puede posponer, son secretos reales).

---

## 2. Estructura del repositorio

Dos carpetas hermanas dentro de `Desktop/Proyectos/Leadly/`:

```
Leadly/
├── CLAUDE.md          ← este archivo
├── plan.md            ← historial de la sesión de origen (no editar, solo referencia)
├── leadly-app/        ← frontend: React 19 + TypeScript + Vite + Tailwind v4
└── leadly-db/         ← Supabase: migraciones SQL, seeds, config de Edge Functions
```

- `leadly-app/`: SPA única con dos layouts protegidos por rol (sección 4). Incluye `CHANGELOG.md` propio (sección 7).
- `leadly-db/`: `supabase/migrations/*.sql`, `supabase/functions/*` (Edge Functions), `supabase/seed.sql`. Sin backend propio — Postgres + RLS + Auth + Storage + Edge Functions cubren todo.

---

## 3. Modelo de datos (núcleo, v1)

**Convención transversal — borrado siempre es soft-delete (2026-08-04):** ninguna acción de "eliminar" desde la app hace un `DELETE` real sobre un registro de negocio (`crm_contacts`, `conversation_tags`, y cualquier tabla nueva con un botón "Eliminar" a futuro). En vez de eso, la tabla tiene `deleted_at timestamptz` + `deleted_by uuid references profiles(id)`, y "eliminar" es un `UPDATE` que los setea; las queries de listado siempre filtran `deleted_at is null`. Motivo: permitir deshacer/auditar, y no perder historial referenciado desde otro lado (ej. un contacto borrado que sigue teniendo conversaciones/mensajes vinculados). **Excepción explícita:** tablas puente muchos-a-muchos sin identidad propia (ej. `conversation_tag_assignments`) — ahí "quitar una asignación" no es un registro de negocio con historial, así que sigue siendo un `DELETE` real. Las policies RLS de `DELETE` en Postgres no se quitan (quedan como backstop para uso directo de superadmin/soporte vía SQL), pero el código de la app nunca las ejercita para las tablas de negocio.

### 3.1 Multi-tenancy y roles
- `tenants` — un negocio cliente de Leadly. Más allá de `name`/`status`, incluye datos reales de identidad legal y contacto (agregados en `20260802000013_tenant_profile_fields.sql` a pedido explícito del usuario, "el formulario de crear empresa es muy corto"): `entity_type` (`persona`|`empresa`), `legal_name` (obligatorio en el formulario si es empresa), `document_type`/`document_number`, `contact_email`/`contact_phone` (obligatorios en el formulario), `country`/`state_province`, `preferred_language` (`es`|`en` — primer paso hacia multi-idioma), `logo_url` (bucket Storage `tenant-logos`, público de lectura, máx. 5MB, RLS de escritura por tenant/superadmin). Estos campos son NULLables a nivel de base de datos (para no romper `self_register_tenant`, que solo pide el nombre) — lo "obligatorio" vive en la validación del formulario (`leadly-app/src/pages/backoffice/useTenantForm.ts`), no en constraints de DB.
- `profiles` — `id` (= `auth.users.id`), `role` (`superadmin` | `tenant_admin` | `tenant_agent`), `tenant_id` (null para superadmin).
- `auth_tenant_id()` / `is_superadmin()` — funciones `SECURITY DEFINER` que resuelven tenant/rol del `auth.uid()` actual, usadas como base de toda policy RLS del proyecto.
- RLS en toda tabla con `tenant_id`: el tenant solo ve sus propias filas; el superadmin ve todo.

### 3.2 WhatsApp
- `whatsapp_lines` — una fila por número de WhatsApp Business asignado a un tenant: `tenant_id`, `phone_number_id`, `business_account_id`, `access_token` (cifrado), `display_name`, `status` (`pending_verification` | `active` | `suspended`).
- `whatsapp_conversations` — `tenant_id`, `whatsapp_line_id`, `contact_phone`, `contact_name` nullable, `mode` (`ia` | `humano`), `status` (`open` | `closed`), `last_message_at`.
- `whatsapp_messages` — ledger inmutable (append-only): `conversation_id`, `direction` (`inbound` | `outbound`), `sender` (`contact` | `ia` | `agent:<profile_id>`), `content`, `wamid`, `tokens_used` nullable, `created_at`. **Solo se inserta si el envío a Meta fue confirmado exitoso** (fix "mensajes fantasma", 2026-08-05) — un envío fallido nunca aparece en el ledger, solo se loguea server-side (`console.error`); por eso ya no existe una columna `error_message` (se eliminó el 2026-08-06, estaba muerta).

### 3.3 Configuración de IA (multi-proveedor)
- `ai_assistants` — un **agente reutilizable por tenant**, no por línea (desacoplado el 2026-08-06, ver "Estado actual"): `tenant_id`, `name`, y `whatsapp_lines.ai_assistant_id` (nullable) es lo que asigna un agente a una línea. El mismo agente puede asignarse a varias líneas a la vez; una línea nunca tiene más de un agente activo respondiendo:
  - `provider` (`openai` | `gemini`)
  - `model` (string libre validado contra una lista permitida por proveedor, ej. `gpt-4.1`, `gpt-4o-mini`, `gemini-2.5-pro`, `gemini-2.5-flash`)
  - `system_prompt` (text)
  - `temperature`, `max_tokens` (opcional, valores por defecto razonables)
  - `is_active` (boolean — permite desactivar la IA sin borrar la config)
- La UI del backoffice/cliente para editar esto es un **select de proveedor + select de modelo dependiente** (el segundo select se filtra según el proveedor elegido), no campos de texto libre — requisito explícito del usuario.
- Las llamadas a IA son *stateless por turno*: se arma el contexto (system prompt + últimos N mensajes de `whatsapp_messages`) y se envía a la API del proveedor correspondiente (Chat Completions/Responses de OpenAI, `generateContent` de Gemini) vía Edge Function. No se depende del objeto "Assistant" ni "Thread" del dashboard de OpenAI.

### 3.4 CRM del tenant (contactos)
Agregado el 2026-08-03 a pedido explícito del usuario ("no quiero nada a medias, piensa en un CRM completo") — un `whatsapp_conversations.contact_phone` suelto no bastaba, cada tenant necesita una ficha de cliente persistente con historial, no conversaciones desechables.

- `crm_contacts` — el lead/cliente **del tenant** (no confundir con `tenants`, que son los clientes de Leadly): `tenant_id`, `full_name`, `phone` (único por tenant), `email`/`company` nullable, `stage` (`lead`|`contactado`|`negociacion`|`cliente`|`perdido`), `tags` (`text[]`), `assigned_to` (nullable, `profiles.id`).
- `crm_notes` — bitácora manual append-only sobre un contacto: `contact_id`, `author_id`, `content`, `created_at`. Sin políticas de update/delete a propósito, igual que `whatsapp_messages`.
- `whatsapp_conversations.contact_id` — FK nullable a `crm_contacts`. `whatsapp-webhook` lo resuelve automáticamente en cada mensaje entrante (busca por `tenant_id`+`phone`, crea el contacto si no existe) — así toda conversación real queda ligada a una ficha de cliente sin que el tenant tenga que hacer nada manual.
- El detalle de un contacto (`/app/clientes/:id`) es donde vive "todo lo que se ha hablado con él": notas manuales + la lista de conversaciones de WhatsApp vinculadas (con link directo al Inbox).
- `crm_pqrs`/`crm_pqr_updates` — casos formales (Petición/Queja/Reclamo) por contacto, con código secuencial por tenant (`PQR-{code}`) y seguimientos anidados con su propio consecutivo heredado (`PQR-{code}-{seq}`); ambos con `created_by_ai` para distinguir lo que hizo la IA de lo que escribió un agente. La IA interactúa con esto mediante tool-calling (`whatsapp-ai-tools`, catálogo compartido en `_shared/aiTools.ts`): crear PQR, agregar nota, agregar seguimiento, cambiar estado y consultar estado, siempre resolviendo "el PQR más reciente del contacto" del lado del servidor (nunca un id que el modelo tenga que recordar entre turnos, ver 3.3).
- `crm_attachments` — imágenes adjuntas a un PQR o a uno de sus seguimientos (exactamente uno de los dos, nunca ambos), solo imágenes por ahora (`mime_type like 'image/%'`), guardando mimetype/peso/nombre original. Bucket privado `crm-attachments` (URLs firmadas, no públicas, a diferencia de `tenant-logos`) porque una foto de un reclamo puede ser sensible. Dos orígenes: un agente la sube manualmente desde el drawer de PQR o el composer de seguimiento en el CRM, o un cliente la manda por WhatsApp -- `whatsapp-webhook` descarga el medio de la Graph API (dos pasos: resolver `url` temporal por `media.id`, después bajar los bytes) y lo sube al bucket; si en ese mismo turno la IA crea o actualiza un PQR, `whatsapp-ai-respond` inyecta esa imagen ya subida como `pending_attachment` (mismo principio de inyección server-side que `tenant_id`/`contact_id`) y `whatsapp-ai-tools` la enlaza al caso. La IA no interpreta el contenido de la imagen (sin visión real) -- solo la guarda y la muestra al agente; queda abierto para una iteración futura si hace falta. Sí puede, en cambio, **reenviársela al cliente**: `get_pqr_status` devuelve los `attachments` (id + mimetype) del caso y sus seguimientos, y la tool `send_attachment` genera una URL firmada de corta duración y la manda por Graph API (`type: image`, `image.link`) -- decisión explícita del usuario (2026-08-05): ante un pedido tipo "compárteme el soporte del reembolso", la IA lo envía sola en vez de derivar siempre a un humano. Solo puede enviar adjuntos del PQR más reciente del contacto (mismo resguardo que el resto de las tools).
- `crm_contacts.stage` (`lead`|`contactado`|`negociacion`|`cliente`|`perdido`) sigue existiendo como clasificación general del contacto, independiente de sus oportunidades — ver 3.6, que agrega seguimiento real de negociaciones (valor, etapa de pipeline, fecha de cierre) sin reemplazar este campo.
- Pendiente/spike abierto: vista de pipeline tipo kanban con drag-and-drop para `crm_opportunities` (hoy la data existe — ver 3.6 — pero no hay UI todavía, ni siquiera en lista).

### 3.5 Habilidades de IA (`ai_skills`)
Agregado el 2026-08-06 — generaliza el tool-calling de PQR (antes hardcodeado: mismo set de *tools* mandado siempre a **todos** los asistentes, con las instrucciones de uso escritas a mano en cada `system_prompt`, ej. el prompt de "Tenant QA Uno") a un catálogo activable por asistente.

- `ai_skills` — catálogo fijo (`key`, `name`, `description`, `prompt_fragment`). Agregar una habilidad nueva siempre requiere código (una tool nueva en `_shared/aiTools.ts` con su `skill` + un caso en `whatsapp-ai-tools`) — esta tabla solo controla el lado de *display* + *prompt*, nunca la implementación.
- `ai_assistant_skills` — tabla puente (`ai_assistant_id`, `skill_key`), sin identidad de negocio propia → "desactivar" es un `DELETE` real (misma excepción que `conversation_tag_assignments`, ver la convención de soft-delete arriba).
- **La activación es real, no solo texto**: `whatsapp-ai-respond` arma el `tools` que le manda a OpenAI/Gemini filtrado por las habilidades activas del asistente (`toolsForSkills`) — si una habilidad está apagada, el modelo no puede llamar esas funciones, es imposible, no solo "se le pide que no lo haga". El `system_prompt` final = prompt propio del negocio + `prompt_fragment` de cada habilidad activa, concatenados. `whatsapp-ai-tools` tiene además un chequeo de defensa en profundidad (`isToolAllowed`) por si algo llama una tool de una habilidad deshabilitada.
- UI: sección "Habilidades" dentro del drawer "Asistente de IA" (`AiAssistantDrawer.tsx`, prop `manageSkills`), **solo visible en el backoffice** (`ClienteDetalle.tsx`) — es el superadmin quien se la habilita al cliente, el tenant no se autogestiona esto. Toggle por habilidad, aplica al instante (no depende del "Guardar cambios" del resto del formulario).
- Único skill hoy: `pqr` (las 6 tools de PQR/notas/adjuntos de la sección 3.4). Backfill: todos los asistentes que existían antes de esta migración quedaron con `pqr` activo (mismo comportamiento que tenían de facto). Asistentes nuevos arrancan sin ninguna habilidad — elección explícita del superadmin desde ahí en más.

### 3.6 CRM ampliado: Empresas, Oportunidades y Tareas
Agregado el 2026-08-06 a partir de un modelo de datos más completo que trajo el usuario (inspirado en CRMs genéricos tipo HubSpot) — se evaluó sección por sección qué cerraba un vacío real vs. qué era sobre-alcance para el estado actual del producto. Ver el análisis completo en el historial de la sesión; resumen de las decisiones:

- **`crm_accounts`** — la capa B2B que faltaba: hasta ahora `crm_contacts.company` era texto libre, no una entidad (no se podían agrupar varios contactos de la misma empresa cliente, ni ver sus oportunidades juntas). `crm_contacts.account_id` (nullable) enlaza un contacto a su empresa — B2C/individual sigue sin necesitar uno. `crm_contacts.company` se deja intacto por ahora (dato histórico ya cargado); no se migró automáticamente a filas de `crm_accounts`.
- **`crm_pipelines` / `crm_pipeline_stages` / `crm_opportunities`** — reemplaza la limitación de que un contacto solo podía estar en **un** `stage` fijo a la vez, sin valor ni fecha de cierre. Una oportunidad tiene `pipeline_id`/`stage_id`/`contact_id`/`account_id` (opcional)/`owner_id`/`value`/`currency`/`priority`/`expected_close_date`/`status` (`open`|`won`|`lost`, derivado de si la etapa tiene `is_won`/`is_lost`). **Decisión deliberada de mantenerlo simple**: cada tenant tiene un único pipeline "Ventas" con 6 etapas fijas (Nuevo/Contactado/Propuesta/Negociación/Ganado/Perdido), creado automáticamente por un trigger `after insert on tenants` (`seed_default_pipeline`) — no un constructor de pipelines genérico multi-pipeline configurable. Las tablas ya soportan más de un pipeline por tenant (sin constraint que lo impida), así que agregar esa UI en el futuro no requiere otra migración, pero no se construyó ahora sin un caso de uso real que lo pida.
- **`crm_tasks`** — no existía ningún tipo de tarea/seguimiento antes. `contact_id`/`opportunity_id` ambos nullable (una tarea puede ser general, sobre un contacto, o sobre una negociación puntual).
- **Todas con el mismo patrón que `crm_contacts`**: `tenant_id` + RLS vía `auth_active_tenant_id()`, soft-delete (`deleted_at`/`deleted_by`), trigger `set_updated_at`.
- **Explícitamente fuera de esta ronda** (evaluados y descartados por ahora, no por ser mala idea sino por evitar sobre-construir sin un caso de uso real todavía):
  - Motor de automatizaciones genérico (no-code workflow builder) — se prefiere seguir agregando automatizaciones puntuales *hardcodeadas* como las que ya existen (recordatorios de citas, reset de contexto, retry de IA, habilidades) hasta que haya patrones repetidos reales entre tenants que justifiquen un motor configurable.
  - Base de conocimiento con embeddings (RAG) para los asistentes — inversión de ingeniería seria aparte (pgvector, chunking, pipeline de embeddings), se evalúa cuando un cliente concreto lo pida.
  - `products`/`quotes`/`quote_items` (catálogo + cotizaciones formales) — "Catálogo" ya está reservado a propósito para el catálogo nativo de WhatsApp (Beta bloqueado, ver 4.2), no para un sistema de cotización/facturación propio.
  - Generalizar `whatsapp_conversations`/`whatsapp_messages` a multicanal (`channel` genérico) — toda la infraestructura actual (webhook, Embedded Signup) está construida 100% alrededor de WhatsApp Cloud API; generalizar ahora es pagar complejidad por adelantado sin necesidad real de otro canal todavía.
  - Reportes como tabla propia — se construyen consultando `crm_opportunities`/`whatsapp_conversations`/`crm_tasks` directamente cuando se aborde el Dashboard real, nunca una tabla de "reportes" nueva.
- **UI**: `pages/tenant/Empresas.tsx` (+ `accounts/AccountDrawer.tsx`), `pages/tenant/Oportunidades.tsx` (+ `opportunities/OpportunityDrawer.tsx`, cambio de etapa inline con un `Select` por fila, actualiza `status` automáticamente vía `moveOpportunityToStage`), `pages/tenant/Tareas.tsx` (+ `tasks/TaskDrawer.tsx`, checkbox para completar). Todas siguen el mismo patrón que `Contactos.tsx`/`ContactDrawer.tsx` (lista + drawer, soft-delete con confirmación inline). **Pendiente explícito**: vista kanban con drag-and-drop para Oportunidades (hoy es una lista simple con filtro por etapa).
- `pages/tenant/EmpresaDetalle.tsx` (`/app/empresas/:id`, 2026-08-06) — ficha 360 de la empresa: header (avatar, badge `is_active`, botón "Editar" solo como el lápiz junto al nombre — **no duplicar ese botón en ningún lado más**, feedback explícito del usuario), fila de datos de contacto, tabs con contador (`Resumen`/`Contactos (n)`/`Oportunidades (n)`/`Conversaciones`/`Tareas`/`Cotizaciones` bloqueado con `LockedFeature`). El tab "Resumen" arma tiles/cards a partir de datos reales (`crm_opportunities`, `crm_tasks`, `whatsapp_conversations` de los contactos vinculados) — el tile "Cotizaciones enviadas" se deja en 0/"Próximamente" a propósito, no se fabrica dato. **Textos de botón cortos** (`+ Contacto`, `+ Oportunidad`, no "Nueva/Vincular X") y **todo link a un contacto abre en pestaña nueva** (`<a target="_blank">`, nunca `navigate()`) son decisiones explícitas del usuario para esta pantalla — replicar el mismo criterio en pantallas nuevas similares.
  - `crm_accounts.notes` y `crm_accounts.is_active` (migraciones `20260806000006`/`...007`) se agregaron específicamente para que el card "Notas internas" y el badge "Cliente activo" del diseño de referencia del usuario fueran datos reales, no inventados.
  - Eliminar una empresa (`deleteAccount`) ya es soft-delete real, pero **falta decidir qué pasa con los contactos/oportunidades vinculados** al eliminarla (hoy quedan huérfanos pero visibles, sin soft-delete en cascada) — pendiente explícito, no bloquea nada hoy.

### 3.7 Fase A: Kanban de Oportunidades pipeline-céntrico (spec, 2026-08-06 — implementación arrancando)

El usuario propuso (y se validó en sesión) reorientar el CRM alrededor del Pipeline en vez de tener módulos aislados de Tareas/Actividades/Automatizaciones — inspirado en, pero deliberadamente más simple que, HubSpot/Kommo/Pipedrive. **Se evaluó la propuesta completa del usuario y se acordó dividirla en fases** porque mezclaba cosas ya alcanzables con 2-3 proyectos de ingeniería grandes por separado. Esta sección documenta **solo la Fase A** (la parte que se aprobó para construir ahora) — si retomás esto en otra conversación, este es el punto de partida.

**Fuera de alcance a propósito (Fases B/C/D, no tocar en esta ronda):**
- **Motor de automatizaciones visual tipo n8n** (nodos "Cuando entra a etapa → esperar N días → condicional → acción"). Es un proyecto de meses (motor de ejecución con estado, scheduling, branching, editor visual) — se sigue prefiriendo automatizaciones puntuales hardcodeadas (como las que ya existen: recordatorios de citas, reset de contexto, retry de IA, habilidades) hasta que haya patrones repetidos reales entre tenants.
- **IA por oportunidad** ("Aurora": resumen de la conversación + sugerencias accionables tipo "Ofrecer descuento"/"Agendar llamada"). Es una feature de IA nueva y separada (nuevo prompt, nuevo Edge Function, costo por generación) — proyecto propio después de que el Kanban esté andando.
- **Cotizaciones y Archivos por oportunidad** — ya estaban descartados para esta ronda (ver 3.6), la propuesta los reintroduce como tabs del panel de oportunidad; siguen descartados.

**Sí en alcance para la Fase A:**

1. **`crm_opportunity_stage_history`** (tabla nueva) — sin esto, "Conversión" y "Tiempo promedio" en el dashboard del pipeline serían números inventados (hoy solo existe el estado *actual* de cada oportunidad, no cuándo cambió de etapa). Columnas: `id`, `opportunity_id references crm_opportunities`, `from_stage_id references crm_pipeline_stages nullable` (null en la primera fila, al crearse la oportunidad), `to_stage_id references crm_pipeline_stages`, `changed_by uuid references profiles nullable`, `created_at`. Se inserta una fila cada vez que `crm_opportunities.stage_id` cambia — hacerlo con un trigger `after update` en `crm_opportunities` (comparando `old.stage_id` vs `new.stage_id`), no confiar en que el frontend siempre recuerde insertar el log. Con esta tabla: "tiempo promedio" = promedio de `created_at` entre entrada y salida de cada etapa; "conversión" = % de oportunidades cuyo `to_stage_id` más reciente tiene `is_won = true` sobre el total cerrado (ganado+perdido) en un período.
2. **Vista Kanban** reemplazando/extendiendo `Oportunidades.tsx` — columnas por `crm_pipeline_stages.display_order`, tarjetas arrastrables entre columnas (evaluar una librería liviana de drag-and-drop tipo `@dnd-kit/core` — no hay ninguna instalada en el proyecto todavía, revisar bundle size antes de sumarla dado el warning de chunk grande que ya tiene `vite build`). Al soltar una tarjeta en otra columna: llama `moveOpportunityToStage` (ya existe, en `lib/api/opportunities.ts`) — el trigger de arriba registra el historial solo. Encabezado del pipeline: selector de pipeline (hoy siempre va a ser "Ventas", el único que existe — el selector es de cara al futuro, no funcional todavía) + métricas (Valor total, N° oportunidades, Conversión, Tiempo promedio, Tareas pendientes) calculadas con datos reales.
3. **Panel lateral de oportunidad** (Drawer, no navegación a otra ruta) con tabs: Resumen (campos: pipeline, etapa, responsable, valor, probabilidad, fecha estimada, etiquetas si se agregan, descripción), Conversaciones (reusar `listConversationsForContact` del contacto de la oportunidad), Tareas (usar `crm_tasks.opportunity_id`, ya existe la columna). Nada de tabs Automatizaciones/Archivos/IA todavía (fuera de alcance, ver arriba).
4. **Nav**: sacar "Tareas" del nav del tenant (`TenantLayout.tsx`) — las tareas se acceden desde el panel de la oportunidad/contacto, no como módulo aislado. A cambio, un widget "Mis tareas" en el Dashboard del tenant. **Esto depende de que el Dashboard real exista** — hoy sigue siendo placeholder (`ComingSoon.tsx` o similar, ver Fase 1 en el roadmap de la sección 8) — construir ese widget probablemente implica construir el Dashboard real al mismo tiempo, o al menos una versión mínima de él. Evaluar si conviene primero un Dashboard mínimo (solo el widget de tareas) antes de sacar "Tareas" del nav, para no dejar esa funcionalidad sin ningún punto de entrada mientras tanto.
5. **`Empresas.tsx`/`EmpresaDetalle.tsx`**: la tab "Oportunidades" de una empresa puede seguir siendo la tabla simple que ya existe (no hace falta un mini-kanban ahí también) — el Kanban completo vive en `/app/oportunidades`.

**Checklist de implementación** (marcar acá a medida que avance, para que quien retome sepa exactamente dónde quedó):
- [x] Migración `crm_opportunity_stage_history` + trigger de log automático (`20260806000008_crm_opportunity_stage_history.sql` — dos triggers: uno en `insert` para la etapa inicial, otro en `update` cuando cambia `stage_id`; RLS de solo lectura, nadie inserta a mano)
- [x] Backfill: una fila inicial por cada `crm_opportunities` existente (con `from_stage_id = null`, `to_stage_id = stage_id` actual) — verificado en Supabase, corrió sobre la única oportunidad que existía hoy
- [x] `lib/api/opportunities.ts`: `listStageHistoryForOpportunities` + `computePipelineMetrics` (función pura, testeable aparte de cualquier componente)
- [x] Componente Kanban — implementación manual con HTML5 Drag and Drop nativo (`draggable` + `onDragStart`/`onDragOver`/`onDrop`, usando `dataTransfer.setData('text/plain', opportunity.id)` para pasar el id, **no** un `useState` — se probó que depender de estado de React para identificar qué tarjeta se soltó tiene condición de carrera con el ciclo de eventos nativos del navegador). Sin librería nueva, evita el warning de bundle grande que ya tenía el proyecto.
- [x] Reemplazar `Oportunidades.tsx` (lista) por la vista Kanban + métricas del header (`computePipelineMetrics` en `lib/api/opportunities.ts`, `null` en vez de `0` cuando todavía no hay datos para "Conversión"/"Tiempo promedio" — mostrar "—" en la UI en ese caso). **Pitfall real encontrado**: la consulta a `crm_opportunity_stage_history` embebiendo `crm_pipeline_stages` fallaba silenciosamente porque la tabla tiene dos FKs a `crm_pipeline_stages` (`from_stage_id`/`to_stage_id`) — PostgREST necesita el hint explícito `to_stage:crm_pipeline_stages!to_stage_id(...)`, si no, es ambiguo.
- [x] Configuración de pipelines (2026-08-06, ronda aparte antes del panel lateral -- el usuario notó que crear un pipeline nuevo no tenía UI): `lib/api/pipelines.ts` (CRUD de `crm_pipelines`/`crm_pipeline_stages`, sin migraciones nuevas -- el schema ya soportaba múltiples pipelines por tenant). En `Oportunidades.tsx` el chip "Pipeline: X" pasó a ser un popover real para cambiar/crear pipeline (crear solo `tenant_admin`), con un ícono de engranaje al lado que abre `opportunities/PipelineSettingsDrawer.tsx` (nombre/color/descripción del pipeline, etapas con autosave por campo, reordenar con flechas, eliminar etapa/pipeline con `ConfirmDialog`). `listOpportunities` ganó un filtro `pipelineId` para que el tablero y las métricas no mezclen datos entre pipelines cuando hay más de uno.
- [x] Panel lateral de oportunidad -- `opportunities/OpportunityPanel.tsx`, exactamente 3 tabs (Resumen/Conversaciones/Tareas, nada de Archivos/Actividad/IA todavía, sigue fuera de alcance). Resumen es de solo lectura (Pipeline, Etapa, Responsable, Probabilidad, Prioridad, Cierre estimado, Origen, Creada el, Descripción) con botones "Editar" (abre el `OpportunityDrawer` existente, no se duplican campos editables) y "Eliminar" (`ConfirmDialog`); Conversaciones reusa el mismo patrón que `ContactoDetalle.tsx` (`listConversationsForContact`); Tareas usa la nueva `listTasksForOpportunity` + `TaskDrawer` (que ganó un prop `defaultOpportunityId`, mismo patrón que `defaultAccountId`).
- [x] Resto del header del mockup, todo en la misma ronda que el panel: toggle Kanban/Lista (`opportunities/OpportunityListView.tsx`, tabla con select de etapa por fila igual que la lista pre-Kanban), filtro "Responsable" y "Ordenar por" (siempre visibles), botón "Filtros" con Prioridad (popover, mismo patrón que `Inbox.tsx`), y breadcrumb Empresas/{empresa}/Oportunidades vía `?account=<id>` en la URL (mismo patrón `?empresa=<id>` de `Contactos.tsx`) con un link "Ver en Kanban" nuevo en la sección Oportunidades de `EmpresaDetalle.tsx`.
- [ ] Definir si el Dashboard real se construye ahora (aunque sea mínimo) o si "Tareas" se queda en el nav un poco más hasta que exista un lugar para el widget -- **decisión tomada el 2026-08-06: se queda en el nav por ahora**, es un proyecto aparte.
- [ ] Sacar "Tareas" del nav una vez el widget de Dashboard exista -- depende del punto anterior, no entra todavía.
- [ ] Probar todo en navegador antes de dar la fase por cerrada -- pendiente, lo prueba el usuario (sin acceso a navegador en este entorno).

---

## 4. Layouts y navegación (guard por rol)

Dos layouts, cada uno con su propio árbol de rutas, protegidos tanto por **guard de navegación** (redirección si el rol no corresponde) como por **RLS en la base de datos** (defensa en profundidad — nunca confiar solo en el guard de frontend):

### 4.1 `BackofficeLayout` (rol `superadmin`)
- Dashboard: resumen de tenants activos, líneas de WhatsApp, conversaciones del día.
- **Clientes (tenants)**: CRUD — crear, ver detalle, activar/desactivar, ver plan.
- **Líneas de WhatsApp**: CRUD, asignación a un tenant, estado de verificación de Meta.
- **Configuración de IA por línea**: system prompt (editor de texto largo) + selects de proveedor/modelo + parámetros opcionales, con vista previa/chat de prueba antes de guardar.
- **Usuarios por tenant**: crear usuarios (`tenant_admin`/`tenant_agent`) vía Edge Function `admin-create-tenant-user`.
- **Changelog** (sección 7): ver el historial de versiones publicado, visible también para tenants.

### 4.2 `TenantLayout` (roles `tenant_admin` / `tenant_agent`)
- Dashboard simple: conversaciones abiertas, mensajes sin responder (aún placeholder).
- **Conversaciones** (Inbox): lista + vista de chat estilo WhatsApp Web (burbujas, orden cronológico).
  - Toggle **modo IA / modo humano** por conversación — al pasar a humano, la IA deja de responder automáticamente y el agente escribe directo.
  - Historial completo de mensajes, incluyendo los que respondió la IA (marcados visualmente distinto a los del agente humano).
  - **Nueva conversación**: el agente puede iniciar una conversación él mismo (elige un cliente del CRM o crea uno nuevo + línea), no solo reaccionar a mensajes entrantes.
- **Clientes** (CRM, ver 3.4): listado de contactos con buscador/etapa/etiquetas, ficha de detalle con info + notas manuales + historial de conversaciones vinculadas.
- **Campañas** / **Catálogo**: visibles en el nav con tag "Beta", pantalla bloqueada sin funcionalidad — reservado para pausas publicitarias y catálogo de productos, no es parte del alcance actual.
- **Configuración de IA de su(s) línea(s)**: mismo componente de selects proveedor/modelo + system prompt que en backoffice, pero acotado a su propio tenant (RLS).
- **Changelog**: versión de solo lectura, mismo componente que en backoffice.

### 4.3 Guards
- Guard de ruta (`RequireAuth` + `RequireRole`) que redirige según `profiles.role`.
- La navegación (sidebar/menú) se arma condicionalmente según el rol; nunca se renderiza un link a una ruta que el guard vaya a bloquear.

---

## 5. Diseño / UX

- Diseño simple, moderno, **mobile-first** — el panel de cliente debe usarse cómodamente desde el teléfono (revisar conversaciones y responder en modo humano es el caso de uso más probable en movilidad).
- Paleta e identidad: usar los assets de marca ya definidos (azul principal `#101A35`, turquesa `#2FA9A5`, tipografía Manrope/Inter) — ver brandbook compartido.
- Componentes reutilizables entre ambos layouts donde aplique (selects, tablas, chat), variando solo el scope de datos por rol.

---

## 6. Edge Functions (v1)

Todas desplegadas y probadas end-to-end (webhook simulado con payload de Meta firmado + curl directo). Sin keys reales de OpenAI/Gemini ni línea verificada por Meta todavía, así que las llamadas externas fallan con gracia (mensaje de fallback al contacto si el envío se puede confirmar; si el envío mismo falla, no se inserta nada en `whatsapp_messages`, solo se loguea server-side, ver 3.2) — comportamiento esperado hasta que el usuario aporte esas credenciales.

- `whatsapp-webhook` — ✅ handshake `GET` + mensajes entrantes `POST`, valida firma (`X-Hub-Signature-256`) y `verify_token`, resuelve/crea conversación (`upsert` por `whatsapp_line_id`+`contact_phone`), inserta mensaje inbound, e invoca `whatsapp-ai-respond` (con `await`, ver pitfall en "Estado actual") cuando la conversación está en modo IA y la línea activa. Desplegada con `--no-verify-jwt` (endpoint público, Meta no manda JWT).
- `whatsapp-ai-respond` — ✅ interno, solo acepta llamadas cuyo bearer token sea exactamente el `SUPABASE_SERVICE_ROLE_KEY` del proyecto. Arma contexto (últimos 10 mensajes), detecta traspaso a humano por frase antes de llamar al LLM, llama al proveedor configurado (`ai_assistants.provider`), responde por Graph API. No se ejecuta si la conversación está en modo humano, la línea no está activa, o el asistente está inactivo. **Cumplimiento Meta implementado** (política AI-Assisted Business Messaging Guidelines, vigente desde 2026-01-15): divulga que es IA en el primer mensaje automático de cada conversación (`alreadyRespondedByAi` chequea el historial), y frases tipo "hablar con humano" cambian `mode` a `humano` automáticamente (`_shared/whatsapp.ts::requestsHumanHandoff`). Pendiente/spike abierto: validación/aviso en el editor de `system_prompt` para que un tenant no lo configure como asistente genérico sin límite de tema.
- `whatsapp-send-human` — ✅ envío de mensaje manual desde el panel del tenant en modo humano; usa el JWT del propio caller (no admin), así que RLS aplica tal cual sin lógica de autorización reinventada. Desde 2026-08-05 también acepta un `media` opcional (`storage_path`/`mime_type`/`size_bytes`, subido antes por el agente al bucket `crm-attachments` con su propio JWT) para adjuntar una imagen al mensaje saliente: la función firma una URL de corta duración (Meta necesita poder descargarla ella misma) y llama a la Graph API con `type: image`, mismo patrón que `send_attachment` de la IA (ver 3.4).
- `admin-create-tenant-user` — ✅ creación de usuarios de un tenant, invita por correo (`inviteUserByEmail`).

---

## 7. Changelog del producto

- `leadly-app/CHANGELOG.md`, formato [Keep a Changelog](https://keepachangelog.com/) + versionado semántico, empezando en `0.1.0` para el MVP.
- Se expone también dentro de la app (sección "Novedades" visible en ambos layouts, componente `Changelog`) — **decidido en Fase 1**: sigue siendo el archivo estático `CHANGELOG.md`, no una tabla editable.

---

## 8. Roadmap / backlog real (reescrito el 2026-09-06 — la versión anterior, por fases "MVP 0-2", quedó obsoleta hace semanas: el MVP original fue superado por completo por el pivote a ERP del 2026-08-15)

**Completado, no re-litigar** (fundación, backoffice básico, Inbox/CRM inicial — MVP original de las fases 0-2, todo el detalle en las entradas de "Estado actual" de agosto): Auth, multi-tenancy, backoffice de tenants/líneas/usuarios, IA multi-proveedor con habilidades activables, Inbox en tiempo real, CRM de contactos, Dashboard real, WhatsApp Embedded Signup construido (queda una duda real abajo). A partir de ahí, completado en rondas posteriores y confirmado por código/migraciones/commits:
- Pivote a ERP sin prefijo `crm_*` (2026-08-15) — esquema nuevo para contactos/productos/oportunidades/tareas/ventas.
- Roles y permisos por acción — **hitos 1-2 de 5 completos** (esquema + UI de gestión), hito 3 arrancó (candado de ruta por permiso) pero **hitos 3 (gating de botones) 4 y 5 (RLS real) siguen sin terminar** — ver la entrada del 2026-08-29 para el detalle exacto. Mientras el hito 5 no se haga, destildar un permiso en el editor de roles no restringe nada de verdad a nivel de base de datos.
- Tienda pública multi-tenant (marketplace, checkout con OTP, pagos Wompi/crédito) — 2026-08-26/27.
- CRM ampliado (empresas→fusionado de vuelta en contactos, pipeline/oportunidades con Kanban, tareas, direcciones) — 2026-08-06/09, Kanban de la Fase A (sección 3.7) dado por completo en esa misma ronda.
- Inventario multi-bodega + kardex, Despachos, Devoluciones, Crédito de clientes — construidos en rondas de agosto no detalladas sección por sección en este archivo; confiar en el código (`Warehouses`, `Dispatches`, `Returns`, `Credit` en el nav del tenant).
- Dashboard/Calendario rediseñados con datos operativos reales — 2026-09-02.
- Módulo **POS** completo (venta rápida, cuentas abiertas, puntos de venta, tickets térmicos) — 2026-09-04/05, con el fix de validación de stock correcto (cargar, no cobrar) recién cerrado el 2026-09-06.
- **Facturación electrónica DIAN** (impuestos por producto, esquema, XML firmado UBL 2.1 + XAdES-EPES, envío SOAP/mTLS, PDF factura/remisión) — 2026-09-03/04/05, y **aceptada de verdad en PRODUCCIÓN el 2026-09-10** (factura FE12 de Barriles de la sexta, "Procesado Correctamente", CUFE real — ver la entrada de ese día para los 5 rechazos previos y qué destapó cada uno). Producción usa `SendBillSync` (síncrono, veredicto en la misma respuesta); habilitación sigue con `SendTestSetAsync` + polling.
- Carrito único (`carts`/`cart_items` compartido entre POS/portal/tienda pública, reemplaza `storefront_carts`) — 2026-09-04.
- Confirmación de venta centralizada en triggers de DB (`guard_sales_order_confirmation`/`apply_sales_order_confirmed_effects`), en vez de en el código de cada caller — 2026-09-03.
- Paginación server-side real para listados grandes (Órdenes, Gastos) — patrón replicable para cualquier listado futuro que supere 1000 filas.
- Módulo **Gastos** (categorías, entrada de inventario desde un gasto, import de 6072 registros históricos de Fudo) — 2026-09-06, deshabilitado por defecto hasta habilitarlo para "Barriles de la sexta".
- Rebrand visual a **Lexy** (logo, copy de marketing reposicionado como plataforma de operaciones, no CRM) — 2026-09-02, sin cambio de nombre en código/repo.
- Agendamiento real de citas (duración, vínculo a oportunidad) — 2026-09-06. Queda pendiente exponerle duración/oportunidad al tool-calling de la IA (`book_appointment` sigue mandando solo `scheduled_at`).
- Voseo → tuteo en todo el español de la app — 2026-09-04.
- Servicio de notificaciones (toasts) de toda la app (`useToast`) — 2026-09-10, con `DianDirectoCredentialDrawer` como primer consumidor. Los otros cuatro drawers de Integraciones siguen con el texto verde viejo, pendiente de migrar.
- Entrega de documentos electrónicos al adquiriente por correo (Resend) + conservación del XML firmado en Storage — 2026-09-10. Falta solo cargar el secreto de la API.

**Pendiente explícito, confirmado reciente**:
- **Modo de venta de la IA (`ventas`)** — pendiente desde el 2026-08-27, nada de lo construido después lo tocó: falta un flag en Configuración para que cada tenant elija si su asistente cierra venta directa (comportamiento actual), arma una cotización, o llena el carrito y redirige al cliente a la tienda pública (que para el 08-27 ni siquiera existía — hoy sí, ver 2026-08-26, así que esta tercera opción ya es técnicamente viable). Sin diseñar todavía.
- Roles y permisos, hitos 3 (gating de botones por `usePermission` en los ~11 módulos operativos), 4 (reemplazar `isAdmin` sueltos por `usePermission`) y 5 (RLS real módulo por módulo, empezando por `clients`) — ver 2026-08-29.
- Facturación electrónica DIAN — **factura Y nota crédito confirmadas aceptadas en producción** (2026-09-10). Lo que queda abierto: (1) cargar el secreto `RESEND_API_KEY` para que la entrega por correo al adquiriente empiece a funcionar (todo lo demás de ese flujo ya está construido y probado hasta ese punto); (2) el contenedor `AttachedDocument`, que exige la segunda modalidad de entrega del numeral 9.3; (3) la notificación blanda `FAZ09` en cada factura (falta un código de estándar externo acreditado por producto); (4) los textos de los conceptos de corrección 5 y 6, sin verificar contra el xlsx de la Caja de Herramientas (`CBF04`); (5) las retenciones, que `sendInvoiceToDian` sigue mandando en `withholdings: []` aunque `tenant_withholding_configs` ya exista. Todo el detalle en la entrada del 2026-09-10.
- Recordatorios de citas por WhatsApp — **retirado, no resuelto**: el cron que los mandaba se desactivó el 2026-09-06 porque el mecanismo (texto libre) no puede cumplir la promesa sin una plantilla HSM aprobada por Meta, que nunca se construyó (mismo requisito ya resuelto para el OTP de checkout de la tienda pública — se podría reusar ese conocimiento si se retoma).
- Infraestructura de despliegue de `leadly-app` — se dejó de usar el hosting anterior (2026-09-06 según nota original, sin fecha exacta confirmada), sin reemplazo elegido. CI/monitoreo/backups siguen sin resolver.

**Sin verificar recientemente (abierto hace semanas, puede que ya no aplique — confirmar contra el código antes de asumir cualquier cosa)**:
- WhatsApp Embedded Signup (self-service por tenant) — construido y desplegado el 2026-08-04, bloqueado entonces por el App Review de Meta (`whatsapp_business_management`/`whatsapp_business_messaging`) y por elegir dominio final. Sin actualizaciones documentadas desde el 2026-08-06.
- Planes y facturación de Leadly/Lexy a sus propios tenants (`plans`/`subscriptions`) — nunca se empezó, según todo lo revisado.
- Motor de automatizaciones genérico (no-code) y base de conocimiento con embeddings (RAG) para los asistentes — evaluados y diferidos a propósito el 2026-08-06 (ver 3.6), sin indicios de que se hayan retomado.
- Campañas (envíos masivos) y Catálogo nativo de WhatsApp — visibles en el nav como "Beta" bloqueado desde el MVP original; no hay evidencia reciente de que tengan backend.
- Cumplimiento (ventana 24h de WhatsApp para texto libre, plantillas HSM, habeas data) — tema transversal, reaparece cada vez que se topa con un límite real de Meta (ver recordatorios de citas arriba).

---

## 9. Cómo se trackea el trabajo

### Migraciones: siempre por archivo + `supabase db push` (decisión explícita del usuario, 2026-09-13)

**Todo cambio de esquema/datos se escribe como archivo en `leadly-db/supabase/migrations/` y se aplica con `supabase db push`** — nunca aplicándolo directo contra la base (ni por el MCP de Supabase, ni con SQL suelto). El motivo es trazabilidad: el repo tiene que poder responder qué cambió, cuándo y por qué, y eso solo se cumple si cada cambio aplicado tiene su archivo con el mismo nombre y la misma versión que la fila de `supabase_migrations.schema_migrations`.

Qué pasó cuando no se respetó (y cómo se reparó): 7 migraciones se aplicaron con el MCP (`apply_migration`), que asigna su PROPIA marca de tiempo al registrarlas. El archivo local quedó con otra versión aunque el contenido y el nombre fueran idénticos, así que `supabase db push` empezó a fallar con "Remote migration versions not found in local migrations directory" y proponía `migration repair`. Se reparó **renombrando los archivos locales a la versión que quedó registrada en el servidor** (no con `repair`, que solo miente en la tabla y deja el archivo huérfano). Verificado con `supabase db push --dry-run` → "Remote database is up to date".

Usar `execute_sql` del MCP para LEER (auditorías, diagnósticos) sigue siendo correcto y es lo esperable; lo que no se hace es escribir esquema o datos por ahí.

**Este `CLAUDE.md` es la fuente de verdad tanto de arquitectura/decisiones como de backlog** (evita reprocesos si se usa otra IA o si el usuario avanza manualmente): el roadmap por fases (sección 8) y las notas de "pendiente"/"fuera de alcance" repartidas en cada sección numerada son el estado real del qué falta. Antes de empezar a implementar cualquier tarea, revisar si ya figura como hecha (`[x]`) en este documento — si el código no coincide con lo que dice acá, confiar en el código y corregir el documento, no al revés (ver notas de "desfase" repartidas en el historial de "Estado actual").
