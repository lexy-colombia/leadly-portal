# Leadly — Plataforma multi-tenant de asesores de WhatsApp con IA

> Este archivo es la fuente de verdad técnica del proyecto. El backlog vive en **Linear** (una vez creado) — este documento explica el *por qué* y el *cómo*, Linear lleva el *qué falta*. Historial de decisiones previas en [plan.md](plan.md).

## Estado actual (2026-08-15) — Pivote a ERP

**Decisión de producto explícita del usuario**: Leadly deja de ser "solo" un CRM conversacional y pasa a construirse como un ERP completo para que el tenant gestione toda su empresa (clientes, oportunidades, productos, inventario, despachos, facturación, cartera/crédito), sin perder nada de lo que ya funciona hoy en producción (contactos, pipeline, catálogo, pedidos/cotizaciones — todo lo documentado en 3.4/3.6/3.7 abajo, que sigue vigente y **no se toca**).

**Reglas del pivote (no re-litigar, decisiones ya cerradas)**:
1. ~~Ninguna tabla `crm_*` existente se renombra~~ — **reabierta y revertida el 2026-08-15** (mismo día, sesión posterior). Ver "Migración de nomenclatura `crm_*` → esquema ERP sin prefijo" más abajo para la decisión vigente.
2. **Las tablas nuevas del ERP van sin prefijo** (`warehouses`, `stock_movements`, `dispatches`, `invoices`, `customer_credit_accounts`, etc.) — ya no tiene sentido seguir prefijando con `crm_` algo que ya no es solo CRM.
3. **Rama `develop`** (creada 2026-08-15, apunta a `main`) es la base de todo este trabajo; cada módulo del ERP se construye en una rama `feature/erp-<modulo>` propia desde `develop`, con PR de vuelta a `develop`. `develop` se mergea a `main` módulo por módulo, solo después de validarlo — no hay una rama/proyecto de Supabase separado para esto (un solo proyecto real `leadly-portal`), así que las migraciones nuevas se aplican directo ahí; es seguro porque son 100% aditivas y cada módulo queda invisible por tenant vía `tenant_enabled_modules` hasta que el superadmin lo habilite manualmente (mismo mecanismo de feature-gating que ya existe, ver la ronda del 2026-08-11 más abajo).
4. **Orden de construcción**: Inventario (bodegas + kardex) → Despachos → Facturas (al cliente del tenant, **no confundir con `payment_invoices`**, que es Leadly facturándole al tenant su suscripción) → Cartera/crédito de clientes. Cada capa depende de datos que produce la anterior.
5. Exponer estos módulos nuevos al tool-calling de IA (Aurora) es una fase deliberadamente posterior (Fase 5), una vez cada módulo esté validado en la UI — mismo patrón ya usado (`ai_skills`/`ai_assistant_skills`/`_shared/aiTools.ts`/`whatsapp-ai-tools`), no se mezcla con la construcción inicial para no multiplicar el alcance de cada PR.

**Migración de nomenclatura `crm_*` → esquema ERP sin prefijo (2026-08-15, rama `feature/erp-core-schema` desde `develop`)**: el usuario pidió reabrir la regla 1 — no alcanza con que lo *nuevo* del ERP vaya sin prefijo, quiere dejar de usar `crm_*` del todo, incluido lo que ya existe (contactos, productos, oportunidades, tareas, ventas, PQR). Se modeló un **esquema paralelo completo sin prefijo**, 1:1 con las 19 tablas `crm_*` vivas (columna por columna, verificado contra todos los `ALTER TABLE` posteriores a cada `CREATE TABLE`, no solo la migración original): `contacts`/`notes`/`contact_addresses` (`20260815010001`), `products`/`product_categories`/`suppliers`/`product_images` (`20260815010002`), `pipelines`/`pipeline_stages`/`opportunities`/`opportunity_stage_history` (`20260815010003`), `tasks` (`20260815010004`), `sales_orders`/`sales_order_items`/`sales_order_payments`/`sales_order_comments` (`20260815010005`), `pqrs`/`pqr_updates`/`attachments` (`20260815010006`). Ya aplicado al proyecto real (`leadly-portal`), sin advertencias en `get_advisors`.

Reglas de esta migración (distintas de la Fase 1 de Inventario, que sí referenciaba `crm_products` por FK):
- **Cero `ALTER`/`DROP` sobre ninguna tabla `crm_*`** — siguen intactas, el frontend y las Edge Functions (`whatsapp-ai-tools`, `Contactos.tsx`, `Productos.tsx`, etc.) las siguen usando exactamente igual, nadie nota el cambio todavía.
- **Sin migración de datos** — las tablas nuevas nacen vacías. Copiar filas es una decisión de cutover explícita aparte, no asumida acá.
- **Esquema autocontenido** — las tablas nuevas se referencian solo entre sí, nunca a una `crm_*`. Consecuencia conocida: `product_stock`/`stock_movements` (Fase 1 Inventario, ya en prod) siguen apuntando a `crm_products`, no a `products` — van a quedar desalineadas hasta un cutover futuro que las repunte.
- **Una sola desviación deliberada del 1:1**: `products` no tiene `stock_quantity`/`reserved_stock` (el contador plano que sí tiene `crm_products`). Como se está rediseñando de cero, se aprovechó para no recrear la doble fuente de verdad que Fase 1 ya dejó pendiente — el stock de `products` vive únicamente en `product_stock`. `sales_order_items` sí lleva `warehouse_id` (nullable) para cuando exista ese trigger de efecto de stock — no se construyó todavía porque depende de cómo Fase 2 (Despachos) defina reservas por bodega, y era mejor no inventar esa semántica ahora.
- `sales_orders` (no `orders` a secas — el ERP va a tener despachos/compras a proveedor más adelante, y "orders" sin calificar es ambiguo entre venta y compra).
- Funciones/triggers con nombre igual al original (`seed_default_pipeline`, `log_opportunity_initial_stage`, `log_opportunity_stage_change`) se portaron con sufijo `_core` (`seed_default_pipeline_core`, etc.) — redefinirlas con `create or replace` habría sobrescrito las que ya usan los triggers de `crm_opportunities`/`tenants` en producción. `seed_default_pipeline_core()` corre en paralelo a `seed_default_pipeline()` (ambos disparan `after insert on tenants`), así que todo tenant nuevo arranca con pipeline poblado en las dos tablas mientras dure la transición.
- **Pendiente explícito, no bloquea nada**: el cutover en sí (frontend/Edge Functions apuntando a las tablas nuevas), la migración de datos existentes, el efecto de stock de `sales_order_items` contra `product_stock`, y la decisión de cuándo (o si) se borran las `crm_*` — todo eso es trabajo futuro después de probar este modelo, no asumido por esta migración.

**Fase 1 (Inventario multi-bodega + kardex) — en construcción, rama `feature/erp-inventory`**: hoy el único modelo de stock es `crm_products.stock_quantity`/`reserved_stock` (un número plano por producto, sin bodegas), mantenido por los triggers existentes sobre `crm_order_items`/`crm_orders` (`adjust_product_stock()` y compañía) que el flujo de ventas por WhatsApp sigue usando tal cual. Tablas nuevas: `warehouses` (una "Bodega Principal" por tenant vía trigger `seed_default_warehouse()`, mismo patrón que `seed_default_pipeline()`), `product_stock` (stock por producto+bodega) y `stock_movements` (kardex append-only, trigger `apply_stock_movement()` mantiene `product_stock` al día). Backfill: una entrada `carga_inicial` por producto con stock actual, sin tocar `crm_products`. La reconciliación futura (si `product_stock` pasa a ser la fuente de verdad única) es una decisión explícita pendiente, no asumida.

**Nota de desfase**: el bloque "Estado actual (2026-08-09)" de abajo también quedó desactualizado — entre esa fecha y este pivote se construyó un módulo de Productos/Ventas completo (`crm_products`, `crm_product_images`, `crm_product_categories`, `crm_suppliers`, `crm_orders`, `crm_order_items`, `crm_order_payments`, `crm_order_comments`, `crm_contact_addresses`; páginas `Productos.tsx`/`ProductoDetalle.tsx`/`Ventas.tsx`/`VentaDetalle.tsx`) y el módulo de Integraciones (Wompi/HubSpot/Shopify/LaFactura). Ninguno de los dos está descrito en las secciones numeradas de abajo todavía — tratar el código como la fuente de verdad hasta que se documenten a fondo.

## Estado actual (2026-08-09)

**Ronda del 2026-08-11 (módulos por tenant)**: nuevo sistema de feature-gating a nivel de tenant, pedido explícito del superadmin — hasta ahora el nav del panel de cliente (`TenantLayout.tsx`) era el mismo catálogo fijo de 14 ítems para todos los tenants, sin forma de restringir qué ve cada uno. `tenant_enabled_modules` (`20260811000001_tenant_modules.sql`) es una tabla puente presence-based (sin catálogo aparte, mismo shape que `ai_assistant_skills`: una fila = módulo habilitado, sin fila = deshabilitado; RLS de escritura solo superadmin, lectura propia + superadmin) contra el catálogo fijo de módulos que ahora vive en `leadly-app/src/lib/modules.ts` (`TENANT_MODULES`, única fuente de verdad para label/ícono/ruta, usada tanto por el nav como por el guard como por la tab nueva). **Los 14 ítems del nav son togglables, incluidos Dashboard/Conversaciones/Configuración** — decisión explícita del superadmin de no dejar ningún módulo protegido de fábrica. El gating es real en dos capas: `TenantLayout.tsx` filtra el nav por `AuthContext.enabledModules` (cargado junto con el perfil/tenant), y cada ruta de `/app` en `App.tsx` está envuelta en el guard nuevo `RequireModule` (`routes/guards.tsx`, mismo patrón "lock en el lugar, no redirect" que `RequireRole`) para que un link directo a una ruta deshabilitada tampoco funcione. Backoffice: tab nueva "Módulos" en `ClienteDetalle.tsx` (`TenantModulesSection.tsx`) con un switch por módulo, aplica al instante (sin botón "Guardar"), mismo patrón que la sección "Habilidades" de IA. **Backfill: al ser presence-based, la tabla arrancó vacía** — decisión explícita del superadmin de que todos los tenants existentes (ya en producción real) quedaran sin ningún módulo habilitado tras aplicar la migración, para reactivarlos él mismo manualmente desde la tab nueva en vez de auto-habilitar todo.

**Nota de desfase**: el bloque "Estado actual (2026-08-06)" de abajo (y la sección 3.6) quedó desactualizado por dos días de trabajo no documentado antes de esta sesión — el modelo B2B (`crm_accounts`/`Empresas.tsx`/`EmpresaDetalle.tsx`) se **revirtió el 2026-08-08** (fusionado de vuelta en `crm_contacts`, ver `leadly-db/supabase/migrations/20260808172403_merge_contacts_accounts.sql`) y se construyó un pipeline completo de catálogo → cotizaciones → ventas (`crm_products`, `crm_orders`/`crm_order_items`, habilidades de IA `catalogo`/`ventas`) entre el 08-08 y el 08-09, ya en producción y afinado con pruebas reales por WhatsApp. Tratar ese bloque como historial, no como estado vigente, hasta que se reescriba a fondo.

**Ronda del 2026-08-09** (seguimiento inteligente de oportunidades + direcciones + ficha de cliente completa):
- **`crm_orders` confirmada → oportunidad "Ganado" centralizado en un trigger de DB** (`20260809170000_crm_orders_confirmed_moves_opportunity.sql`): antes solo pasaba si la IA confirmaba la venta (`confirm_quote` en `whatsapp-ai-tools`) — un agente humano confirmando desde `Ventas.tsx` (`updateOrderStatus`) dejaba la oportunidad desactualizada. Ahora es un trigger `after update of status on crm_orders` que cubre ambos caminos por igual; la función Deno equivalente (`moveLinkedOpportunityToWon`) se borró.
- **Calificación de interés en tiempo real + tarea de seguimiento automática** (`20260809170100_ai_skill_oportunidades_qualified_followup.sql`, nueva tool `flag_interest_for_followup`): pedido explícito del usuario — la IA no puede ofrecer/crear una cotización ante interés ambiguo. Tres niveles: (1) interés ambiguo → sigue creando la oportunidad (barra baja sin cambios) pero en vez de cotizar llama `flag_interest_for_followup` (crea una `crm_tasks` asignada al agente del contacto si tiene uno) y le avisa al cliente que un agente lo va a contactar; (2) intención clara pero falta info → pregunta lo que falta; (3) intención clara y completa → pregunta explícitamente si quiere comprar o prefiere una cotización. `create_quote` ahora exige confirmación explícita del cliente antes de llamarse (`20260809170200_ai_skill_ventas_confirmation_and_notes.sql`) — revierte puntualmente la instrucción "avanzá vos misma sin esperar confirmación" que tenían las rondas anteriores de `ventas`, solo para este paso.
- **Observaciones bidireccionales en la cotización**: `get_quote_status` ahora devuelve `notes` (la IA se las comunica al cliente cuando son relevantes); nueva tool `add_quote_note` para que la IA registre una observación que el cliente da en la conversación, sumándola sin pisar lo que un agente ya escribió.
- **Módulo de direcciones** (`20260809170300_crm_contact_addresses.sql`): tabla nueva `crm_contact_addresses` — libreta por contacto, reutilizable entre cotizaciones (`is_shipping`/`is_billing` en la misma fila, no un enum rígido; `is_default` para no repetir la dirección en cada venta). `crm_orders` ganó `shipping_address_id`/`billing_address_id`. Tools nuevas `list_contact_addresses`/`save_contact_address` bajo la habilidad `ventas` (no una habilidad aparte — solo importan junto al flujo de venta). Prompt (`20260809170400_ai_skill_ventas_addresses.sql`): después de `confirm_quote`, si el pedido implica envío físico, la IA consulta las direcciones guardadas y confirma con el cliente en vez de volver a pedirlas si ya hay una default.
- **`ContactoDetalle.tsx` enriquecido**: pasó de 3 tabs (Actividad/PQR/Conversaciones) a 7 — se agregaron **Resumen** (cards de oportunidades abiertas, ventas confirmadas, cotizaciones pendientes, tareas pendientes, próxima cita; nuevo tab por defecto), **Oportunidades** (abre el `OpportunityPanel` existente), **Ventas** (abre `OrderDrawer`, que ganó un prop `defaultContactId`) y **Direcciones** (CRUD vía `AddressDrawer` nuevo). Esto es lo que reemplaza al viejo `EmpresaDetalle.tsx` (ver nota de desfase arriba) — decisión explícita del usuario de enriquecer Contactos en vez de revivir el modelo B2B. `OrderDrawer` también ganó selects de dirección de envío/facturación y una sección de solo lectura de tareas relacionadas (vía la oportunidad vinculada); `OpportunityPanel` ganó una sección de cotizaciones/ventas vinculadas dentro de su tab Resumen. Todas las listas nuevas están paginadas (regla del proyecto, ver memoria de la sesión).
- **Pendiente**: todo lo anterior compila y pasó `npm run build`, pero el comportamiento conversacional de la IA (los 3 niveles de calificación, el gate de confirmación, el flujo de direcciones) todavía necesita una ronda de prueba real por WhatsApp — mismo patrón que toda la iteración de `ventas`/`oportunidades` de los días previos (ver migraciones `20260809*` de esa ronda). No asumir que el prompt quedó perfecto al primer intento.

## Estado actual (2026-08-06)

**Addenda desde el 2026-08-04** (el bloque original de abajo sigue siendo válido, esto es lo que cambió después):
- **Desplegado en producción real**: `leadly-app` corre en Firebase Hosting (`https://leadly-crm.web.app`, proyecto `lexy-colombia`, sitio `leadly-crm`), pendiente de apuntar el subdominio final `leadly.lexycolombia.com` (GoDaddy). Supabase Auth (Site URL + Redirect URLs) y el SDK de Facebook (dominios permitidos) ya están configurados para esa URL.
- **Meta App Review en curso**: formulario de `whatsapp_business_management`/`whatsapp_business_messaging` completado (procesadores de datos: Supabase/OpenAI/Gemini, todos en EE.UU.; responsable: Lexy Colombia SAS), llamadas de prueba a la Graph API ya ejecutadas. Embedded Signup: plantilla correcta identificada ("token que caduca en 60 días", acceso completo — no la de "solo lectura"), `config_id` obtenido (`992764367102956`) y ya en `.env`. Migrar un número real de la app de WhatsApp Business al Cloud API requiere el flujo de "Crear cuenta de WhatsApp Business" del Embedded Signup con el mismo número (dispara OTP de migración automáticamente) — no hace falta una migración manual aparte.
- **Habilidades de IA (`ai_skills`/`ai_assistant_skills`, 2026-08-06)**: el tool-calling de PQR (antes: mismo set de tools mandado siempre a todos los asistentes, con las instrucciones de uso escritas a mano en cada `system_prompt`) ahora es la primera "habilidad" activable por asistente desde el backoffice. Ver sección 3.5.
- **CRM ampliado (`crm_accounts`, `crm_pipelines`/`crm_pipeline_stages`/`crm_opportunities`, `crm_tasks`, 2026-08-06)**: capas nuevas sobre `crm_contacts` — empresas B2B, oportunidades con pipeline/etapa/valor (reemplaza el `stage` fijo a nivel de contacto para seguimiento de negociaciones reales), y tareas. Backend + UI completos: pantallas **Empresas** (`/app/empresas`), **Oportunidades** (`/app/oportunidades`, lista con cambio de etapa inline, sin kanban todavía) y **Tareas** (`/app/tareas`, con toggle de completada) en el nav del tenant. Decisión explícita: no se migró a un modelo genérico multicanal/multi-pipeline — ver sección 3.6 para el razonamiento.
- **Limpieza de esquema**: `whatsapp_messages.error_message` eliminado (columna muerta desde el fix de "mensajes fantasma" del 2026-08-05 — un mensaje solo se inserta si Meta confirmó la entrega, así que nunca más se escribía ahí).
- **Switches (`components/ui/Switch.tsx`) tenían un bug visual** (el knob se salía del track) que afectaba a toda la app — corregido con un `left-0` explícito.
- **`EmpresaDetalle.tsx` (ficha 360 de Empresa, 2026-08-06)**: reemplazó un primer intento de UI fea/desconectada por un diseño validado por el usuario (header con avatar+badge, tabs con contador, cards de resumen con datos reales). Ver 3.6.
- **🚧 Fase A en curso (empezada el 2026-08-06, no terminada)**: Kanban de Oportunidades pipeline-céntrico — reemplaza la lista simple de `/app/oportunidades` por un tablero drag-and-drop, agrega historial de cambios de etapa (para poder calcular conversión/tiempo promedio real), panel lateral de oportunidad, y saca "Tareas" del nav a favor de un widget en el Dashboard. **Spec completa + checklist de progreso en la sección 3.7 — si estás retomando este proyecto en una conversación nueva, empezá ahí.**
- **Pasada de restyling visual sobre una referencia de diseño (2026-08-06)**: el usuario trajo una captura de referencia del look que quiere para el Kanban de Oportunidades y el sidebar. Se ajustó `Oportunidades.tsx` (barra de métricas como un solo contenedor dividido en vez de tiles sueltos, cards del Kanban con avatar de iniciales + fila de iconos teléfono/correo/calendario + fecha, botón "+ Nueva oportunidad") y `OpportunityPanel.tsx` (chip de empresa + badge de estado sobre las tabs, footer fijo con Editar/Eliminar, barra de progreso en la tab Tareas) — **se mantuvieron exactamente las 3 tabs ya decididas en 3.7** (Resumen/Conversaciones/Tareas), no se agregaron Archivos/Actividad/IA aunque la referencia las mostraba, sigue fuera de alcance. También se alineó el sidebar del tenant a la referencia: "Clientes" pasó a llamarse **"Contactos"** en el nav (mismo componente/ruta `/app/clientes`, solo cambió la etiqueta), se agregó la card de upsell "Prueba Leadly Pro" (`AppShell` ganó un prop `showUpsell`, solo lo usa `TenantLayout`) y se sumaron 4 ítems de nav nuevos como placeholders bloqueados (mismo patrón `LockedFeature` que Campañas/Catálogo): **Calendario**, **Reportes**, **Automatizaciones**, **IA & Agentes** — decisión explícita del usuario de sumarlos ya al nav aunque el motor de automatizaciones y los reportes como módulo propio siguen fuera de alcance de ingeniería real (ver 3.6), es solo para que el nav se vea completo. Pendiente: el usuario todavía tiene que verificarlo en el navegador (sin acceso a navegador en este entorno).
- **Adjuntos en Tareas + tareas visibles en el Kanban (2026-08-06, migración `20260806000012`)**: `crm_attachments` era exclusivo de PQR/seguimientos (solo imágenes, ver 3.4) — se agregó `task_id` (nullable, exactamente uno de `pqr_id`/`pqr_update_id`/`task_id`) para poder adjuntar un archivo a una tarea, ej. "se envió una propuesta". A diferencia de los adjuntos de PQR, acá también se permite `application/pdf` (una propuesta casi seguro es un PDF, no una foto) — el bucket `crm-attachments` amplió su `allowed_mime_types` acorde. Sección nueva "Archivos adjuntos" en `TaskDrawer.tsx`, visible solo al editar una tarea ya existente (necesita el `id` real de la tarea). Además, `Oportunidades.tsx` ahora trae el arreglo completo de tareas del tenant (antes solo el conteo para el tile del header) y muestra una etiqueta con ícono + cantidad de tareas pendientes directamente en cada card del Kanban que tenga alguna — para verla sin tener que abrir el panel lateral (que ya tenía su tab "Tareas" desde la Fase A).
- **`ai_assistants` desacoplado de `whatsapp_lines` (2026-08-06, migración `20260806000011`)**: hasta ahora un agente de IA pertenecía 1:1 a una línea (`ai_assistants.whatsapp_line_id not null unique`) — un tenant con 3 líneas queriendo "el mismo agente de ventas" en las 3 tenía que configurar (y mantener sincronizados a mano) 3 prompts idénticos. Pedido explícito del usuario: "a una línea le puedo asignar un agente, pero este mismo agente lo puedo configurar en varias líneas" — M:1 (varias líneas → un agente), no M:N (una línea nunca tiene más de un agente respondiendo a la vez). Se invirtió la FK: ahora `whatsapp_lines.ai_assistant_id` (nullable) apunta al agente asignado, en vez de que el agente apunte a su única línea. `ai_assistants` ganó `tenant_id` propio (antes derivaba el tenant únicamente a través de la línea) y `name` (ya no tiene sentido identificarlo por "la línea tal", es una persona reutilizable). RLS de `ai_assistants` reescrita para usar `tenant_id` directo, y de paso se abrió `insert`/`delete` al propio tenant (antes solo superadmin, un resabio de cuando crear un agente era un efecto secundario de que superadmin creara una línea) — mismo patrón que `crm_accounts`/`crm_opportunities`. `ai_assistant_skills` no necesitó cambios de esquema (ya estaba modelada correctamente por `ai_assistant_id`, nunca por línea), solo se simplificó su policy de select. Migración 100% aditiva/backfill: cada agente existente sobrevive con su `tenant_id`/`name` derivados de su línea de origen antes de borrar la columna vieja. **Los 3 edge functions que resolvían el agente vía `whatsapp_line_id` (`whatsapp-ai-respond`, `whatsapp-ai-tools`, `whatsapp-embedded-signup`) se actualizaron y redesplegaron en el mismo momento que la migración** — necesario, si no la IA se hubiera quedado sin poder resolver ningún agente apenas se aplicara el `alter table drop column`. `createWhatsappLine` (frontend) sigue auto-provisionando un agente privado al crear una línea nueva (no cambia esa UX), solo que ahora lo enlaza escribiendo `whatsapp_lines.ai_assistant_id` en vez de la relación inversa. UI: `IaAgentes.tsx` (ver debajo) pasó de una tarjeta por línea con "Configurar" inline a dos secciones separadas -- catálogo de Agentes (CRUD real) y Líneas con un selector para asignar/reasignar el agente de cada una.
- **"Asistentes de IA" se movió de Configuración a "IA & Agentes" (2026-08-06)**: esa sección (gestión de líneas de WhatsApp + su asistente, incluyendo "Conectar tu WhatsApp") vivía dentro de `Configuracion.tsx` (tenant) mezclada con el logo y las tags de conversación, aunque el nav ya tenía un ítem "IA & Agentes" separado (agregado el mismo día como placeholder bloqueado). Se sacó entera a `pages/tenant/IaAgentes.tsx`, que ahora es el contenido real de esa ruta (`/app/ia-agentes`, dejó de ser `LockedFeature`). `Configuracion.tsx` (tenant) quedó solo con Logo + Tags de conversación.
- **Dashboard real, backoffice y tenant (2026-08-06)**: reemplaza el placeholder `ComingSoon` del backoffice y agrega un Dashboard nuevo para el tenant (no existía ninguna pantalla de resumen ahí antes, el índice de `/app` siempre fue directamente el Inbox). Nuevo `lib/api/dashboard.ts` con `getBackofficeDashboardStats`/`getTenantDashboardStats` (conteos vía `count: 'exact', head: true`, sin traer filas). Backoffice (`pages/backoffice/Dashboard.tsx`, en la ruta índice `/backoffice`, el nav ya tenía el ítem "Dashboard" apuntando ahí): clientes activos/total, líneas de WhatsApp activas/total, conversaciones con actividad hoy, + lista de clientes recientes. Tenant (`pages/tenant/Dashboard.tsx`, ruta nueva `/app/dashboard`, nav ganó un ítem "Dashboard" primero en la lista -- **decisión deliberada de no tocar el índice de `/app`**, que sigue siendo el Inbox, para no cambiar el landing por defecto sin que el usuario lo pida explícitamente): conversaciones abiertas, "esperando respuesta" (abiertas en modo humano -- no hay una columna de "no leído" real, es la mejor aproximación honesta sin una query pesada por conversación), valor del pipeline abierto, tareas pendientes, + widgets de "Tareas próximas" y "Conversaciones recientes". Pendiente/spike abierto: mover "Tareas" del nav a un widget de este Dashboard sigue sin hacerse (ver 3.7) -- ahora que el Dashboard ya existe, es la próxima decisión a tomar.
- **Colores por etapa del pipeline + demo de tareas del QA tenant (2026-08-06, migración `20260806000010`)**: `crm_pipeline_stages.color` vivía con el mismo gris por defecto para las 6 etapas de todo tenant — ahora cada etapa tiene un color distinto (Nuevo violeta, Contactado azul, Propuesta ámbar, Negociación naranja, Ganado verde, Perdido rojo), aplicado tanto a los pipelines existentes (UPDATE) como a los de tenants futuros (`seed_default_pipeline()` reescrita con los mismos colores). Además, los 6 casos demo de `20260806000009` (Ana Gómez, Carlos Ruiz, Marcela Torres, Andrés Peña, Laura Méndez, Jorge Salas) tenían cero `crm_tasks` y todos compartían la misma fecha de cierre (+15 días) y la misma descripción genérica sin importar la etapa — se agregaron 14 tareas realistas (2 por caso + 2 generales, mezclando pendiente/en_proceso/completada) y se corrigieron fecha de cierre/descripción de cada oportunidad para que tengan sentido con su etapa real (los casos Ganado/Perdido ahora cierran en el pasado, no en el futuro). **Nota de proceso**: se había arrancado por error a construir un tema de "restaurante y casa de banquetes" con empresas/líneas de WhatsApp nuevas para este mismo pedido — el usuario corrigió que no era esa la intención antes de escribir nada en la base, el alcance real era mucho más acotado (tareas sobre lo que ya existía + coherencia).

---

## Estado actual (2026-08-03, actualizado en la sesión de implementación — demo el 2026-08-04)

**Fase 0, Fase 1, Fase 2 (backend + Inbox) y un CRM de contactos completo ya están construidos y probados end-to-end**. Proyecto de Supabase real: `leadly-portal` (ref `kkdtrkfcnyvuefazndnj`, org "Lexy"). Si retomas desde la terminal, esto es lo que ya existe:

- `leadly-db/supabase/migrations/` — 17 migraciones aplicadas (`supabase db push` desde `leadly-db/`): esquema completo de `tenants`/`profiles`/RLS, catálogo `ai_models`, `whatsapp_lines`/`ai_assistants`/`whatsapp_conversations`/`whatsapp_messages` con RLS y hardening de privilegios, `self_register_tenant` (auto-registro), enforcement de desactivación de tenant (`auth_active_tenant_id()`), Vault para el access token de Meta (escritura: `set_whatsapp_line_access_token`; lectura: `get_whatsapp_line_access_token`), campos extendidos de tenant + bucket `tenant-logos`, `whatsapp_conversations`/`whatsapp_messages` en la publicación `supabase_realtime`, y **`crm_contacts`/`crm_notes`** (ver 3.4) con `whatsapp_conversations.contact_id` y una policy de insert para que un agente pueda iniciar una conversación desde el panel (antes solo el webhook, vía service_role, podía crear conversaciones).
- `leadly-db/supabase/functions/` — 5 Edge Functions desplegadas (`supabase functions deploy <nombre> --use-api`, sin Docker): `self-delete-account`, `admin-create-tenant-user`, `whatsapp-webhook` (público, `--no-verify-jwt` — **ojo:** cada redeploy sin ese flag reactiva la verificación de JWT del gateway y rompe el webhook con 401, pasó una vez en esta sesión), `whatsapp-ai-respond` (interno, solo invocable con el service role key), `whatsapp-send-human` (JWT del propio agente/tenant_admin). `whatsapp-webhook` ahora también auto-crea/vincula un `crm_contacts` por teléfono en cada mensaje entrante (select-then-insert, no upsert, para no pisar el nombre si el tenant ya lo editó). Lógica compartida de Meta en `functions/_shared/whatsapp.ts`.
- `leadly-app/` — corre con `npm run dev` (puerto 5173). Auth completa, backoffice con CRUD de Clientes (tenants) + Líneas de WhatsApp anidadas + Asistente de IA + Usuarios, Changelog estático. Panel del tenant (`/app`) ahora es un **CRM real**, no solo un inbox:
  - **Conversaciones** (`/app`, `pages/tenant/Inbox.tsx`): lista + chat con burbujas (contacto/IA/agente), toggle IA/humano en tiempo real, envío manual, botón **"Nueva conversación"** (elige un cliente existente o crea uno nuevo + línea, `pages/tenant/inbox/NewConversationDrawer.tsx`), mobile-first.
  - **Clientes** (`/app/clientes`, `pages/tenant/Contactos.tsx` + `ContactoDetalle.tsx`): listado de contactos del CRM del tenant (buscador, etapa, etiquetas) con drawer crear/editar (`pages/tenant/contacts/ContactDrawer.tsx`), y detalle con info + **notas manuales** (`crm_notes`) + **historial de conversaciones de WhatsApp vinculadas** (deep-link a `/app?c=<id>` que auto-selecciona la conversación en el Inbox).
  - **Campañas** y **Catálogo** (`/app/campanas`, `/app/catalogo`): visibles en el nav con tag "Beta" y una pantalla bloqueada (`pages/tenant/LockedFeature.tsx`) — sin funcionalidad real a propósito, quedan para una fase futura (pausas publicitarias / catálogo de productos de WhatsApp).
  - **Categoría de conversación**: select en el header del chat (venta/soporte/consulta/reclamo/otro, `whatsapp_conversations.category`), visible como badge en la lista de conversaciones.
  - **Mi cuenta** (`/backoffice/perfil` y `/app/perfil`, `pages/shared/MiCuenta.tsx`, enlazado desde el nombre al pie del sidebar en `AppShell`): cualquier usuario logueado edita su nombre/teléfono y cambia su contraseña. No existía ninguna vista de perfil antes del 2026-08-04.
  - **Configuración de IA** (`/backoffice/configuracion`, superadmin únicamente, `pages/backoffice/Configuracion.tsx`): setea las API keys de OpenAI/Gemini **compartidas por toda la plataforma** (no por tenant, ver 1 "API keys de IA") sin usar la terminal. Antes del 2026-08-04 esas keys solo existían como `Deno.env` secrets de Edge Function (`supabase secrets set`) — ahora viven cifradas en Vault (`platform_ai_keys`, mismo patrón write-only que el token de Meta) y `whatsapp-ai-respond` las lee vía `get_platform_ai_key` (RPC solo para `service_role`) en vez de `Deno.env.get`.
  - **Conectar tu WhatsApp (Embedded Signup)** (`/app/asistente`, botón visible solo para `tenant_admin`): permite que un tenant vincule **su propia** cuenta de WhatsApp Business (WABA) en vez de que el superadmin le asigne manualmente una línea del Business Manager de Leadly — decisión de negocio del 2026-08-04: los costos de Meta (plantillas, conversaciones) quedan a nombre del tenant, no de Leadly. Piezas nuevas: `leadly-app/src/lib/metaEmbeddedSignup.ts` (carga el SDK de Facebook, `FB.login` con `config_id`, captura `code`+`waba_id`+`phone_number_id` vía `postMessage`), Edge Function `whatsapp-embedded-signup` (intercambia el `code` por access token con el App Secret — nunca en el browser —, crea la línea + asistente por defecto, guarda el token en Vault, suscribe la app a los webhooks de esa WABA), y `set_whatsapp_line_access_token` ahora acepta `service_role` además de superadmin (antes solo el backoffice podía guardar un token). **Bloqueado hasta que Meta apruebe el "Tech Provider App Review"** (estaba "En revisión" el 2026-08-04) y el usuario cree la "Embedded Signup Configuration" en el dashboard de Meta y me pase el `config_id` (`VITE_META_WHATSAPP_CONFIG_ID`, hoy vacío en `.env`) — el botón ya está construido y probado hasta ese punto (muestra el error claro de qué falta), pero no se puede completar una conexión real todavía. `META_APP_ID` (`2438721193212979`) ya está en `.env`/`VITE_META_APP_ID` y como secreto `META_APP_ID` de Supabase.
- Sistema de diseño propio en `leadly-app/src/components/ui/` — **pendiente**: rediseño visual (referencia nunca compartida) queda diferido al final, decisión explícita del usuario.
- **Linear**: sigue sin verse como conector en esta sesión. Tracking local únicamente — reconstruir backlog desde este documento si se retoma desde la terminal.
- **Pitfalls descubiertos en esta sesión** (ver también sección 6): (1) Edge Functions matan cualquier `fetch()` fire-and-forget no esperado al retornar la `Response` — usar siempre `await`. (2) `SUPABASE_SERVICE_ROLE_KEY` puede venir en formato JWT clásico o `sb_secret_...` — comparar por string, no decodificar como JWT. (3) Redesplegar `whatsapp-webhook` sin `--no-verify-jwt` explícito en cada `deploy` reactiva la verificación de JWT del gateway (no es "sticky" desde el primer deploy).
- **Falta explícitamente**: Dashboard real (backoffice y cliente, hoy placeholder), Changelog como tabla editable, rediseño visual pendiente, pipeline visual tipo kanban para las etapas de `crm_contacts` (hoy solo texto/badge en la tabla y el detalle), y las dependencias externas del usuario para producción real: keys de OpenAI/Gemini y una línea de WhatsApp Business verificada por Meta (todo probado con payloads simulados firmados y credenciales placeholder — falla con gracia como se espera).

---

## 1. Decisiones de producto (cerradas)

| Tema | Decisión | Notas |
|---|---|---|
| Nombre | **Leadly** | |
| Organización de Supabase | **Lexy** | |
| Proyecto de Supabase | `leadly-portal` (ref `kkdtrkfcnyvuefazndnj`), nuevo y dedicado | |
| Integración de WhatsApp | **Meta Cloud API directo** | `graph.facebook.com`, `phone_number_id`, `business_account_id`, webhook propio con `X-Hub-Signature-256` + `verify_token`. No hay agregador (Twilio/360dialog). Implica que cada tenant debe pasar por verificación de negocio de Meta — se documenta como requisito de onboarding, no como bloqueante técnico. |
| Quién paga los costos de Meta (plantillas/conversaciones) | **Cada tenant paga directo a Meta con su propia WABA**, no Leadly (decisión del 2026-08-04, revierte el modelo inicial "una sola WABA de Leadly con clientes como líneas") | Requiere que cada tenant conecte **su propia** cuenta de WhatsApp Business vía Embedded Signup (ver "Conectar tu WhatsApp" en Estado actual) — Meta factura al Business Manager dueño de la WABA, no hay forma de fraccionar el cobro dentro de una WABA compartida. El modelo alternativo más simple (Leadly paga y lo traslada dentro de su propio plan/suscripción) queda descartado por ahora pero documentado como Camino A si se necesita algo más rápido en el futuro. |
| API keys de IA | **Leadly usa keys propias compartidas** (OpenAI y Gemini) | Costeado por Leadly, facturado al tenant por plan (límite de mensajes/tokens). Se deja la puerta abierta a que un tenant traiga su propia key en el futuro (columna nullable, no v1). |
| Proveedor de IA | **Multi-proveedor desde el día 1**: OpenAI (ChatGPT) o Google (Gemini), seleccionable por asistente | Ver sección 3.3 — esto descarta usar el objeto "Assistant" del dashboard de OpenAI como fuente de verdad (Gemini no tiene equivalente), así que la config vive en nuestra propia tabla y las llamadas a los LLM son *stateless* (chat/responses por turno), no threads persistentes del lado del proveedor. |
| Auth | Email/password + Google OAuth (Supabase Auth), **registro libre abierto** | `disable_signup` se deja en `false` a propósito (decisión revertida el 2026-08-02: se evaluó cerrarlo y se decidió lo contrario). Cualquiera puede crear una cuenta; lo que la separa de "tener acceso" es la fila en `profiles` (ver siguiente fila). |
| Multi-tenancy | Dos caminos para crear un tenant, ambos terminan en la misma tabla `tenants`+`profiles`: (1) el superadmin lo crea desde el backoffice y asigna línea de WhatsApp, o (2) **auto-registro**: un usuario nuevo (Google o email/password) sin fila en `profiles` aterriza en la pantalla de onboarding `/create-company`, y al enviar el nombre de su empresa se crea el tenant y queda como su `tenant_admin` vía la función `self_register_tenant` (RPC `SECURITY DEFINER`, `leadly-db/supabase/migrations/20260802000008_self_register_tenant.sql`). Si fue sin querer, puede borrar su cuenta ahí mismo (`self-delete-account`, Edge Function) — solo funciona si esa cuenta *todavía* no tiene perfil, nunca sobre un tenant ya activo. | La app (`AuthContext.unprovisioned` + `RequireAuth`) es la que decide mostrar `/create-company` en vez de contenido real cuando `auth.uid()` no tiene fila en `profiles`. Un tenant auto-registrado queda con el mismo `status='active'` que uno creado por el backoffice — sin distinción por diseño (decisión del usuario). No pueden usar WhatsApp real hasta que el superadmin les asigne una línea manualmente. |

### Spikes que quedan abiertos (no bloquean el MVP, se resuelven en Fase 2+)

- ~~Alcance de "herramientas"/function-calling por tenant~~ — resuelto el 2026-08-06 como "Habilidades" (`ai_skills`, ver 3.5): catálogo activable por asistente, no genérico-configurable ni fijo-por-vertical.
- Modelo de facturación/planes de Leadly a sus tenants (límites de mensajes/tokens). Se diseña en Fase 3.
- Cifrado en reposo de credenciales (Meta token, API keys) — Supabase Vault/`pgsodium`. Se resuelve como parte de Fase 0 (no se puede posponer, son secretos reales).

---

## 2. Estructura del repositorio

Dos carpetas hermanas dentro de `Desktop/Proyectos/Leadly/`:

```
Leadly/
├── CLAUDE.md          ← este archivo
├── plan.md            ← historial de la sesión de origen (no editar, solo referencia)
├── leadly-app/        ← frontend: React 19 + TypeScript + Vite + Tailwind v4
└── leadly-db/         ← Supabase: migraciones SQL, seeds, config de Edge Functions
```

- `leadly-app/`: SPA única con dos layouts protegidos por rol (sección 4). Incluye `CHANGELOG.md` propio (sección 7).
- `leadly-db/`: `supabase/migrations/*.sql`, `supabase/functions/*` (Edge Functions), `supabase/seed.sql`. Sin backend propio — Postgres + RLS + Auth + Storage + Edge Functions cubren todo.

---

## 3. Modelo de datos (núcleo, v1)

**Convención transversal — borrado siempre es soft-delete (2026-08-04):** ninguna acción de "eliminar" desde la app hace un `DELETE` real sobre un registro de negocio (`crm_contacts`, `conversation_tags`, y cualquier tabla nueva con un botón "Eliminar" a futuro). En vez de eso, la tabla tiene `deleted_at timestamptz` + `deleted_by uuid references profiles(id)`, y "eliminar" es un `UPDATE` que los setea; las queries de listado siempre filtran `deleted_at is null`. Motivo: permitir deshacer/auditar, y no perder historial referenciado desde otro lado (ej. un contacto borrado que sigue teniendo conversaciones/mensajes vinculados). **Excepción explícita:** tablas puente muchos-a-muchos sin identidad propia (ej. `conversation_tag_assignments`) — ahí "quitar una asignación" no es un registro de negocio con historial, así que sigue siendo un `DELETE` real. Las policies RLS de `DELETE` en Postgres no se quitan (quedan como backstop para uso directo de superadmin/soporte vía SQL), pero el código de la app nunca las ejercita para las tablas de negocio.

### 3.1 Multi-tenancy y roles
- `tenants` — un negocio cliente de Leadly. Más allá de `name`/`status`, incluye datos reales de identidad legal y contacto (agregados en `20260802000013_tenant_profile_fields.sql` a pedido explícito del usuario, "el formulario de crear empresa es muy corto"): `entity_type` (`persona`|`empresa`), `legal_name` (obligatorio en el formulario si es empresa), `document_type`/`document_number`, `contact_email`/`contact_phone` (obligatorios en el formulario), `country`/`state_province`, `preferred_language` (`es`|`en` — primer paso hacia multi-idioma), `logo_url` (bucket Storage `tenant-logos`, público de lectura, máx. 5MB, RLS de escritura por tenant/superadmin). Estos campos son NULLables a nivel de base de datos (para no romper `self_register_tenant`, que solo pide el nombre) — lo "obligatorio" vive en la validación del formulario (`leadly-app/src/pages/backoffice/useTenantForm.ts`), no en constraints de DB.
- `profiles` — `id` (= `auth.users.id`), `role` (`superadmin` | `tenant_admin` | `tenant_agent`), `tenant_id` (null para superadmin).
- `auth_tenant_id()` / `is_superadmin()` — funciones `SECURITY DEFINER` que resuelven tenant/rol del `auth.uid()` actual, usadas como base de toda policy RLS del proyecto.
- RLS en toda tabla con `tenant_id`: el tenant solo ve sus propias filas; el superadmin ve todo.

### 3.2 WhatsApp
- `whatsapp_lines` — una fila por número de WhatsApp Business asignado a un tenant: `tenant_id`, `phone_number_id`, `business_account_id`, `access_token` (cifrado), `display_name`, `status` (`pending_verification` | `active` | `suspended`).
- `whatsapp_conversations` — `tenant_id`, `whatsapp_line_id`, `contact_phone`, `contact_name` nullable, `mode` (`ia` | `humano`), `status` (`open` | `closed`), `last_message_at`.
- `whatsapp_messages` — ledger inmutable (append-only): `conversation_id`, `direction` (`inbound` | `outbound`), `sender` (`contact` | `ia` | `agent:<profile_id>`), `content`, `wamid`, `tokens_used` nullable, `created_at`. **Solo se inserta si el envío a Meta fue confirmado exitoso** (fix "mensajes fantasma", 2026-08-05) — un envío fallido nunca aparece en el ledger, solo se loguea server-side (`console.error`); por eso ya no existe una columna `error_message` (se eliminó el 2026-08-06, estaba muerta).

### 3.3 Configuración de IA (multi-proveedor)
- `ai_assistants` — un **agente reutilizable por tenant**, no por línea (desacoplado el 2026-08-06, ver "Estado actual"): `tenant_id`, `name`, y `whatsapp_lines.ai_assistant_id` (nullable) es lo que asigna un agente a una línea. El mismo agente puede asignarse a varias líneas a la vez; una línea nunca tiene más de un agente activo respondiendo:
  - `provider` (`openai` | `gemini`)
  - `model` (string libre validado contra una lista permitida por proveedor, ej. `gpt-4.1`, `gpt-4o-mini`, `gemini-2.5-pro`, `gemini-2.5-flash`)
  - `system_prompt` (text)
  - `temperature`, `max_tokens` (opcional, valores por defecto razonables)
  - `is_active` (boolean — permite desactivar la IA sin borrar la config)
- La UI del backoffice/cliente para editar esto es un **select de proveedor + select de modelo dependiente** (el segundo select se filtra según el proveedor elegido), no campos de texto libre — requisito explícito del usuario.
- Las llamadas a IA son *stateless por turno*: se arma el contexto (system prompt + últimos N mensajes de `whatsapp_messages`) y se envía a la API del proveedor correspondiente (Chat Completions/Responses de OpenAI, `generateContent` de Gemini) vía Edge Function. No se depende del objeto "Assistant" ni "Thread" del dashboard de OpenAI.

### 3.4 CRM del tenant (contactos)
Agregado el 2026-08-03 a pedido explícito del usuario ("no quiero nada a medias, piensa en un CRM completo") — un `whatsapp_conversations.contact_phone` suelto no bastaba, cada tenant necesita una ficha de cliente persistente con historial, no conversaciones desechables.

- `crm_contacts` — el lead/cliente **del tenant** (no confundir con `tenants`, que son los clientes de Leadly): `tenant_id`, `full_name`, `phone` (único por tenant), `email`/`company` nullable, `stage` (`lead`|`contactado`|`negociacion`|`cliente`|`perdido`), `tags` (`text[]`), `assigned_to` (nullable, `profiles.id`).
- `crm_notes` — bitácora manual append-only sobre un contacto: `contact_id`, `author_id`, `content`, `created_at`. Sin políticas de update/delete a propósito, igual que `whatsapp_messages`.
- `whatsapp_conversations.contact_id` — FK nullable a `crm_contacts`. `whatsapp-webhook` lo resuelve automáticamente en cada mensaje entrante (busca por `tenant_id`+`phone`, crea el contacto si no existe) — así toda conversación real queda ligada a una ficha de cliente sin que el tenant tenga que hacer nada manual.
- El detalle de un contacto (`/app/clientes/:id`) es donde vive "todo lo que se ha hablado con él": notas manuales + la lista de conversaciones de WhatsApp vinculadas (con link directo al Inbox).
- `crm_pqrs`/`crm_pqr_updates` — casos formales (Petición/Queja/Reclamo) por contacto, con código secuencial por tenant (`PQR-{code}`) y seguimientos anidados con su propio consecutivo heredado (`PQR-{code}-{seq}`); ambos con `created_by_ai` para distinguir lo que hizo la IA de lo que escribió un agente. La IA interactúa con esto mediante tool-calling (`whatsapp-ai-tools`, catálogo compartido en `_shared/aiTools.ts`): crear PQR, agregar nota, agregar seguimiento, cambiar estado y consultar estado, siempre resolviendo "el PQR más reciente del contacto" del lado del servidor (nunca un id que el modelo tenga que recordar entre turnos, ver 3.3).
- `crm_attachments` — imágenes adjuntas a un PQR o a uno de sus seguimientos (exactamente uno de los dos, nunca ambos), solo imágenes por ahora (`mime_type like 'image/%'`), guardando mimetype/peso/nombre original. Bucket privado `crm-attachments` (URLs firmadas, no públicas, a diferencia de `tenant-logos`) porque una foto de un reclamo puede ser sensible. Dos orígenes: un agente la sube manualmente desde el drawer de PQR o el composer de seguimiento en el CRM, o un cliente la manda por WhatsApp -- `whatsapp-webhook` descarga el medio de la Graph API (dos pasos: resolver `url` temporal por `media.id`, después bajar los bytes) y lo sube al bucket; si en ese mismo turno la IA crea o actualiza un PQR, `whatsapp-ai-respond` inyecta esa imagen ya subida como `pending_attachment` (mismo principio de inyección server-side que `tenant_id`/`contact_id`) y `whatsapp-ai-tools` la enlaza al caso. La IA no interpreta el contenido de la imagen (sin visión real) -- solo la guarda y la muestra al agente; queda abierto para una iteración futura si hace falta. Sí puede, en cambio, **reenviársela al cliente**: `get_pqr_status` devuelve los `attachments` (id + mimetype) del caso y sus seguimientos, y la tool `send_attachment` genera una URL firmada de corta duración y la manda por Graph API (`type: image`, `image.link`) -- decisión explícita del usuario (2026-08-05): ante un pedido tipo "compárteme el soporte del reembolso", la IA lo envía sola en vez de derivar siempre a un humano. Solo puede enviar adjuntos del PQR más reciente del contacto (mismo resguardo que el resto de las tools).
- `crm_contacts.stage` (`lead`|`contactado`|`negociacion`|`cliente`|`perdido`) sigue existiendo como clasificación general del contacto, independiente de sus oportunidades — ver 3.6, que agrega seguimiento real de negociaciones (valor, etapa de pipeline, fecha de cierre) sin reemplazar este campo.
- Pendiente/spike abierto: vista de pipeline tipo kanban con drag-and-drop para `crm_opportunities` (hoy la data existe — ver 3.6 — pero no hay UI todavía, ni siquiera en lista).

### 3.5 Habilidades de IA (`ai_skills`)
Agregado el 2026-08-06 — generaliza el tool-calling de PQR (antes hardcodeado: mismo set de *tools* mandado siempre a **todos** los asistentes, con las instrucciones de uso escritas a mano en cada `system_prompt`, ej. el prompt de "Tenant QA Uno") a un catálogo activable por asistente.

- `ai_skills` — catálogo fijo (`key`, `name`, `description`, `prompt_fragment`). Agregar una habilidad nueva siempre requiere código (una tool nueva en `_shared/aiTools.ts` con su `skill` + un caso en `whatsapp-ai-tools`) — esta tabla solo controla el lado de *display* + *prompt*, nunca la implementación.
- `ai_assistant_skills` — tabla puente (`ai_assistant_id`, `skill_key`), sin identidad de negocio propia → "desactivar" es un `DELETE` real (misma excepción que `conversation_tag_assignments`, ver la convención de soft-delete arriba).
- **La activación es real, no solo texto**: `whatsapp-ai-respond` arma el `tools` que le manda a OpenAI/Gemini filtrado por las habilidades activas del asistente (`toolsForSkills`) — si una habilidad está apagada, el modelo no puede llamar esas funciones, es imposible, no solo "se le pide que no lo haga". El `system_prompt` final = prompt propio del negocio + `prompt_fragment` de cada habilidad activa, concatenados. `whatsapp-ai-tools` tiene además un chequeo de defensa en profundidad (`isToolAllowed`) por si algo llama una tool de una habilidad deshabilitada.
- UI: sección "Habilidades" dentro del drawer "Asistente de IA" (`AiAssistantDrawer.tsx`, prop `manageSkills`), **solo visible en el backoffice** (`ClienteDetalle.tsx`) — es el superadmin quien se la habilita al cliente, el tenant no se autogestiona esto. Toggle por habilidad, aplica al instante (no depende del "Guardar cambios" del resto del formulario).
- Único skill hoy: `pqr` (las 6 tools de PQR/notas/adjuntos de la sección 3.4). Backfill: todos los asistentes que existían antes de esta migración quedaron con `pqr` activo (mismo comportamiento que tenían de facto). Asistentes nuevos arrancan sin ninguna habilidad — elección explícita del superadmin desde ahí en más.

### 3.6 CRM ampliado: Empresas, Oportunidades y Tareas
Agregado el 2026-08-06 a partir de un modelo de datos más completo que trajo el usuario (inspirado en CRMs genéricos tipo HubSpot) — se evaluó sección por sección qué cerraba un vacío real vs. qué era sobre-alcance para el estado actual del producto. Ver el análisis completo en el historial de la sesión; resumen de las decisiones:

- **`crm_accounts`** — la capa B2B que faltaba: hasta ahora `crm_contacts.company` era texto libre, no una entidad (no se podían agrupar varios contactos de la misma empresa cliente, ni ver sus oportunidades juntas). `crm_contacts.account_id` (nullable) enlaza un contacto a su empresa — B2C/individual sigue sin necesitar uno. `crm_contacts.company` se deja intacto por ahora (dato histórico ya cargado); no se migró automáticamente a filas de `crm_accounts`.
- **`crm_pipelines` / `crm_pipeline_stages` / `crm_opportunities`** — reemplaza la limitación de que un contacto solo podía estar en **un** `stage` fijo a la vez, sin valor ni fecha de cierre. Una oportunidad tiene `pipeline_id`/`stage_id`/`contact_id`/`account_id` (opcional)/`owner_id`/`value`/`currency`/`priority`/`expected_close_date`/`status` (`open`|`won`|`lost`, derivado de si la etapa tiene `is_won`/`is_lost`). **Decisión deliberada de mantenerlo simple**: cada tenant tiene un único pipeline "Ventas" con 6 etapas fijas (Nuevo/Contactado/Propuesta/Negociación/Ganado/Perdido), creado automáticamente por un trigger `after insert on tenants` (`seed_default_pipeline`) — no un constructor de pipelines genérico multi-pipeline configurable. Las tablas ya soportan más de un pipeline por tenant (sin constraint que lo impida), así que agregar esa UI en el futuro no requiere otra migración, pero no se construyó ahora sin un caso de uso real que lo pida.
- **`crm_tasks`** — no existía ningún tipo de tarea/seguimiento antes. `contact_id`/`opportunity_id` ambos nullable (una tarea puede ser general, sobre un contacto, o sobre una negociación puntual).
- **Todas con el mismo patrón que `crm_contacts`**: `tenant_id` + RLS vía `auth_active_tenant_id()`, soft-delete (`deleted_at`/`deleted_by`), trigger `set_updated_at`.
- **Explícitamente fuera de esta ronda** (evaluados y descartados por ahora, no por ser mala idea sino por evitar sobre-construir sin un caso de uso real todavía):
  - Motor de automatizaciones genérico (no-code workflow builder) — se prefiere seguir agregando automatizaciones puntuales *hardcodeadas* como las que ya existen (recordatorios de citas, reset de contexto, retry de IA, habilidades) hasta que haya patrones repetidos reales entre tenants que justifiquen un motor configurable.
  - Base de conocimiento con embeddings (RAG) para los asistentes — inversión de ingeniería seria aparte (pgvector, chunking, pipeline de embeddings), se evalúa cuando un cliente concreto lo pida.
  - `products`/`quotes`/`quote_items` (catálogo + cotizaciones formales) — "Catálogo" ya está reservado a propósito para el catálogo nativo de WhatsApp (Beta bloqueado, ver 4.2), no para un sistema de cotización/facturación propio.
  - Generalizar `whatsapp_conversations`/`whatsapp_messages` a multicanal (`channel` genérico) — toda la infraestructura actual (webhook, Embedded Signup) está construida 100% alrededor de WhatsApp Cloud API; generalizar ahora es pagar complejidad por adelantado sin necesidad real de otro canal todavía.
  - Reportes como tabla propia — se construyen consultando `crm_opportunities`/`whatsapp_conversations`/`crm_tasks` directamente cuando se aborde el Dashboard real, nunca una tabla de "reportes" nueva.
- **UI**: `pages/tenant/Empresas.tsx` (+ `accounts/AccountDrawer.tsx`), `pages/tenant/Oportunidades.tsx` (+ `opportunities/OpportunityDrawer.tsx`, cambio de etapa inline con un `Select` por fila, actualiza `status` automáticamente vía `moveOpportunityToStage`), `pages/tenant/Tareas.tsx` (+ `tasks/TaskDrawer.tsx`, checkbox para completar). Todas siguen el mismo patrón que `Contactos.tsx`/`ContactDrawer.tsx` (lista + drawer, soft-delete con confirmación inline). **Pendiente explícito**: vista kanban con drag-and-drop para Oportunidades (hoy es una lista simple con filtro por etapa).
- `pages/tenant/EmpresaDetalle.tsx` (`/app/empresas/:id`, 2026-08-06) — ficha 360 de la empresa: header (avatar, badge `is_active`, botón "Editar" solo como el lápiz junto al nombre — **no duplicar ese botón en ningún lado más**, feedback explícito del usuario), fila de datos de contacto, tabs con contador (`Resumen`/`Contactos (n)`/`Oportunidades (n)`/`Conversaciones`/`Tareas`/`Cotizaciones` bloqueado con `LockedFeature`). El tab "Resumen" arma tiles/cards a partir de datos reales (`crm_opportunities`, `crm_tasks`, `whatsapp_conversations` de los contactos vinculados) — el tile "Cotizaciones enviadas" se deja en 0/"Próximamente" a propósito, no se fabrica dato. **Textos de botón cortos** (`+ Contacto`, `+ Oportunidad`, no "Nueva/Vincular X") y **todo link a un contacto abre en pestaña nueva** (`<a target="_blank">`, nunca `navigate()`) son decisiones explícitas del usuario para esta pantalla — replicar el mismo criterio en pantallas nuevas similares.
  - `crm_accounts.notes` y `crm_accounts.is_active` (migraciones `20260806000006`/`...007`) se agregaron específicamente para que el card "Notas internas" y el badge "Cliente activo" del diseño de referencia del usuario fueran datos reales, no inventados.
  - Eliminar una empresa (`deleteAccount`) ya es soft-delete real, pero **falta decidir qué pasa con los contactos/oportunidades vinculados** al eliminarla (hoy quedan huérfanos pero visibles, sin soft-delete en cascada) — pendiente explícito, no bloquea nada hoy.

### 3.7 Fase A: Kanban de Oportunidades pipeline-céntrico (spec, 2026-08-06 — implementación arrancando)

El usuario propuso (y se validó en sesión) reorientar el CRM alrededor del Pipeline en vez de tener módulos aislados de Tareas/Actividades/Automatizaciones — inspirado en, pero deliberadamente más simple que, HubSpot/Kommo/Pipedrive. **Se evaluó la propuesta completa del usuario y se acordó dividirla en fases** porque mezclaba cosas ya alcanzables con 2-3 proyectos de ingeniería grandes por separado. Esta sección documenta **solo la Fase A** (la parte que se aprobó para construir ahora) — si retomás esto en otra conversación, este es el punto de partida.

**Fuera de alcance a propósito (Fases B/C/D, no tocar en esta ronda):**
- **Motor de automatizaciones visual tipo n8n** (nodos "Cuando entra a etapa → esperar N días → condicional → acción"). Es un proyecto de meses (motor de ejecución con estado, scheduling, branching, editor visual) — se sigue prefiriendo automatizaciones puntuales hardcodeadas (como las que ya existen: recordatorios de citas, reset de contexto, retry de IA, habilidades) hasta que haya patrones repetidos reales entre tenants.
- **IA por oportunidad** ("Aurora": resumen de la conversación + sugerencias accionables tipo "Ofrecer descuento"/"Agendar llamada"). Es una feature de IA nueva y separada (nuevo prompt, nuevo Edge Function, costo por generación) — proyecto propio después de que el Kanban esté andando.
- **Cotizaciones y Archivos por oportunidad** — ya estaban descartados para esta ronda (ver 3.6), la propuesta los reintroduce como tabs del panel de oportunidad; siguen descartados.

**Sí en alcance para la Fase A:**

1. **`crm_opportunity_stage_history`** (tabla nueva) — sin esto, "Conversión" y "Tiempo promedio" en el dashboard del pipeline serían números inventados (hoy solo existe el estado *actual* de cada oportunidad, no cuándo cambió de etapa). Columnas: `id`, `opportunity_id references crm_opportunities`, `from_stage_id references crm_pipeline_stages nullable` (null en la primera fila, al crearse la oportunidad), `to_stage_id references crm_pipeline_stages`, `changed_by uuid references profiles nullable`, `created_at`. Se inserta una fila cada vez que `crm_opportunities.stage_id` cambia — hacerlo con un trigger `after update` en `crm_opportunities` (comparando `old.stage_id` vs `new.stage_id`), no confiar en que el frontend siempre recuerde insertar el log. Con esta tabla: "tiempo promedio" = promedio de `created_at` entre entrada y salida de cada etapa; "conversión" = % de oportunidades cuyo `to_stage_id` más reciente tiene `is_won = true` sobre el total cerrado (ganado+perdido) en un período.
2. **Vista Kanban** reemplazando/extendiendo `Oportunidades.tsx` — columnas por `crm_pipeline_stages.display_order`, tarjetas arrastrables entre columnas (evaluar una librería liviana de drag-and-drop tipo `@dnd-kit/core` — no hay ninguna instalada en el proyecto todavía, revisar bundle size antes de sumarla dado el warning de chunk grande que ya tiene `vite build`). Al soltar una tarjeta en otra columna: llama `moveOpportunityToStage` (ya existe, en `lib/api/opportunities.ts`) — el trigger de arriba registra el historial solo. Encabezado del pipeline: selector de pipeline (hoy siempre va a ser "Ventas", el único que existe — el selector es de cara al futuro, no funcional todavía) + métricas (Valor total, N° oportunidades, Conversión, Tiempo promedio, Tareas pendientes) calculadas con datos reales.
3. **Panel lateral de oportunidad** (Drawer, no navegación a otra ruta) con tabs: Resumen (campos: pipeline, etapa, responsable, valor, probabilidad, fecha estimada, etiquetas si se agregan, descripción), Conversaciones (reusar `listConversationsForContact` del contacto de la oportunidad), Tareas (usar `crm_tasks.opportunity_id`, ya existe la columna). Nada de tabs Automatizaciones/Archivos/IA todavía (fuera de alcance, ver arriba).
4. **Nav**: sacar "Tareas" del nav del tenant (`TenantLayout.tsx`) — las tareas se acceden desde el panel de la oportunidad/contacto, no como módulo aislado. A cambio, un widget "Mis tareas" en el Dashboard del tenant. **Esto depende de que el Dashboard real exista** — hoy sigue siendo placeholder (`ComingSoon.tsx` o similar, ver Fase 1 en el roadmap de la sección 8) — construir ese widget probablemente implica construir el Dashboard real al mismo tiempo, o al menos una versión mínima de él. Evaluar si conviene primero un Dashboard mínimo (solo el widget de tareas) antes de sacar "Tareas" del nav, para no dejar esa funcionalidad sin ningún punto de entrada mientras tanto.
5. **`Empresas.tsx`/`EmpresaDetalle.tsx`**: la tab "Oportunidades" de una empresa puede seguir siendo la tabla simple que ya existe (no hace falta un mini-kanban ahí también) — el Kanban completo vive en `/app/oportunidades`.

**Checklist de implementación** (marcar acá a medida que avance, para que quien retome sepa exactamente dónde quedó):
- [x] Migración `crm_opportunity_stage_history` + trigger de log automático (`20260806000008_crm_opportunity_stage_history.sql` — dos triggers: uno en `insert` para la etapa inicial, otro en `update` cuando cambia `stage_id`; RLS de solo lectura, nadie inserta a mano)
- [x] Backfill: una fila inicial por cada `crm_opportunities` existente (con `from_stage_id = null`, `to_stage_id = stage_id` actual) — verificado en Supabase, corrió sobre la única oportunidad que existía hoy
- [x] `lib/api/opportunities.ts`: `listStageHistoryForOpportunities` + `computePipelineMetrics` (función pura, testeable aparte de cualquier componente)
- [x] Componente Kanban — implementación manual con HTML5 Drag and Drop nativo (`draggable` + `onDragStart`/`onDragOver`/`onDrop`, usando `dataTransfer.setData('text/plain', opportunity.id)` para pasar el id, **no** un `useState` — se probó que depender de estado de React para identificar qué tarjeta se soltó tiene condición de carrera con el ciclo de eventos nativos del navegador). Sin librería nueva, evita el warning de bundle grande que ya tenía el proyecto.
- [x] Reemplazar `Oportunidades.tsx` (lista) por la vista Kanban + métricas del header (`computePipelineMetrics` en `lib/api/opportunities.ts`, `null` en vez de `0` cuando todavía no hay datos para "Conversión"/"Tiempo promedio" — mostrar "—" en la UI en ese caso). **Pitfall real encontrado**: la consulta a `crm_opportunity_stage_history` embebiendo `crm_pipeline_stages` fallaba silenciosamente porque la tabla tiene dos FKs a `crm_pipeline_stages` (`from_stage_id`/`to_stage_id`) — PostgREST necesita el hint explícito `to_stage:crm_pipeline_stages!to_stage_id(...)`, si no, es ambiguo.
- [x] Configuración de pipelines (2026-08-06, ronda aparte antes del panel lateral -- el usuario notó que crear un pipeline nuevo no tenía UI): `lib/api/pipelines.ts` (CRUD de `crm_pipelines`/`crm_pipeline_stages`, sin migraciones nuevas -- el schema ya soportaba múltiples pipelines por tenant). En `Oportunidades.tsx` el chip "Pipeline: X" pasó a ser un popover real para cambiar/crear pipeline (crear solo `tenant_admin`), con un ícono de engranaje al lado que abre `opportunities/PipelineSettingsDrawer.tsx` (nombre/color/descripción del pipeline, etapas con autosave por campo, reordenar con flechas, eliminar etapa/pipeline con `ConfirmDialog`). `listOpportunities` ganó un filtro `pipelineId` para que el tablero y las métricas no mezclen datos entre pipelines cuando hay más de uno.
- [x] Panel lateral de oportunidad -- `opportunities/OpportunityPanel.tsx`, exactamente 3 tabs (Resumen/Conversaciones/Tareas, nada de Archivos/Actividad/IA todavía, sigue fuera de alcance). Resumen es de solo lectura (Pipeline, Etapa, Responsable, Probabilidad, Prioridad, Cierre estimado, Origen, Creada el, Descripción) con botones "Editar" (abre el `OpportunityDrawer` existente, no se duplican campos editables) y "Eliminar" (`ConfirmDialog`); Conversaciones reusa el mismo patrón que `ContactoDetalle.tsx` (`listConversationsForContact`); Tareas usa la nueva `listTasksForOpportunity` + `TaskDrawer` (que ganó un prop `defaultOpportunityId`, mismo patrón que `defaultAccountId`).
- [x] Resto del header del mockup, todo en la misma ronda que el panel: toggle Kanban/Lista (`opportunities/OpportunityListView.tsx`, tabla con select de etapa por fila igual que la lista pre-Kanban), filtro "Responsable" y "Ordenar por" (siempre visibles), botón "Filtros" con Prioridad (popover, mismo patrón que `Inbox.tsx`), y breadcrumb Empresas/{empresa}/Oportunidades vía `?account=<id>` en la URL (mismo patrón `?empresa=<id>` de `Contactos.tsx`) con un link "Ver en Kanban" nuevo en la sección Oportunidades de `EmpresaDetalle.tsx`.
- [ ] Definir si el Dashboard real se construye ahora (aunque sea mínimo) o si "Tareas" se queda en el nav un poco más hasta que exista un lugar para el widget -- **decisión tomada el 2026-08-06: se queda en el nav por ahora**, es un proyecto aparte.
- [ ] Sacar "Tareas" del nav una vez el widget de Dashboard exista -- depende del punto anterior, no entra todavía.
- [ ] Probar todo en navegador antes de dar la fase por cerrada -- pendiente, lo prueba el usuario (sin acceso a navegador en este entorno).

---

## 4. Layouts y navegación (guard por rol)

Dos layouts, cada uno con su propio árbol de rutas, protegidos tanto por **guard de navegación** (redirección si el rol no corresponde) como por **RLS en la base de datos** (defensa en profundidad — nunca confiar solo en el guard de frontend):

### 4.1 `BackofficeLayout` (rol `superadmin`)
- Dashboard: resumen de tenants activos, líneas de WhatsApp, conversaciones del día.
- **Clientes (tenants)**: CRUD — crear, ver detalle, activar/desactivar, ver plan.
- **Líneas de WhatsApp**: CRUD, asignación a un tenant, estado de verificación de Meta.
- **Configuración de IA por línea**: system prompt (editor de texto largo) + selects de proveedor/modelo + parámetros opcionales, con vista previa/chat de prueba antes de guardar.
- **Usuarios por tenant**: crear usuarios (`tenant_admin`/`tenant_agent`) vía Edge Function `admin-create-tenant-user`.
- **Changelog** (sección 7): ver el historial de versiones publicado, visible también para tenants.

### 4.2 `TenantLayout` (roles `tenant_admin` / `tenant_agent`)
- Dashboard simple: conversaciones abiertas, mensajes sin responder (aún placeholder).
- **Conversaciones** (Inbox): lista + vista de chat estilo WhatsApp Web (burbujas, orden cronológico).
  - Toggle **modo IA / modo humano** por conversación — al pasar a humano, la IA deja de responder automáticamente y el agente escribe directo.
  - Historial completo de mensajes, incluyendo los que respondió la IA (marcados visualmente distinto a los del agente humano).
  - **Nueva conversación**: el agente puede iniciar una conversación él mismo (elige un cliente del CRM o crea uno nuevo + línea), no solo reaccionar a mensajes entrantes.
- **Clientes** (CRM, ver 3.4): listado de contactos con buscador/etapa/etiquetas, ficha de detalle con info + notas manuales + historial de conversaciones vinculadas.
- **Campañas** / **Catálogo**: visibles en el nav con tag "Beta", pantalla bloqueada sin funcionalidad — reservado para pausas publicitarias y catálogo de productos, no es parte del alcance actual.
- **Configuración de IA de su(s) línea(s)**: mismo componente de selects proveedor/modelo + system prompt que en backoffice, pero acotado a su propio tenant (RLS).
- **Changelog**: versión de solo lectura, mismo componente que en backoffice.

### 4.3 Guards
- Guard de ruta (`RequireAuth` + `RequireRole`) que redirige según `profiles.role`.
- La navegación (sidebar/menú) se arma condicionalmente según el rol; nunca se renderiza un link a una ruta que el guard vaya a bloquear.

---

## 5. Diseño / UX

- Diseño simple, moderno, **mobile-first** — el panel de cliente debe usarse cómodamente desde el teléfono (revisar conversaciones y responder en modo humano es el caso de uso más probable en movilidad).
- Paleta e identidad: usar los assets de marca ya definidos (azul principal `#101A35`, turquesa `#2FA9A5`, tipografía Manrope/Inter) — ver brandbook compartido.
- Componentes reutilizables entre ambos layouts donde aplique (selects, tablas, chat), variando solo el scope de datos por rol.

---

## 6. Edge Functions (v1)

Todas desplegadas y probadas end-to-end (webhook simulado con payload de Meta firmado + curl directo). Sin keys reales de OpenAI/Gemini ni línea verificada por Meta todavía, así que las llamadas externas fallan con gracia (mensaje de fallback al contacto si el envío se puede confirmar; si el envío mismo falla, no se inserta nada en `whatsapp_messages`, solo se loguea server-side, ver 3.2) — comportamiento esperado hasta que el usuario aporte esas credenciales.

- `whatsapp-webhook` — ✅ handshake `GET` + mensajes entrantes `POST`, valida firma (`X-Hub-Signature-256`) y `verify_token`, resuelve/crea conversación (`upsert` por `whatsapp_line_id`+`contact_phone`), inserta mensaje inbound, e invoca `whatsapp-ai-respond` (con `await`, ver pitfall en "Estado actual") cuando la conversación está en modo IA y la línea activa. Desplegada con `--no-verify-jwt` (endpoint público, Meta no manda JWT).
- `whatsapp-ai-respond` — ✅ interno, solo acepta llamadas cuyo bearer token sea exactamente el `SUPABASE_SERVICE_ROLE_KEY` del proyecto. Arma contexto (últimos 10 mensajes), detecta traspaso a humano por frase antes de llamar al LLM, llama al proveedor configurado (`ai_assistants.provider`), responde por Graph API. No se ejecuta si la conversación está en modo humano, la línea no está activa, o el asistente está inactivo. **Cumplimiento Meta implementado** (política AI-Assisted Business Messaging Guidelines, vigente desde 2026-01-15): divulga que es IA en el primer mensaje automático de cada conversación (`alreadyRespondedByAi` chequea el historial), y frases tipo "hablar con humano" cambian `mode` a `humano` automáticamente (`_shared/whatsapp.ts::requestsHumanHandoff`). Pendiente/spike abierto: validación/aviso en el editor de `system_prompt` para que un tenant no lo configure como asistente genérico sin límite de tema.
- `whatsapp-send-human` — ✅ envío de mensaje manual desde el panel del tenant en modo humano; usa el JWT del propio caller (no admin), así que RLS aplica tal cual sin lógica de autorización reinventada. Desde 2026-08-05 también acepta un `media` opcional (`storage_path`/`mime_type`/`size_bytes`, subido antes por el agente al bucket `crm-attachments` con su propio JWT) para adjuntar una imagen al mensaje saliente: la función firma una URL de corta duración (Meta necesita poder descargarla ella misma) y llama a la Graph API con `type: image`, mismo patrón que `send_attachment` de la IA (ver 3.4).
- `admin-create-tenant-user` — ✅ creación de usuarios de un tenant, invita por correo (`inviteUserByEmail`).

---

## 7. Changelog del producto

- `leadly-app/CHANGELOG.md`, formato [Keep a Changelog](https://keepachangelog.com/) + versionado semántico, empezando en `0.1.0` para el MVP.
- Se expone también dentro de la app (sección "Novedades" visible en ambos layouts, componente `Changelog`) — **decidido en Fase 1**: sigue siendo el archivo estático `CHANGELOG.md`, no una tabla editable.

---

## 8. Roadmap por fases (MVP = Fases 0-2)

### Fase 0 — Fundación ✅ completa
- [x] Proyecto Supabase `leadly-portal` en la org "Lexy".
- [x] Scaffold `leadly-app/` (Vite+React+TS+Tailwind) y `leadly-db/` (migraciones).
- [x] Esquema: `tenants`, `profiles`, `auth_tenant_id()`/`is_superadmin()`, RLS base.
- [x] Auth: email/password + Google OAuth + registro libre + onboarding de auto-registro.
- [x] `BackofficeLayout` + `TenantLayout` con guards de ruta, sidebar colapsable y navegación condicional por rol.

### Fase 1 — Backoffice del superadmin (mayormente completa)
- [x] CRUD de tenants (formulario completo, ver 3.1).
- [x] CRUD de líneas de WhatsApp + asignación a tenant — vive **dentro** del detalle de cada Cliente, no como sección aparte del menú (decisión del usuario).
- [x] Cifrado del access token de Meta (Supabase Vault).
- [x] Configuración de IA (selects proveedor/modelo dependientes + system prompt) por línea, en backoffice y en panel del cliente (`/app/asistente`).
- [x] Creación de usuarios por tenant (`admin-create-tenant-user`, invita por correo con `inviteUserByEmail` — nunca se maneja una contraseña directamente) — pantalla "Usuarios" tanto en backoffice (dentro de cada Cliente) como en panel del cliente (`tenant_admin` únicamente).
- [x] Changelog visible en la app (archivo estático `CHANGELOG.md`, no tabla editable todavía).
- [x] Dashboard real (backoffice y cliente) — implementado el 2026-08-06, ver addenda de "Estado actual" para el detalle.

### Fase 2 — Panel del cliente + conversación en vivo + CRM ✅ completa (pendiente de credenciales reales del usuario para probar en producción)
- [x] Webhook de WhatsApp (`whatsapp-webhook`) + recepción de mensajes — probado con payloads de Meta simulados y firmados.
- [x] Edge function de respuesta con IA multi-proveedor (`whatsapp-ai-respond`) + envío manual (`whatsapp-send-human`) — probados end-to-end; fallan con gracia sin `OPENAI_API_KEY`/`GEMINI_API_KEY` reales ni token de Meta real, como se esperaba.
- [x] Inbox de conversaciones del tenant (`/app`, `leadly-app/src/pages/tenant/Inbox.tsx` + `pages/tenant/inbox/`) + toggle IA/humano en tiempo real (Supabase Realtime) + envío manual + "Nueva conversación", mobile-first.
- [x] CRM de contactos (`crm_contacts`/`crm_notes`, ver 3.4): listado + detalle con notas e historial de conversaciones, auto-creación de contacto desde el webhook. Agregado el 2026-08-03, un día antes de una demo del usuario — "no quiero nada a medias, piensa en un CRM completo" (se investigó qué ofrecen Kommo/Rasayel/Chatwoot para no dejar cosas básicas fuera: contacto único con historial completo, etapas tipo pipeline, notas, inbox unificado — todo cubierto salvo el tablero kanban visual).
- [x] Campañas y Catálogo visibles en el nav como "Beta" bloqueado (`pages/tenant/LockedFeature.tsx`) — a propósito sin funcionalidad, para que la demo no se sienta con huecos en el menú sin prometer algo que no existe.
- **Fin del MVP** funcionalmente completo — falta solo que el usuario aporte keys de IA reales y una línea de WhatsApp verificada por Meta para probar con tráfico real. A partir de aquí, priorizar con el usuario según feedback real de los primeros tenants.

### Fase 3+ (post-MVP, no detallado en Linear todavía)
- [~] **WhatsApp Embedded Signup** (self-service, cada tenant conecta su propia WABA) — código construido y desplegado el 2026-08-04. `config_id` ya obtenido (`992764367102956`) y en `.env`. Falta: que termine de propagar el dominio (`leadly.lexycolombia.com`) y que Meta apruebe el App Review de `whatsapp_business_management`/`whatsapp_business_messaging` (formulario + llamadas de prueba ya completados el 2026-08-05/06).
- [x] Function-calling / herramientas por tenant — generalizado el 2026-08-06 como "Habilidades" (`ai_skills`, ver 3.5), activables por asistente desde el backoffice. Único skill hoy: PQR.
- [x] Infraestructura de despliegue — `leadly-app` corre en Firebase Hosting (`leadly-crm.web.app`, proyecto `lexy-colombia`), pendiente solo el subdominio final. CI/monitoreo/backups siguen sin resolver.
- [~] **CRM ampliado** (`crm_accounts`, `crm_pipelines`/`crm_pipeline_stages`/`crm_opportunities`, `crm_tasks`, ver 3.6) — backend + UI (lista, no kanban) completos el 2026-08-06: pantallas Empresas/Oportunidades/Tareas en el nav del tenant, más `EmpresaDetalle.tsx` (ficha 360 de empresa). Falta: lo de la Fase A de abajo.
- [ ] **Fase A: Kanban de Oportunidades pipeline-céntrico** — spec completa en la sección 3.7, arrancando el 2026-08-06. Checklist de implementación ahí mismo (tabla de historial de etapas, vista Kanban, panel lateral de oportunidad, mover "Tareas" del nav a un widget de Dashboard). **Si retomás este proyecto en otra conversación, empezá leyendo la sección 3.7.**
- Campañas (pausas publicitarias / envíos masivos) y Catálogo de WhatsApp — hoy solo bloqueados con tag "Beta" en el nav del tenant, sin ningún backend.
- Planes y facturación (`plans`/`subscriptions`).
- Motor de automatizaciones genérico (no-code workflow builder) — evaluado y diferido a propósito el 2026-08-06, ver 3.6. Se sigue prefiriendo agregar automatizaciones puntuales hardcodeadas caso por caso.
- Base de conocimiento con embeddings (RAG) para los asistentes de IA — evaluado y diferido el 2026-08-06, ver 3.6.
- Cotizaciones/catálogo de productos propio (`products`/`quotes`) — evaluado y diferido el 2026-08-06, ver 3.6 (distinto del Catálogo nativo de WhatsApp, que ya está reservado como Beta bloqueado).
- Cumplimiento (ventana 24h de WhatsApp, plantillas, habeas data).

---

## 9. Cómo se trackea el trabajo

**Linear es la fuente de verdad del backlog** (evita reprocesos si se usa otra IA o si el usuario avanza manualmente). Este `CLAUDE.md` explica arquitectura y decisiones; Linear lleva el estado de cada tarea. Antes de empezar a implementar cualquier tarea, revisar su estado en Linear — si ya está "In Progress" o "Done" fuera de esta sesión, no reprocesar sin confirmar con el usuario.
